[pytest]
markers =
    gpu: real-inference smoke needing a GPU + frozen checkpoint (Hard Rule 1, not mocked); excluded by -m "not gpu"
    network: requires internet access (skipped by default; gate via env var)
