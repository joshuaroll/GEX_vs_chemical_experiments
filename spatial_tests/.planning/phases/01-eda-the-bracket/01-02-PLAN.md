---
phase: 01-eda-the-bracket
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/spatial/eda/__init__.py
  - src/spatial/eda/labels.py
  - src/spatial/eda/smiles_join.py
  - src/spatial/eda/fingerprints.py
autonomous: true
requirements: [EDA-01]
user_setup: []

must_haves:
  truths:
    - "DILIrank loads with header=1, normalizes vDILI-Concern case variants, excludes Ambiguous, encodes binary per Wang/Li (D-04)"
    - "DILIst loads despite the trailing-space column name (D-04 secondary cross-check)"
    - "SMILES join cascades dili_canonical -> drugbank -> (TDC stub) and reports coverage (D-03)"
    - "SMILES are validated by RDKit (MolFromSmiles None -> zero-vector + valid_mask False) before ECFP4 fingerprinting (V5 input validation)"
  artifacts:
    - path: "src/spatial/eda/labels.py"
      provides: "load_dilirank / load_dilist with Wang/Li binary encoding"
      contains: "def load_dilirank"
    - path: "src/spatial/eda/smiles_join.py"
      provides: "join_smiles_cascade (3-layer, coverage-warned)"
      contains: "def join_smiles_cascade"
    - path: "src/spatial/eda/fingerprints.py"
      provides: "smiles_to_ecfp4 (2048-bit radius-2, RDKit-validated)"
      contains: "GetMorganFingerprintAsBitVect"
  key_links:
    - from: "src/spatial/eda/fingerprints.py"
      to: "rdkit.Chem.MolFromSmiles"
      via: "None-check before fingerprint (V5 validation)"
      pattern: "MolFromSmiles"
    - from: "src/spatial/eda/labels.py"
      to: "data/raw/labels/dilirank/dilirank.xlsx (read at script time, not here)"
      via: "pd.read_excel(path, header=1)"
      pattern: "header=1"
---

<objective>
Build the shared EDA-01 data-prep library that both the floor (plan 03) and the
ceiling label-alignment (plan 04) consume: label loading + binary encoding (D-04),
the 3-layer SMILES join (D-03), and RDKit ECFP4 fingerprinting. Pure library (no
hardcoded paths, no I/O of real files at import) following the src/spatial/ conventions.

Purpose: contracts-first foundation so floor/ceiling don't re-resolve SMILES or
re-encode labels.
Output: src/spatial/eda/{__init__,labels,smiles_join,fingerprints}.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md

<interfaces>
From src/spatial/orthology.py (header + NamedTuple + warn-on-threshold conventions to copy):
  "from __future__ import annotations"; logging.getLogger(__name__); __all__ = [...]
  class OrthologTable(NamedTuple): pairs, n_input, n_one2one, dropped_fraction

Reused input column layouts (verified, RESEARCH.md Data File Inventory):
  dilirank.xlsx (header=1): LTKBID, CompoundName, SeverityClass, LabelSection, vDILI-Concern, Comment
  dilist.xlsx: column "DILIst Classification " (trailing space); compound name column
  dili_canonical.csv: pert_id, drug_name, smiles, canonical_smiles, scaffold, dili_binary, ... (join key drug_name->lower)
  drugbank_smiles_index.csv: name_lower, name, smiles
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: labels.py — DILIrank + DILIst loaders with Wang/Li binary encoding</name>
  <read_first>
    - src/spatial/orthology.py (analog: module header hard-rules block, __future__ import, logger, __all__, NamedTuple result schema, warn-on-high-exclusion — PATTERNS.md lines 44-113)
    - tests/spatial/test_eda_labels.py (the RED contract from plan 01 — exact assertions to satisfy)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 1 + Pitfalls 1,2,3 (header=1; case variants; trailing space)
  </read_first>
  <files>src/spatial/eda/__init__.py, src/spatial/eda/labels.py</files>
  <behavior>
    - load_dilirank: header=1; concern = vDILI-Concern.str.lower().str.strip(); pos = {vmost-dili-concern, vless-dili-concern} -> 1; neg = {vno-dili-concern} -> 0; Ambiguous excluded; returns LTKBID, CompoundName, name_lower, dili_binary, SeverityClass.
    - load_dilist: df.columns.str.strip(); dili_binary from "DILIst Classification"; name_lower from compound column.
    - Warn via log.warning when excluded (Ambiguous) fraction > 0.30 (mirror orthology high-dropped warn).
  </behavior>
  <action>
    Create `src/spatial/eda/__init__.py` as an empty/minimal file (PATTERNS.md: blank init, no
    re-exports — callers import from submodules). Create `src/spatial/eda/labels.py` copying the
    orthology.py header structure verbatim (hard-rules docstring block, `from __future__ import
    annotations`, `import logging`, `log = logging.getLogger(__name__)`, `__all__`). Define a
    `LabelTable(NamedTuple)` with fields df, n_total, n_positive, n_negative, n_excluded, source.
    Implement load_dilirank exactly per RESEARCH.md Pattern 1: `pd.read_excel(path, header=1)`, lowercase-strip
    vDILI-Concern, pos_mask {"vmost-dili-concern","vless-dili-concern"}, neg_mask {"vno-dili-concern"},
    exclude everything else (Ambiguous), `dili_binary = pos_mask.astype(int)` on the kept subset,
    `name_lower = CompoundName.str.lower().str.strip()`. Implement load_dilist with
    `df.columns = df.columns.str.strip()` then read "DILIst Classification". Tests monkeypatch
    pd.read_excel, so the loaders must call pd.read_excel(path, ...) (do not pre-open the file another way).
    Add the &gt;0.30 excluded-fraction log.warning.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_labels.py -x -q -k "dilirank or dilist"</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_labels.py::test_dilirank_binary_encoding -x` exits 0
    - `grep -c "header=1" src/spatial/eda/labels.py` >= 1
    - `grep -c "str.strip()" src/spatial/eda/labels.py` >= 1 (DILIst trailing-space + name_lower)
    - `grep -c "vmost-dili-concern" src/spatial/eda/labels.py` >= 1 (case-normalized match)
    - `ls src/spatial/eda/__init__.py` exists
  </acceptance_criteria>
  <done>load_dilirank/load_dilist pass their plan-01 unit tests; Ambiguous excluded; case variants and trailing space handled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: smiles_join.py + fingerprints.py — 3-layer join (D-03) and RDKit ECFP4 (V5-validated)</name>
  <read_first>
    - src/spatial/gene_alignment.py (analog: imports block, coverage-fraction + low-coverage warn pattern — PATTERNS.md lines 144-174, 200-228)
    - tests/spatial/test_eda_fingerprints.py + the smiles-cascade test in test_eda_labels.py (RED contracts from plan 01)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 2 (cascade), Pattern 3 (ECFP4), Security Domain (V5: MolFromSmiles None-check)
  </read_first>
  <files>src/spatial/eda/smiles_join.py, src/spatial/eda/fingerprints.py</files>
  <behavior>
    - join_smiles_cascade: layer1 dili_canonical (canonical_smiles, join key name_lower from drug_name);
      layer2 drugbank_smiles_index (name_lower key) fills residual NaN; layer3 a TDC fallback stub
      callable (only invoked if gap > 10% — leave a documented hook, do not block on network).
      Adds a 'smiles' column; reports covered fraction; log.warning if coverage < 0.40 of input rows.
    - smiles_to_ecfp4: for each SMILES, mol = Chem.MolFromSmiles(smi); if None -> np.zeros(n_bits, uint8)
      + valid_mask False (V5 validation, no crash); else GetMorganFingerprintAsBitVect(mol, radius=2,
      nBits=2048). Returns (fps (n,2048) uint8, valid_mask (n,) bool).
  </behavior>
  <action>
    Create smiles_join.py with the gene_alignment.py header/imports style (pandas instead of numpy).
    Implement join_smiles_cascade per RESEARCH.md Pattern 2: merge dili_canonical[["name_lower",
    "canonical_smiles"]] renamed to smiles on name_lower (how="left"); for residual NaN, merge
    drugbank_smiles_index[["name_lower","smiles"]] and fill via merged.loc[missing_mask,"smiles"].
    Compute covered = smiles.notna().mean(); if covered < 0.40 emit the gene_alignment-style
    low-coverage log.warning (this is the &lt;40% non-Ambiguous flag from the acquisition risk).
    Add a `tdc_fallback` parameter (callable | None, default None) representing layer 3; if provided
    and residual gap > 0.10, call it on still-missing name_lower values — do NOT hard-require pytdc.
    Create fingerprints.py with the same header style. Implement smiles_to_ecfp4 exactly per Pattern 3
    including the MolFromSmiles None -> zero-vector + valid_mask=False branch. Use
    `from rdkit import Chem` and `from rdkit.Chem import AllChem`.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_fingerprints.py tests/spatial/test_eda_labels.py::test_smiles_join_cascade -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_fingerprints.py::test_ecfp4_shape -x` exits 0
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_labels.py::test_smiles_join_cascade -x` exits 0
    - `grep -c "MolFromSmiles" src/spatial/eda/fingerprints.py` >= 1 (V5 validation present)
    - `grep -c "GetMorganFingerprintAsBitVect" src/spatial/eda/fingerprints.py` >= 1
    - `grep -c "0.40\|0.4" src/spatial/eda/smiles_join.py` >= 1 (coverage flag threshold)
  </acceptance_criteria>
  <done>Cascade join and ECFP4 fingerprinting pass plan-01 tests; coverage flag wired; SMILES validated by RDKit before fingerprinting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| join-table SMILES string → RDKit fingerprint | untrusted SMILES text crosses into the cheminformatics parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering / DoS | smiles_to_ecfp4 on malformed SMILES | mitigate | Chem.MolFromSmiles returns None on parse failure → emit zero-vector + valid_mask False, never raise (V5 input validation per RESEARCH Security Domain) |
| T-01-04 | Information disclosure | label/SMILES join tables | accept | Public toxicology label data; no PII/credentials |
</threat_model>

<verification>
- `conda run -n dili_v04_env pytest tests/spatial/test_eda_labels.py tests/spatial/test_eda_fingerprints.py -q` green.
- RDKit None-check present in fingerprints.py; coverage flag in smiles_join.py.
</verification>

<success_criteria>
EDA-01 data-prep library (labels, SMILES join, fingerprints) passes its plan-01 unit
tests; Wang/Li binary encoding, 3-layer cascade with coverage flag, and RDKit-validated
ECFP4 all implemented per the locked decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-02-SUMMARY.md`
</output>
