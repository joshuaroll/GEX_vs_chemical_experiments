---
phase: 01-eda-the-bracket
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - src/spatial/eda/floor.py
autonomous: true
requirements: [EDA-01]
user_setup: []

must_haves:
  truths:
    - "The structure-only floor reports label entropy, class balance, AUPRC base rate, and logistic + RF AUROC per organ+species (D-03)"
    - "Floor runs on the SMILES-covered DILIrank subset; uncovered drugs are excluded and counted (not silently dropped)"
    - "Floor AUROC and predicted probabilities are produced per drug for the downstream paired-bootstrap gap test"
  artifacts:
    - path: "src/spatial/eda/floor.py"
      provides: "FloorResult + compute_floor (LR + RF on ECFP4, >=3 seeds)"
      contains: "class FloorResult"
  key_links:
    - from: "src/spatial/eda/floor.py"
      to: "sklearn LogisticRegression + RandomForestClassifier"
      via: "cross-val-predict probabilities → roc_auc_score / average_precision_score"
      pattern: "roc_auc_score"
---

<objective>
Implement the EDA-01 structure-only FLOOR (D-03): logistic regression + random forest
on ECFP4 fingerprints of the SMILES-joined DILIrank drugs, reporting label entropy,
class balance, AUPRC base rate, and floor AUROC, plus per-drug predicted probabilities
that plan 06 feeds into the paired-bootstrap gap test against the ceiling.

Purpose: the lower bracket of Halt Gate 2 (structure-only achievable AUROC).
Output: src/spatial/eda/floor.py (pure library).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md

<interfaces>
Consumed (from plan 02):
  src/spatial/eda/fingerprints.py: smiles_to_ecfp4(smiles_list, n_bits=2048, radius=2) -> (fps, valid_mask)
  src/spatial/eda/labels.py: load_dilirank(path) -> df[..., dili_binary]
  src/spatial/eda/smiles_join.py: join_smiles_cascade(labels_df, dili_canonical_path, drugbank_path) -> df[..., smiles]
Contract this plan must satisfy (RED test from plan 01, tests/spatial/test_eda_floor.py::test_floor_auroc_above_chance):
  class FloorResult(NamedTuple): lr_auroc, rf_auroc, label_entropy, class_balance, auprc_base_rate, n_drugs, n_drugs_with_smiles
  def compute_floor(fps: np.ndarray, y: np.ndarray, seeds=(0,1,2)) -> FloorResult
Don't-hand-roll (RESEARCH.md): sklearn roc_auc_score, average_precision_score; no custom AUROC.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: floor.py — LR + RF floor on ECFP4 with entropy/balance/AUPRC base rate</name>
  <read_first>
    - src/spatial/orthology.py (NamedTuple output-schema convention — PATTERNS.md lines 236-258)
    - ../dili_downstream/src/data/wangli_lincs_lookup.py (log.info completion-count convention — PATTERNS.md lines 260-268)
    - tests/spatial/test_eda_floor.py (RED contract: synthetic separable fingerprints → lr_auroc > 0.5)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md (D-03 floor spec; Don't-Hand-Roll table; XC-06 seeds>=3)
  </read_first>
  <files>src/spatial/eda/floor.py</files>
  <behavior>
    - compute_floor(fps, y, seeds=(0,1,2)) returns FloorResult.
    - lr_auroc/rf_auroc are mean across >=3 seeds of out-of-fold roc_auc_score (StratifiedKFold,
      cross_val_predict with method='predict_proba'); report mean (std logged).
    - label_entropy = binary Shannon entropy of y (bits). class_balance = y.mean() (positive fraction).
    - auprc_base_rate = average_precision_score(y, y.mean()*ones) == positive prevalence (the no-skill PR baseline).
    - n_drugs = len(y); n_drugs_with_smiles = number of rows with a valid fingerprint (caller passes the covered subset).
    - Also expose a helper floor_probabilities(fps, y, seed) -> np.ndarray of per-drug OOF probs (LR),
      so plan 06 can supply floor_probs to the paired bootstrap on the shared drug set.
  </behavior>
  <action>
    Create src/spatial/eda/floor.py with the shared module header (PATTERNS.md "Shared Patterns").
    Define FloorResult(NamedTuple) with the 7 fields in the &lt;interfaces&gt; block.
    Implement compute_floor: LogisticRegression(max_iter=1000, class_weight='balanced') and
    RandomForestClassifier(n_estimators=300, class_weight='balanced', random_state=seed); for each
    seed in seeds use StratifiedKFold(n_splits=5, shuffle=True, random_state=seed) +
    sklearn.model_selection.cross_val_predict(method='predict_proba') to get out-of-fold probs;
    AUROC via roc_auc_score(y, oof_prob[:,1]); average across seeds (XC-06 mean ± std; log the std
    via log.info). Compute label_entropy with the binary Shannon formula
    `-(p*log2 p + (1-p)*log2(1-p))` guarding p in {0,1}. auprc_base_rate = float(y.mean()) (the PR
    no-skill baseline equals positive prevalence). Add floor_probabilities(fps, y, seed=0) returning
    the LR out-of-fold positive-class probability vector aligned to input order. Log completion with
    the wangli_lincs_lookup-style log.info(n_drugs, n_drugs_with_smiles, lr_auroc, rf_auroc).
    Raise ValueError with an f-string `!r` message if fps.ndim != 2 or len(fps) != len(y).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_floor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_floor.py::test_floor_auroc_above_chance -x` exits 0
    - `grep -c "class FloorResult" src/spatial/eda/floor.py` >= 1
    - `grep -c "roc_auc_score" src/spatial/eda/floor.py` >= 1 (no hand-rolled AUROC)
    - `grep -c "def floor_probabilities" src/spatial/eda/floor.py` >= 1 (per-drug probs for plan 06)
    - `grep -Ec "seeds|random_state" src/spatial/eda/floor.py` >= 1 (XC-06 multi-seed)
  </acceptance_criteria>
  <done>compute_floor passes the plan-01 smoke test; returns entropy/balance/AUPRC-base-rate + LR/RF AUROC across >=3 seeds; floor_probabilities exposes per-drug probs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Floor consumes already-validated fingerprints from plan 02; no new untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | fingerprint matrix shape/label-length mismatch | mitigate | ValueError on fps.ndim != 2 or len(fps) != len(y) before any fit |
</threat_model>

<verification>
- `conda run -n dili_v04_env pytest tests/spatial/test_eda_floor.py -q` green.
- floor.py uses sklearn roc_auc_score (no hand-rolled metric) and reports >=3 seeds.
</verification>

<success_criteria>
EDA-01 structure-only floor implemented: LR + RF on ECFP4, entropy/balance/AUPRC base
rate + AUROC across >=3 seeds, with per-drug probabilities exposed for the gap test.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-03-SUMMARY.md`
</output>
