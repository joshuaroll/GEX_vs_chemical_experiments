---
phase: 01-eda-the-bracket
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/spatial/__init__.py
  - tests/spatial/test_eda_labels.py
  - tests/spatial/test_eda_fingerprints.py
  - tests/spatial/test_eda_floor.py
  - tests/spatial/test_eda_ceiling.py
  - tests/spatial/test_eda_bootstrap.py
  - tests/spatial/test_eda_region.py
  - MANIFEST.md
autonomous: true
requirements: [EDA-01, EDA-02, EDA-03]
user_setup: []

must_haves:
  truths:
    - "7 Nyquist test files exist under tests/spatial/ and collect (import-clean) before any EDA library is implemented"
    - "All 6 reused v0.5 files + the PDG cancer-line manifold CSV are recorded in MANIFEST.md with their verified SHA256 before any of them is read"
  artifacts:
    - path: "tests/spatial/test_eda_labels.py"
      provides: "EDA-01 label + SMILES-join unit tests (synthetic fixtures)"
      contains: "def test_dilirank_binary_encoding"
    - path: "tests/spatial/test_eda_bootstrap.py"
      provides: "EDA-02 paired-bootstrap CI unit test"
      contains: "def test_bootstrap_ci_nonzero_width"
    - path: "MANIFEST.md"
      provides: "Provenance for reused external inputs (D-05 discipline)"
      contains: "wangli_measured_de.npy"
  key_links:
    - from: "tests/spatial/test_eda_*.py"
      to: "src/spatial/eda/* (not yet written)"
      via: "import statements (RED — modules absent in Wave 0)"
      pattern: "from src.spatial.eda"
    - from: "MANIFEST.md"
      to: "../dili_downstream/data/processed/wangli_measured_de.npy"
      via: "SHA256 provenance row"
      pattern: "aaeb07ce13222af49c873be8652b01597a4c638119efd306d4f4d7c04b9397f5"
---

<objective>
Lay the Wave-0 foundation for Phase 1: create the 7 Nyquist test files (synthetic
fixtures only) that downstream EDA library tasks must satisfy, and record provenance
in MANIFEST.md for every reused-from-v0.5 file and the PDG cancer-line manifold BEFORE
any of them is read (D-05 provenance discipline / XC-01 real-data-only).

Purpose: tests are the executable contract for floor/ceiling/diagnostics; MANIFEST
entries gate the real-data reads in later waves.
Output: tests/spatial/{__init__.py + 6 test files}; MANIFEST.md provenance section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-VALIDATION.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. The EDA modules are written in plans 02-05;
     the tests import them and will be RED until then. Signatures the tests must target: -->

src/spatial/eda/labels.py:
  def load_dilirank(path: str) -> pd.DataFrame   # cols: LTKBID, CompoundName, name_lower, dili_binary, SeverityClass
  def load_dilist(path: str) -> pd.DataFrame      # cols: name_lower, dili_binary

src/spatial/eda/smiles_join.py:
  def join_smiles_cascade(labels_df, dili_canonical_path, drugbank_path) -> pd.DataFrame  # adds 'smiles'

src/spatial/eda/fingerprints.py:
  def smiles_to_ecfp4(smiles_list, n_bits=2048, radius=2) -> tuple[np.ndarray, np.ndarray]  # (fps (n,2048) uint8, valid_mask (n,) bool)

src/spatial/eda/floor.py:
  class FloorResult(NamedTuple): lr_auroc, rf_auroc, label_entropy, class_balance, auprc_base_rate, n_drugs, n_drugs_with_smiles
  def compute_floor(fps: np.ndarray, y: np.ndarray, seeds=(0,1,2)) -> FloorResult

src/spatial/eda/ceiling.py:
  def participation_ratio(X: np.ndarray) -> float
  def per_gene_mi(X: np.ndarray, y: np.ndarray, random_state=42) -> np.ndarray  # shape (n_genes,)
  def compute_ceiling(de, y) -> dict  # includes 'ceiling_auroc'

src/spatial/eda/bootstrap.py:
  class BootstrapResult(NamedTuple): gap_observed, ci_lower, ci_upper, gate_fires, n_resamples_valid
  def paired_bootstrap_auroc_gap(y, floor_probs, ceil_probs, n_resamples=10_000, seed=42) -> BootstrapResult

src/spatial/eda/region_diagnostics.py:
  def human_mouse_liver_correlation(human_profile, human_genes, mouse_profile, mouse_genes, ortholog_table) -> dict  # 'pearson_r','p_value','n_genes_compared'
  def compute_moran_svgs(adata, spatial_key='spatial', n_neighs=6) -> dict  # 'svg_count','svg_fraction','top_svgs'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 7 Nyquist test files with synthetic-only fixtures</name>
  <read_first>
    - tests/spatial/test_gene_alignment.py (exact analog for test header/import/fixture style — PATTERNS.md lines 566-612)
    - tests/spatial/test_pseudobulk.py (analog for SimpleNamespace duck-typed AnnData fixture — PATTERNS.md lines 628-675)
    - .planning/phases/01-eda-the-bracket/01-VALIDATION.md (the per-task verification map — exact test function names required)
    - .planning/phases/01-eda-the-bracket/01-PATTERNS.md (per-file test guidance, lines 560-678 and 774-798)
  </read_first>
  <files>tests/spatial/__init__.py, tests/spatial/test_eda_labels.py, tests/spatial/test_eda_fingerprints.py, tests/spatial/test_eda_floor.py, tests/spatial/test_eda_ceiling.py, tests/spatial/test_eda_bootstrap.py, tests/spatial/test_eda_region.py</files>
  <action>
    Create `tests/spatial/__init__.py` as an empty file (mirror `tests/__init__.py`).
    Create 6 test files. EVERY fixture is in-memory synthetic (np.ndarray / pd.DataFrame /
    SimpleNamespace) — NO real data file is read (PATTERNS.md "Test fixture convention").
    Each test function is `def test_<behavior>() -> None:` with a one-line docstring.
    Import the contracts in the &lt;interfaces&gt; block above (imports will be RED until
    plans 02-05 land — that is the intended Wave-0 failing-test state).

    Required test functions (names are load-bearing — they appear in 01-VALIDATION.md):

    test_eda_labels.py:
      - test_dilirank_binary_encoding: build a 4-row synthetic df with vDILI-Concern values
        ["vMost-DILI-concern","vNo-DILI-Concern","Ambiguous-DILI-concern","vMOST-DILI-concern"];
        assert Ambiguous is excluded (result has 3 rows), both vMOST/vMost case variants map to
        dili_binary==1, vNo maps to 0. Build the df and pass through load_dilirank via a monkeypatched
        pd.read_excel OR refactor: call a pure encode helper. Simplest: monkeypatch
        `pandas.read_excel` to return the synthetic df, then call load_dilirank("dummy").
      - test_smiles_join_cascade: synthetic labels_df (name_lower col) + two tiny synthetic CSVs
        written to tmp_path; assert dili_canonical layer wins, drugbank fills the residual,
        a name in neither has smiles NaN.
    test_eda_fingerprints.py:
      - test_ecfp4_shape: smiles_to_ecfp4(["CC(=O)Oc1ccccc1C(=O)O","not_a_smiles"]) returns
        fps.shape==(2,2048), dtype uint8, valid_mask==[True, False].
    test_eda_floor.py:
      - test_floor_auroc_above_chance: synthetic separable fingerprint matrix (20 drugs x 2048,
        first 10 rows bit pattern A label 1, last 10 pattern B label 0) → compute_floor(...).lr_auroc > 0.5.
    test_eda_ceiling.py:
      - test_participation_ratio: identity-covariance synthetic (100, 50) standard-normal matrix →
        participation_ratio in (30, 50] (full-rank ~50; allow slack for finite-sample).
      - test_per_gene_mi_shape: per_gene_mi(X (40,8), y binary) returns shape (8,).
      - test_ceiling_ge_floor: synthetic drug-level data where ceiling probs track y better than floor →
        compute_ceiling(...)['ceiling_auroc'] >= a floor AUROC computed inline.
    test_eda_bootstrap.py:
      - test_bootstrap_ci_nonzero_width: synthetic 50-drug y/floor_probs/ceil_probs →
        result.ci_upper > result.ci_lower.
      - (also assert) identical floor==ceil arrays → gate_fires True and gap_observed==0.
    test_eda_region.py:
      - test_moran_returns_dataframe: SKIP-guard with importorskip("squidpy"); build a tiny
        SimpleNamespace/AnnData with obsm["spatial"] populated; assert compute_moran_svgs returns a
        dict with keys svg_count/svg_fraction/top_svgs. (squidpy is installed in dili_v04_env; if the
        runner env lacks it, the importorskip keeps the suite green.)
      - test_cross_species_ortholog_filter: synthetic OrthologTable-like object (pairs DataFrame with
        human_symbol/mouse_symbol where only a subset of human_genes appear) → human_mouse_liver_correlation
        result['n_genes_compared'] < len(human_genes).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/ -q --collect-only 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/spatial/test_eda_labels.py tests/spatial/test_eda_fingerprints.py tests/spatial/test_eda_floor.py tests/spatial/test_eda_ceiling.py tests/spatial/test_eda_bootstrap.py tests/spatial/test_eda_region.py tests/spatial/__init__.py` all exist (exit 0)
    - `grep -c "def test_" tests/spatial/test_eda_ceiling.py` returns >= 3
    - `grep -l "def test_dilirank_binary_encoding" tests/spatial/test_eda_labels.py` matches
    - `grep -l "def test_bootstrap_ci_nonzero_width" tests/spatial/test_eda_bootstrap.py` matches
    - `grep -rL "read_csv(\"data/\|read_excel(\"data/\|np.load(\"data/" tests/spatial/test_eda_*.py` shows no test reads a real data path (synthetic-only)
    - pytest --collect-only either collects the tests OR reports collection errors only on the not-yet-written src.spatial.eda imports (expected RED in Wave 0)
  </acceptance_criteria>
  <done>7 test files exist; function names match 01-VALIDATION.md; fixtures are synthetic-only; suite collects (RED imports expected until plans 02-05).</done>
</task>

<task type="auto">
  <name>Task 2: Record reused-v0.5 + PDG-manifold provenance in MANIFEST.md (D-05 / XC-01)</name>
  <read_first>
    - MANIFEST.md (existing "Data files (SHA256-pinned)" table structure — match it exactly)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md "Data File Inventory" (the 6 files + verified SHA256s, lines 145-161)
    - .planning/phases/01-eda-the-bracket/01-CONTEXT.md D-05 (provenance-before-use rule)
  </read_first>
  <files>MANIFEST.md</files>
  <action>
    Append a new section `## Phase 1 — Reused external inputs (v0.5 dili_downstream + PDG manifold)`
    to MANIFEST.md with a table matching the existing column layout
    (`| File | Path | SHA256 | Source | License | Whole-transcriptome | Notes |`). Add one row each,
    using the SHA256 values verbatim from RESEARCH.md:
      - wangli_measured_de.npy | `../dili_downstream/data/processed/wangli_measured_de.npy` | `aaeb07ce13222af49c873be8652b01597a4c638119efd306d4f4d7c04b9397f5` | dili_downstream v0.5 Phase 1 output | see source | N/A | External input; shape (5517, 978) float32; ceiling DE matrix (D-05)
      - wangli_profiles.csv | `../dili_downstream/data/processed/wangli_profiles.csv` | `963ea3e836b061a6eb2fa066ef5634b4cf9e9cfda8537ea45e98ef1ec3cf872e` | dili_downstream v0.5 | see source | N/A | 5517 rows x 12 cols; profile metadata for the DE matrix
      - dili_canonical.csv | `../dili_downstream/data/processed/dili_canonical.csv` | `8e30f71278fad1391eacab0fc287d2898361249cd119ade810f86212243a3525` | dili_downstream v0.5 | see source | N/A | SMILES join layer 1; join key drug_name->name_lower
      - dilist_smiles_resolved.csv | `../dili_downstream/data/processed/dilist_smiles_resolved.csv` | `b57be7557b52fd6c3f094e718e560a522452895ee7c317a67084b1c83ebbdf26` | dili_downstream v0.5 | see source | N/A | SMILES join (DILIst secondary, D-04)
      - drugbank_smiles_index.csv | `../dili_downstream/data/processed/drugbank_smiles_index.csv` | `2ad9eb4d642f954dfdc8aa30deb0a4089b99292af0917af4df0e034c05134a5e` | DrugBank index via dili_downstream v0.5 | see source | N/A | 38917 rows; SMILES fallback layer 2; key name_lower
      - pdg_diseased manifold | `/raid/home/joshua/projects/MultiDCP_pdg/data/pdg_diseased_brddrugfiltered_avg_over_celltype_10x10717.csv` | `e1e38f118c91be64065bdb8458479bfe3b0fc0a9c118a7e5352523f5a2306fb1` | MultiDCP_pdg | see source | N/A | (10, 10716) cancer-line manifold; EDA-03 OOD reference
    Add a one-line note: "Recorded before first read per D-05; these are static external inputs, not
    live cross-project dependencies. Do NOT touch /raid/home/joshua/.planning (isolation rule)."
    Before writing, VERIFY each SHA on disk by running `sha256sum` (or scripts/compute_sha256.py) on
    the paths; if any SHA mismatches the RESEARCH value, STOP and write the discrepancy into the SUMMARY
    rather than silently overwriting (XC-01 stop-and-ask).
  </action>
  <verify>
    <automated>grep -c "aaeb07ce13222af49c873be8652b01597a4c638119efd306d4f4d7c04b9397f5\|e1e38f118c91be64065bdb8458479bfe3b0fc0a9c118a7e5352523f5a2306fb1\|2ad9eb4d642f954dfdc8aa30deb0a4089b99292af0917af4df0e034c05134a5e" MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wangli_measured_de.npy" MANIFEST.md` >= 1
    - `grep -c "pdg_diseased_brddrugfiltered_avg_over_celltype_10x10717.csv" MANIFEST.md` >= 1
    - All 6 SHA256 strings from RESEARCH.md "Data File Inventory" appear in MANIFEST.md (grep each)
    - `sha256sum ../dili_downstream/data/processed/wangli_measured_de.npy` matches the recorded value (no drift)
  </acceptance_criteria>
  <done>MANIFEST.md has a Phase-1 reused-inputs section with all 6 files, verified SHAs, source/license/notes; on-disk SHAs match the recorded values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| join-table SMILES string → RDKit parser | SMILES read from reused CSVs are untrusted text fed to a structure parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Reused external .npy/.csv inputs (cross-project read) | mitigate | SHA256 recorded in MANIFEST.md and re-verified on disk before first read (Task 2); mismatch halts (XC-01) |
| T-01-02 | Information disclosure | Cross-project filesystem access | accept | Read-only static reads of sibling project files; no credentials/PII; isolation rule forbids touching /raid/home/joshua/.planning |
</threat_model>

<verification>
- `conda run -n dili_v04_env pytest tests/spatial/ -q --collect-only` collects the new test files (RED imports on src.spatial.eda are expected until later plans).
- MANIFEST.md contains all 6 reused-input SHA256 rows; on-disk SHAs match.
- No test file reads a real data path (synthetic fixtures only).
</verification>

<success_criteria>
7 test files exist with the exact function names from 01-VALIDATION.md; MANIFEST.md
records provenance for all reused inputs with verified SHAs before any later wave reads them.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-01-SUMMARY.md`
</output>
