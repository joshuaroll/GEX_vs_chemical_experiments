---
phase: 01-eda-the-bracket
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - scripts/run_p1_eda.py
  - results/tables/P1_eda.md
autonomous: false
requirements: [EDA-01, EDA-02, EDA-03]
user_setup: []

must_haves:
  truths:
    - "scripts/run_p1_eda.py runs end-to-end on the real on-disk data (DILIrank, Wang/Li measured DE, yu2022 liver Visium, GSE272564 mouse control, PDG manifold) under conda env dili_v04_env"
    - "results/tables/P1_eda.md states the structure floor, the measured ceiling, the floor-ceiling gap with its 95% paired-bootstrap CI, region distinguishability, and human-rodent basal concordance (EDA-03 acceptance)"
    - "Halt Gate 2 is evaluated from the BootstrapResult: if the CI of (ceiling - floor) AUROC includes 0, HALT_REASON.md is written and the run exits non-zero (D-02, stop-and-REFRAME)"
    - "SMILES coverage below 40% of non-Ambiguous DILIrank is flagged in P1_eda.md; mouse liver is reported whole-sample with 'no zone annotation available'; OOD method (Mahalanobis, 978-landmark) is named"
  artifacts:
    - path: "scripts/run_p1_eda.py"
      provides: "floor/ceiling/diagnostics/all sub-command driver writing P1_eda.md + halt gate"
      contains: "HALT_REASON.md"
    - path: "results/tables/P1_eda.md"
      provides: "the Phase 1 deliverable (EDA-03 acceptance table)"
      contains: "95% CI"
  key_links:
    - from: "scripts/run_p1_eda.py"
      to: "src/spatial/eda/{labels,smiles_join,fingerprints,floor,ceiling,bootstrap,region_diagnostics}"
      via: "sys.path injection + sub-command dispatch"
      pattern: "from src.spatial.eda"
    - from: "scripts/run_p1_eda.py"
      to: ".planning/phases/01-eda-the-bracket/HALT_REASON.md"
      via: "gate_fires -> write + sys.exit(1)"
      pattern: "HALT_REASON.md"
---

<objective>
Wire the EDA libraries into a single driver that reads the REAL on-disk data, computes
the floor (EDA-01), ceiling (EDA-02), and region + cross-species diagnostics (EDA-03)
for liver/human, writes results/tables/P1_eda.md, and evaluates Halt Gate 2 from the
paired-bootstrap CI (D-02). This is the integration/wiring plan — last in the
interface-first ordering.

Purpose: produce the Phase 1 deliverable and the reproducible gate decision.
Output: scripts/run_p1_eda.py; results/tables/P1_eda.md (+ HALT_REASON.md only if the gate fires).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md
@MANIFEST.md

<interfaces>
Consumed libraries (plans 02-05):
  labels.load_dilirank(path) / load_dilist(path)
  smiles_join.join_smiles_cascade(labels_df, dili_canonical_path, drugbank_path)
  fingerprints.smiles_to_ecfp4(smiles_list) -> (fps, valid_mask)
  floor.compute_floor(fps, y, seeds) -> FloorResult ; floor.floor_probabilities(fps, y, seed)
  ceiling.load_ceiling(de_path, profiles_path) ; ceiling.compute_ceiling(de, y, profiles) -> dict (ceiling_probs per drug)
  bootstrap.paired_bootstrap_auroc_gap(y, floor_probs, ceil_probs, n_resamples=10_000) -> BootstrapResult
  region_diagnostics.compute_moran_svgs / basal_similarity_matrix / svg_retention /
    human_mouse_liver_correlation / ood_mahalanobis
  src/spatial/orthology.build_one2one_orthologs(...) -> OrthologTable (P0 cached TSV)
  src/spatial/pseudobulk.from_anndata(adata, obs_col='category')

Script analog: scripts/report_coverage.py (path block, sub-command main, results-table writer,
HALT_REASON writer — PATTERNS.md lines 489-556 and 759-772).

Real on-disk inputs (paths verified, RESEARCH.md):
  data/raw/labels/dilirank/dilirank.xlsx (header=1) ; data/raw/labels/dilist/dilist.xlsx
  ../dili_downstream/data/processed/{wangli_measured_de.npy (5517,978), wangli_profiles.csv,
    dili_canonical.csv, drugbank_smiles_index.csv}
  data/raw/spatial/yu2022_liver/L5_upload.zip (inside: filtered_feature_bc_matrix.h5,
    l5_category.csv [col 'category': Zone1..Zone3, portal_area, central_area],
    spatial/tissue_positions_list.csv) — Pitfall 5: extract + load coords into obsm['spatial']
  data/raw/spatial/gse272564_mouse_liver_ctrl/GSE272564_RAW.tar — filter *APAP0h* (GSM8404653);
    NO zone annotation -> whole-sample pseudobulk (Pitfall 6 + RESEARCH Open Q3)
  /raid/home/joshua/projects/MultiDCP_pdg/data/pdg_diseased_brddrugfiltered_avg_over_celltype_10x10717.csv (10,10716)
Run via: conda run -n dili_v04_env python scripts/run_p1_eda.py all  (numba/numpy: anndata not scanpy where possible — Pitfall 9)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_p1_eda.py floor + ceiling + gap, write P1_eda.md, evaluate Halt Gate 2</name>
  <read_first>
    - scripts/report_coverage.py (analog: path block + sys.path inject + sub-command main + results-table writer + HALT_REASON writer — PATTERNS.md lines 489-556, 759-772)
    - MANIFEST.md (confirm plan-01 provenance rows exist BEFORE reading the reused files — XC-01/D-05; if absent, STOP)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md (Pattern 1 label load; Pitfall 8 shared-drug-set + drug-level aggregation; D-02 gate; acquisition risks)
    - .planning/phases/01-eda-the-bracket/01-CONTEXT.md (D-01..D-05; "no measured ceiling — floor only" honesty rule)
  </read_first>
  <files>scripts/run_p1_eda.py, results/tables/P1_eda.md</files>
  <action>
    Create scripts/run_p1_eda.py copying the report_coverage.py path block verbatim (_REPO_ROOT,
    sys.path.insert, ROOT, RESULTS = ROOT/"results"/"tables", PHASE_DIR =
    ROOT/".planning"/"phases"/"01-eda-the-bracket"). argparse sub-commands: floor, ceiling,
    diagnostics, all. Before reading any reused file, assert its SHA-row exists in MANIFEST.md
    (read MANIFEST text, check the 6 filenames present); if a row is missing, print an error and
    sys.exit(2) (provenance-before-use).

    run_floor(): load_dilirank("data/raw/labels/dilirank/dilirank.xlsx"); join_smiles_cascade against
    ../dili_downstream/data/processed/{dili_canonical.csv, drugbank_smiles_index.csv}; drop drugs with
    no SMILES (count them); smiles_to_ecfp4 the covered subset; compute_floor(fps, y, seeds=(0,1,2)).
    Compute non-Ambiguous coverage = n_with_smiles / n_non_ambiguous; if < 0.40 set a coverage_flag
    string for the report. Also run the DILIst secondary (D-04) cheaply: base rates + LR floor AUROC,
    reported alongside but DILIrank gates. Keep floor_probabilities for the shared-drug-set gap.

    run_ceiling(): load_ceiling(de_path, profiles_path) on the wangli files; align measured profiles to
    DILIrank labels via name_lower; compute_ceiling(de, y, profiles) (drug-level aggregation, Pitfall 8);
    capture per-drug ceiling_probs.

    Gap + gate (combined under "all"): intersect floor drugs (with SMILES) and ceiling drugs to the
    SHARED drug set; align y, floor_probs, ceil_probs (drug-level);
    paired_bootstrap_auroc_gap(..., n_resamples=10_000). If result.gate_fires: write
    PHASE_DIR/HALT_REASON.md (gap CI lower bound, "includes 0", "stop-and-REFRAME per D-02") and
    sys.exit(1) AFTER writing P1_eda.md. If the ceiling DE is unavailable, report
    "no measured ceiling — floor only" and skip the gate (do not silently pass).

    Write results/tables/P1_eda.md (RESULTS.mkdir(parents=True, exist_ok=True)) with the
    report_coverage.py table-writer style. The report MUST contain, per organ+species (liver/human):
    label entropy, class balance (positive fraction), AUPRC base rate, floor LR/RF AUROC (mean±std over
    seeds), ceiling participation ratio, MI summary, ceiling AUROC, the gap with the literal text
    "95% CI" and [ci_lower, ci_upper], the gate decision (FIRES / clear), the DILIst cross-check row,
    and the SMILES coverage line (with the <40% flag if triggered).
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python scripts/run_p1_eda.py floor 2>&1 | tail -20 && grep -Ec "AUPRC|floor" results/tables/P1_eda.md</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env python scripts/run_p1_eda.py floor` exits 0 and writes/updates results/tables/P1_eda.md
    - `grep -c "from src.spatial.eda" scripts/run_p1_eda.py` >= 1
    - `grep -c "HALT_REASON.md" scripts/run_p1_eda.py` >= 1
    - `grep -E -c "10_000|10000" scripts/run_p1_eda.py` >= 1 (D-02 resamples)
    - `grep -c "no measured ceiling" scripts/run_p1_eda.py` >= 1 (honesty rule)
  </acceptance_criteria>
  <done>Driver runs floor+ceiling+gap on real data, writes P1_eda.md with the floor/ceiling/gap+CI, and fires Halt Gate 2 (HALT_REASON.md + exit 1) iff the CI includes 0.</done>
</task>

<task type="auto">
  <name>Task 2: run_p1_eda.py diagnostics — region + cross-species, finalize P1_eda.md</name>
  <read_first>
    - scripts/report_coverage.py (zip/tar extraction + coverage_fraction usage already in this repo's scripts)
    - src/spatial/pseudobulk.py (from_anndata obs_col usage) and src/spatial/orthology.py (build_one2one_orthologs cached-TSV path from P0)
    - results/tables/P0_orthologs.md + P0_coverage.md (the P0 ortholog map + per-dataset coverage to reuse)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md (Pitfall 5 yu2022 coords; Pitfall 6 GSE272564 APAP0h control; Open Q3 no mouse zone annotation; Pattern 8/9; Mahalanobis 978-landmark)
  </read_first>
  <files>scripts/run_p1_eda.py, results/tables/P1_eda.md</files>
  <action>
    Implement run_diagnostics() in run_p1_eda.py. Load yu2022 L5: extract L5_upload.zip to a tempdir,
    load filtered_feature_bc_matrix.h5 (scanpy.read_10x_h5 inside dili_v04_env, or an anndata-based
    reader), join l5_category.csv on barcode to set adata.obs['category'], and load
    spatial/tissue_positions_list.csv into adata.obsm['spatial'] (Pitfall 5 — Moran's I needs coords).
    Call compute_moran_svgs(adata) and svg_retention against the 10,716-gene list; pseudobulk per zone
    via from_anndata(adata, obs_col='category') and basal_similarity_matrix over the 7 liver zones.
    Load GSE272564_RAW.tar, filter to *APAP0h* (GSM8404653) mtx/features/barcodes, build whole-sample
    mouse pseudobulk (NO zone split — report "no zone annotation available", Open Q3). Build/reuse the
    P0 one-to-one ortholog map (build_one2one_orthologs on the cached TSV); call
    human_mouse_liver_correlation(human_whole_profile, human_genes, mouse_whole_profile, mouse_genes,
    ortholog_table) and report pearson_r + n_genes_compared + ortholog overlap. Load the PDG manifold
    CSV (10,10716); project both manifold and the per-zone human pseudobulk to the 978 LINCS landmark
    genes; ood_mahalanobis(manifold_978, query_978, alpha=1e-2); report per-zone distance and name the
    method ("Mahalanobis, 978-gene landmark subspace, alpha=1e-2"). Append a "Region distinguishability"
    section (SVG retention %, basal similarity summary, gene coverage) and a "Human-rodent basal
    concordance" section (pearson_r, n_genes_compared, ortholog overlap, OOD distances) to
    results/tables/P1_eda.md. Run under conda dili_v04_env (Pitfall 9: prefer anndata; scanpy only
    inside this env where numba/numpy are compatible).
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python scripts/run_p1_eda.py diagnostics 2>&1 | tail -20 && grep -E -c "Moran|concordance|Mahalanobis|distinguishability" results/tables/P1_eda.md</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env python scripts/run_p1_eda.py diagnostics` exits 0
    - `grep -c "spatial" scripts/run_p1_eda.py` >= 1 (obsm coords populated; Pitfall 5)
    - `grep -c "APAP0h" scripts/run_p1_eda.py` >= 1 (GSE272564 control sample; Pitfall 6)
    - `grep -c "no zone annotation" scripts/run_p1_eda.py` >= 1 (Open Q3 honesty)
    - `grep -E -c "978|landmark" scripts/run_p1_eda.py` >= 1 (OOD subspace named)
    - `grep -E -c "concordance|Mahalanobis|distinguishability" results/tables/P1_eda.md` >= 1 (after run)
  </acceptance_criteria>
  <done>run_diagnostics produces SVG retention, basal similarity, gene coverage, ortholog-filtered human-rodent correlation, and 978-landmark Mahalanobis OOD; P1_eda.md has the region + concordance sections with named methods.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Halt Gate 2 interpretation review (floor-ceiling gap CI)</name>
  <what-built>
    scripts/run_p1_eda.py has produced results/tables/P1_eda.md with the structure floor,
    measured ceiling, and the 95% paired-bootstrap CI of (ceiling - floor) AUROC for liver/human.
    If the CI included 0, HALT_REASON.md was written and the run exited non-zero (D-02).
  </what-built>
  <how-to-verify>
    1. Open results/tables/P1_eda.md.
    2. Confirm the reported floor AUROC, ceiling AUROC, gap, and the "95% CI" [lo, hi].
    3. Decide the gate: if the CI includes 0 the bracket is not significantly positive →
       Halt Gate 2 FIRES → stop-and-REFRAME before Phase 2 (negative result is publishable, D-02);
       confirm .planning/phases/01-eda-the-bracket/HALT_REASON.md exists.
    4. Also confirm the SMILES-coverage line and whether the <40% non-Ambiguous flag triggered
       (floor underpowered call), and that mouse liver is labeled "no zone annotation available".
  </how-to-verify>
  <acceptance_criteria>
    - results/tables/P1_eda.md exists and contains the gap, "95% CI", and the gate decision line.
    - If gate fired: .planning/phases/01-eda-the-bracket/HALT_REASON.md exists.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed (or "gate fired — reframe" to halt Phase 2 planning).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reused/external data files → driver | DILIrank xlsx, Wang/Li DE, Visium zips/tars, PDG manifold cross into the driver from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | reading reused files without recorded provenance | mitigate | Driver asserts the 6 MANIFEST SHA-rows exist before reading; sys.exit(2) if missing (D-05) |
| T-01-11 | Tampering | malformed SMILES from join tables at scale | mitigate | smiles_to_ecfp4 (plan 02) returns valid_mask; driver excludes invalid before the floor fit (V5) |
| T-01-12 | Repudiation | gate decision not reproducible | mitigate | Gap CI + gate_fires written verbatim into P1_eda.md and HALT_REASON.md from BootstrapResult |
</threat_model>

<verification>
- `conda run -n dili_v04_env python scripts/run_p1_eda.py all` runs end-to-end on real data.
- results/tables/P1_eda.md contains floor, ceiling, gap + "95% CI", region distinguishability, human-rodent concordance.
- Halt Gate 2: HALT_REASON.md written and exit 1 iff the CI includes 0.
- `conda run -n dili_v04_env pytest tests/ -q` full suite green.
</verification>

<success_criteria>
The Phase 1 deliverable results/tables/P1_eda.md is produced from real on-disk data with
the floor/ceiling/gap+CI (EDA-01/02), region + cross-species diagnostics (EDA-03), and a
reproducible Halt Gate 2 decision (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-06-SUMMARY.md`
</output>
