---
phase: 01-eda-the-bracket
plan: 04
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - src/spatial/eda/ceiling.py
  - src/spatial/eda/bootstrap.py
autonomous: true
requirements: [EDA-02]
user_setup: []

must_haves:
  truths:
    - "The measured-biology ceiling reuses the v0.5 Wang/Li LINCS measured DE and reports effective rank (PCA participation ratio), per-gene mutual information vs label, and a measured-signature-only baseline AUROC (D-05)"
    - "Ceiling predictions are aggregated to drug level (mean across profiles) before the paired test (Pitfall 8)"
    - "The paired bootstrap CI of (ceiling - floor) AUROC is computed on the SHARED drug set with 10,000 resamples and sets gate_fires when the CI includes 0 (D-02 / Halt Gate 2)"
  artifacts:
    - path: "src/spatial/eda/ceiling.py"
      provides: "load_ceiling, participation_ratio, per_gene_mi, compute_ceiling (drug-level AUROC)"
      contains: "def participation_ratio"
    - path: "src/spatial/eda/bootstrap.py"
      provides: "BootstrapResult + paired_bootstrap_auroc_gap (10,000 resamples)"
      contains: "class BootstrapResult"
  key_links:
    - from: "src/spatial/eda/ceiling.py"
      to: "../dili_downstream/data/processed/wangli_measured_de.npy (read at script time; provenance in MANIFEST)"
      via: "np.load + shape validation (5517, 978)"
      pattern: "def load_ceiling"
    - from: "src/spatial/eda/bootstrap.py"
      to: "sklearn.metrics.roc_auc_score"
      via: "paired percentile CI of the AUROC gap"
      pattern: "gate_fires"
---

<objective>
Implement the EDA-02 measured-biology CEILING (D-05) and the paired-bootstrap gap test
(D-02) that operationalizes Halt Gate 2. The ceiling reuses the v0.5 Wang/Li LINCS
measured DE (shape (5517, 978)); diagnostics are effective rank (PCA participation
ratio), per-gene mutual information, and a measured-signature-only baseline AUROC. The
bootstrap computes the 95% paired CI of (ceiling - floor) AUROC over the shared drug set.

Purpose: the upper bracket and the reproducible gate decision.
Output: src/spatial/eda/{ceiling,bootstrap}.py (pure libraries).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md

<interfaces>
Consumed: src/spatial/eda/labels.py: load_dilirank(path) -> df[..., dili_binary, name_lower]
Contracts (RED tests from plan 01):
  tests/spatial/test_eda_ceiling.py::{test_participation_ratio, test_per_gene_mi_shape, test_ceiling_ge_floor}
  tests/spatial/test_eda_bootstrap.py::test_bootstrap_ci_nonzero_width
  def participation_ratio(X: np.ndarray) -> float
  def per_gene_mi(X: np.ndarray, y: np.ndarray, random_state=42) -> np.ndarray  # shape (n_genes,)
  def compute_ceiling(de, y, ...) -> dict  # keys include 'ceiling_auroc', 'participation_ratio', 'mi'
  class BootstrapResult(NamedTuple): gap_observed, ci_lower, ci_upper, gate_fires, n_resamples_valid
  def paired_bootstrap_auroc_gap(y, floor_probs, ceil_probs, n_resamples=10_000, seed=42) -> BootstrapResult

Verified reused-input layout (RESEARCH.md):
  wangli_measured_de.npy: (5517, 978) float32 (already DE — DE rule satisfied)
  wangli_profiles.csv: 5517 rows; cols include compound_name, smiles, dili_binary, dose_um  (>1 profile per drug; 628 unique drugs)
Don't-hand-roll: sklearn roc_auc_score; the paired bootstrap is custom by necessity (RESEARCH.md).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ceiling.py — load measured DE, participation ratio, per-gene MI, drug-level ceiling AUROC</name>
  <read_first>
    - ../dili_downstream/src/data/wangli_lincs_lookup.py (analog: open-validate-close-on-failure I/O pattern — PATTERNS.md lines 276-315)
    - tests/spatial/test_eda_ceiling.py (RED contracts: PR on identity-cov synthetic; MI shape; ceiling>=floor)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 4 (participation ratio), Pattern 5 (per-gene MI), Pitfall 8 (drug-level aggregation), Pitfall 9 (anndata not scanpy / numba)
    - .planning/phases/01-eda-the-bracket/01-CONTEXT.md D-05 (reuse Wang/Li DE; "no measured ceiling — floor only" honesty rule)
  </read_first>
  <files>src/spatial/eda/ceiling.py</files>
  <behavior>
    - load_ceiling(de_path, profiles_path): np.load DE, read profiles CSV; raise ValueError if
      de.shape[0] != len(profiles) (sync check); log shape.
    - participation_ratio(X): (Sigma lambda)^2 / Sigma lambda^2 via SVD on centered X; identity-covariance
      (100,50) standard-normal -> PR ~ 50 (test allows (30,50]).
    - per_gene_mi(X, y, random_state=42): mutual_info_classif(discrete_features=False, n_neighbors=3);
      returns (n_genes,).
    - compute_ceiling(de, y, profiles=None): aggregate measured-signature predictions to DRUG level
      (mean across that drug's profiles, Pitfall 8) before AUROC; ceiling_auroc via a measured-signature-only
      baseline (cross_val_predict LogisticRegression on the 978-d DE, OOF probs, roc_auc_score), plus
      participation_ratio and per-gene MI summary (fraction MI>0, top-20 indices). Returns a dict.
    - If the measured DE is absent for an organ/species, the SCRIPT (plan 06) reports
      "no measured ceiling — floor only"; this library raises FileNotFoundError so the script can catch it.
  </behavior>
  <action>
    Create src/spatial/eda/ceiling.py with the shared header. Implement load_ceiling with the
    wangli_lincs_lookup validate-then-return pattern (PATTERNS.md lines 296-315) including the
    de.shape[0] == len(profiles) check. Implement participation_ratio exactly per RESEARCH Pattern 4
    (center, np.linalg.svd full_matrices=False, lambdas = s**2, drop zeros, return
    (lambdas.sum())**2/(lambdas**2).sum()). Implement per_gene_mi exactly per Pattern 5. Implement
    compute_ceiling: build drug-level arrays by grouping profiles on a drug key (compound_name lower)
    and averaging both the measured-signature OOF probability and confirming one label per drug;
    measured-signature baseline = LogisticRegression(max_iter=1000, class_weight='balanced') via
    StratifiedKFold(5)+cross_val_predict(predict_proba); ceiling_auroc = roc_auc_score(drug_y, drug_prob).
    Return dict with ceiling_auroc, participation_ratio, mi_fraction_nonzero, top20_mi_idx, n_drugs,
    and a per-drug ceiling_probs array keyed by drug name so plan 06 can align to the floor's shared set.
    Use `import anndata` only if needed (not scanpy) — here no h5ad is read so numpy/pandas suffice.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_ceiling.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_ceiling.py::test_participation_ratio -x` exits 0
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_ceiling.py::test_per_gene_mi_shape -x` exits 0
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_ceiling.py::test_ceiling_ge_floor -x` exits 0
    - `grep -c "mutual_info_classif" src/spatial/eda/ceiling.py` >= 1
    - `grep -Ec "groupby|drug.level|compound_name" src/spatial/eda/ceiling.py` >= 1 (Pitfall-8 drug-level aggregation)
  </acceptance_criteria>
  <done>participation_ratio, per_gene_mi, and drug-level ceiling AUROC pass plan-01 tests; DE-shape sync validated; aggregation is drug-level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bootstrap.py — paired 10,000-resample CI of (ceiling - floor) AUROC (Halt Gate 2)</name>
  <read_first>
    - src/spatial/orthology.py (NamedTuple result-schema convention)
    - tests/spatial/test_eda_bootstrap.py (RED contracts: nonzero CI width; identical arrays -> gate_fires True, gap 0)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 7 (paired bootstrap), Pitfall 8 (shared drug set), D-02 (CI includes 0 -> fire; default 10,000, floor 2,000)
  </read_first>
  <files>src/spatial/eda/bootstrap.py</files>
  <behavior>
    - paired_bootstrap_auroc_gap(y, floor_probs, ceil_probs, n_resamples=10_000, seed=42): the three
      arrays are aligned over the SHARED drug set; per resample draw n indices with replacement, skip
      degenerate resamples (all one class), gap = AUROC(ceil) - AUROC(floor); CI = 2.5/97.5 percentiles;
      gate_fires = ci_lower <= 0; returns BootstrapResult.
    - identical floor==ceil arrays -> gap_observed == 0 and gate_fires True (CI brackets 0).
  </behavior>
  <action>
    Create src/spatial/eda/bootstrap.py with the shared header. Define BootstrapResult(NamedTuple)
    with gap_observed, ci_lower, ci_upper, gate_fires, n_resamples_valid. Implement
    paired_bootstrap_auroc_gap exactly per RESEARCH Pattern 7: np.random.default_rng(seed),
    obs_gap = roc_auc_score(y, ceil_probs) - roc_auc_score(y, floor_probs); loop n_resamples drawing
    idx = rng.integers(0, n, size=n), skip when resampled y is single-class (gaps[i]=nan), else gap on
    the resampled arrays; drop nan; ci_lo, ci_hi = np.percentile(gaps, [2.5, 97.5]);
    gate_fires = ci_lo <= 0. Raise ValueError if the three arrays differ in length (`!r` message).
    Note in the docstring that the caller (plan 06) must intersect floor and ceiling to the shared
    drug set and aggregate ceiling to drug level BEFORE calling (Pitfall 8).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_bootstrap.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_bootstrap.py::test_bootstrap_ci_nonzero_width -x` exits 0
    - `grep -c "class BootstrapResult" src/spatial/eda/bootstrap.py` >= 1
    - `grep -c "gate_fires" src/spatial/eda/bootstrap.py` >= 1
    - `grep -Ec "10_000|10000" src/spatial/eda/bootstrap.py` >= 1 (default resamples per D-02)
    - `grep -c "percentile" src/spatial/eda/bootstrap.py` >= 1 (percentile CI, not hand-rolled)
  </acceptance_criteria>
  <done>paired_bootstrap_auroc_gap passes plan-01 tests; default 10,000 resamples; gate_fires set when CI includes 0; degenerate resamples skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reused .npy/.csv DE inputs → ceiling loader | external cross-project array crosses into the ceiling computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | wangli_measured_de.npy / profiles row-count mismatch | mitigate | load_ceiling raises ValueError when de.shape[0] != len(profiles); SHA already pinned in MANIFEST (plan 01) |
| T-01-07 | Information disclosure | reused measured-DE matrix | accept | De-identified perturbation signatures; no PII/credentials |
</threat_model>

<verification>
- `conda run -n dili_v04_env pytest tests/spatial/test_eda_ceiling.py tests/spatial/test_eda_bootstrap.py -q` green.
- ceiling aggregates to drug level (Pitfall 8); bootstrap defaults to 10,000 resamples (D-02).
</verification>

<success_criteria>
EDA-02 ceiling (participation ratio, per-gene MI, measured-signature AUROC) and the
paired-bootstrap gap test pass their plan-01 tests; the gate decision (CI includes 0)
is reproducible from BootstrapResult.gate_fires.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-04-SUMMARY.md`
</output>
