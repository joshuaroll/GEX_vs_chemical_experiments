---
phase: 01-eda-the-bracket
plan: 05
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - src/spatial/eda/region_diagnostics.py
autonomous: true
requirements: [EDA-03]
user_setup: []

must_haves:
  truths:
    - "Region diagnostics report a basal-profile similarity matrix across liver zones, spatially-variable-gene retention via Moran's I (squidpy), and gene coverage vs the 10,716-gene model space"
    - "Cross-species liver diagnostics report ortholog overlap and human-vs-rodent basal Pearson correlation using ONLY one-to-one orthologs (D-01 exception kept in; XC-08)"
    - "OOD distance of healthy liver pseudobulk from the cancer-line manifold is reported via ridge-regularized Mahalanobis in the 978-gene landmark subspace (method named in output)"
  artifacts:
    - path: "src/spatial/eda/region_diagnostics.py"
      provides: "compute_moran_svgs, basal_similarity_matrix, human_mouse_liver_correlation, ood_mahalanobis"
      contains: "def human_mouse_liver_correlation"
  key_links:
    - from: "src/spatial/eda/region_diagnostics.py"
      to: "squidpy.gr.spatial_neighbors_knn + spatial_autocorr(mode='moran')"
      via: "Moran's I on adata.obsm['spatial']"
      pattern: "spatial_autocorr"
    - from: "src/spatial/eda/region_diagnostics.py"
      to: "src/spatial/orthology.OrthologTable.pairs"
      via: "human_symbol->mouse_symbol one-to-one filter"
      pattern: "human_symbol"
---

<objective>
Implement the EDA-03 region + cross-species diagnostics library: basal-profile
similarity matrix across liver zones, spatially-variable-gene retention via squidpy
Moran's I, gene coverage vs the 10,716-gene model space, ortholog-filtered human-vs-rodent
liver basal correlation (D-01 exception kept in), and OOD distance of healthy liver from
the cancer-line manifold (ridge-regularized Mahalanobis in the 978-gene landmark subspace).

Purpose: region distinguishability + human-rodent basal concordance lines of P1_eda.md.
Output: src/spatial/eda/region_diagnostics.py (pure library).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md
@.planning/phases/01-eda-the-bracket/01-RESEARCH.md
@.planning/phases/01-eda-the-bracket/01-PATTERNS.md

<interfaces>
Consumed:
  src/spatial/orthology.py: class OrthologTable(NamedTuple) with .pairs DataFrame
    (cols: human_ensembl, human_symbol, mouse_ensembl, mouse_symbol, ...)
  src/spatial/pseudobulk.py: from_anndata(adata, obs_col, agg='mean', ...) -> PseudobulkResult
    (sparse guard: `if hasattr(raw, "toarray"): mat = raw.toarray()`)
Contracts (RED tests from plan 01, tests/spatial/test_eda_region.py):
  test_moran_returns_dataframe (importorskip squidpy): compute_moran_svgs(adata, ...) -> dict
    keys svg_count/svg_fraction/top_svgs
  test_cross_species_ortholog_filter: human_mouse_liver_correlation(...)['n_genes_compared'] < len(human_genes)
  def compute_moran_svgs(adata, spatial_key='spatial', n_neighs=6) -> dict
  def human_mouse_liver_correlation(human_profile, human_genes, mouse_profile, mouse_genes, ortholog_table) -> dict
  def ood_mahalanobis(manifold, query, alpha=1e-2) -> np.ndarray

Verified facts (RESEARCH.md): yu2022 region field = 'category' in l5_category.csv (Zone1..Zone3,
portal_area, central_area); GSE272564 APAP0h (GSM8404653) = control, NO zone annotation -> whole-sample
pseudobulk; PDG manifold (10, 10716); use 978-gene landmark subspace for Mahalanobis (Pitfall 7).
squidpy 1.8.2: use spatial_neighbors_knn (NOT deprecated spatial_neighbors), connectivity_key
"spatial_connectivities" (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: region_diagnostics.py — Moran's I SVG retention, basal similarity, gene coverage</name>
  <read_first>
    - src/spatial/pseudobulk.py (analog: pure-library + lazy-AnnData from_anndata + sparse toarray guard — PATTERNS.md lines 400-428)
    - src/spatial/gene_alignment.py (coverage_fraction for gene coverage vs 10,716 space)
    - tests/spatial/test_eda_region.py::test_moran_returns_dataframe (RED contract; importorskip squidpy)
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 6 (Moran's I API), Pitfalls 4,5,6 (squidpy deprecation; yu2022 zip coords; GSE272564 control sample)
  </read_first>
  <files>src/spatial/eda/region_diagnostics.py</files>
  <behavior>
    - compute_moran_svgs(adata, spatial_key='spatial', n_neighs=6): sq.gr.spatial_neighbors_knn then
      sq.gr.spatial_autocorr(mode='moran', connectivity_key='spatial_connectivities'); read
      adata.uns['moranI']; svgs = rows with pval_norm < 0.05; return dict svg_count, svg_fraction, top_svgs(20).
    - basal_similarity_matrix(profiles: dict[zone->vector] or array, metric='pearson'): symmetric
      zone x zone similarity DataFrame (cosine or pearson).
    - svg_retention(svg_genes, model_gene_space): fraction of raw-Visium SVGs whose symbol is in the
      10,716-gene space (uses gene_alignment.coverage_fraction).
  </behavior>
  <action>
    Create src/spatial/eda/region_diagnostics.py with the shared header and lazy AnnData usage
    (do NOT import scanpy at module level — Pitfall 9; import squidpy inside the function or guard with
    a try/except ImportError so the file imports even where squidpy is absent). Implement
    compute_moran_svgs exactly per RESEARCH Pattern 6 using spatial_neighbors_knn (n_neighs=6) and
    spatial_autocorr(mode='moran', connectivity_key='spatial_connectivities', transformation=True,
    copy=False), reading adata.uns['moranI'] (index=gene; cols include 'I','pval_norm'). Implement
    basal_similarity_matrix as a pandas DataFrame of pairwise Pearson r across zone pseudobulk vectors
    (apply the pseudobulk.py `hasattr(raw,'toarray')` sparse guard if a matrix is passed). Implement
    svg_retention using gene_alignment.coverage_fraction against the model 10,716 gene list. Note in the
    docstring that yu2022 coords must be loaded into adata.obsm['spatial'] from tissue_positions_list.csv
    (the script in plan 06 does the extraction; this library assumes obsm['spatial'] is present and
    raises KeyError with the available obsm keys if not — mirror pseudobulk.from_anndata KeyError style).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_region.py::test_moran_returns_dataframe -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_region.py::test_moran_returns_dataframe -x` exits 0 (or skips cleanly if squidpy unavailable in runner)
    - `grep -c "spatial_neighbors_knn" src/spatial/eda/region_diagnostics.py` >= 1 (NOT deprecated spatial_neighbors)
    - `grep -c "spatial_autocorr" src/spatial/eda/region_diagnostics.py` >= 1
    - `grep -Ec "def basal_similarity_matrix|def svg_retention" src/spatial/eda/region_diagnostics.py` >= 2
    - `grep -c "import scanpy" src/spatial/eda/region_diagnostics.py` == 0 (Pitfall 9: no module-level scanpy)
  </acceptance_criteria>
  <done>Moran's I SVG retention, basal similarity matrix, and gene coverage implemented; squidpy_neighbors_knn used; no module-level scanpy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: region_diagnostics.py — ortholog-filtered cross-species correlation + OOD Mahalanobis</name>
  <read_first>
    - src/spatial/orthology.py (OrthologTable.pairs columns human_symbol/mouse_symbol — verified lines 63-96)
    - tests/spatial/test_eda_region.py::test_cross_species_ortholog_filter (RED contract: n_genes_compared < len(human_genes))
    - .planning/phases/01-eda-the-bracket/01-RESEARCH.md Pattern 8 (Mahalanobis, 978-landmark subspace), Pattern 9 (human-mouse correlation), Pitfall 7 (rank-deficient full-gene covariance)
    - .planning/phases/01-eda-the-bracket/01-CONTEXT.md D-01 (cross-species liver basal diagnostics stay IN)
  </read_first>
  <files>src/spatial/eda/region_diagnostics.py</files>
  <behavior>
    - human_mouse_liver_correlation(human_profile, human_genes, mouse_profile, mouse_genes, ortholog_table):
      map human_symbol->mouse_symbol via ortholog_table.pairs; keep only genes present in BOTH profiles;
      Pearson r over the matched ortholog vector; return {'pearson_r','p_value','n_genes_compared'}.
      n_genes_compared strictly less than len(human_genes) when not all human genes have a 1:1 ortholog.
    - ood_mahalanobis(manifold (10,d), query (n,d), alpha=1e-2): ridge-regularized covariance inverse;
      return per-query Mahalanobis distance from the manifold mean. Caller projects both to the 978-gene
      landmark subspace first (Pitfall 7); document the subspace + alpha so the script reports them.
  </behavior>
  <action>
    Append to region_diagnostics.py. Implement human_mouse_liver_correlation exactly per RESEARCH
    Pattern 9: build h_to_m = dict(zip(pairs['human_symbol'], pairs['mouse_symbol'])), index maps for
    human/mouse gene lists, collect matched (h_val, m_val) pairs only where both genes are present,
    scipy.stats.pearsonr; return the 3-key dict. Implement ood_mahalanobis exactly per RESEARCH Pattern 8:
    cov = np.cov(manifold.T); cov_reg = cov + alpha*np.eye(d); vi = np.linalg.inv(cov_reg);
    mean_ref = manifold.mean(axis=0); return np.array([mahalanobis(q, mean_ref, vi) for q in query]).
    Add a module constant/docstring note that the RECOMMENDED call projects manifold+query to the 978
    LINCS landmark genes before this function (well-conditioned), and that the chosen method
    ("Mahalanobis, 978-gene landmark subspace, alpha=1e-2") must be named in P1_eda.md (Claude's
    Discretion resolved to Mahalanobis per RESEARCH recommendation). Use
    `from scipy.spatial.distance import mahalanobis` and `from scipy.stats import pearsonr`.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env pytest tests/spatial/test_eda_region.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `conda run -n dili_v04_env pytest tests/spatial/test_eda_region.py::test_cross_species_ortholog_filter -x` exits 0
    - `grep -c "human_symbol" src/spatial/eda/region_diagnostics.py` >= 1 (ortholog one-to-one filter, XC-08)
    - `grep -c "mahalanobis" src/spatial/eda/region_diagnostics.py` >= 1
    - `grep -Ec "978|landmark" src/spatial/eda/region_diagnostics.py` >= 1 (subspace named, Pitfall 7)
    - `grep -c "pearsonr" src/spatial/eda/region_diagnostics.py` >= 1
  </acceptance_criteria>
  <done>Ortholog-filtered human-mouse correlation and ridge-regularized Mahalanobis OOD implemented; 978-landmark subspace and Mahalanobis method named; cross-species test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visium .h5/coords + PDG manifold → diagnostics | external spatial inputs cross into squidpy/Mahalanobis; coords come from in-zip tissue_positions_list.csv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | adata.obsm['spatial'] missing/malformed | mitigate | KeyError with available-keys message if obsm['spatial'] absent (mirror pseudobulk.from_anndata); script populates coords from tissue_positions_list.csv (Pitfall 5) |
| T-01-09 | DoS | full 10,716-gene covariance inversion (rank-deficient) | mitigate | Project to 978-gene landmark subspace + ridge alpha before np.linalg.inv (Pitfall 7) |
</threat_model>

<verification>
- `conda run -n dili_v04_env pytest tests/spatial/test_eda_region.py -q` green (squidpy test skips cleanly if unavailable).
- spatial_neighbors_knn (not deprecated), 978-landmark Mahalanobis, ortholog one-to-one filter all present.
</verification>

<success_criteria>
EDA-03 region + cross-species diagnostics implemented: Moran's I SVG retention, basal
similarity, gene coverage, ortholog-filtered human-rodent correlation, and 978-landmark
Mahalanobis OOD — all passing their plan-01 tests with the chosen methods named.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-05-SUMMARY.md`
</output>
