---
phase: 01-eda-the-bracket
plan: 07
type: execute
wave: 3
depends_on: [01-03, 01-04, 01-06]
files_modified:
  - src/spatial/eda/floor.py
  - scripts/run_p1_eda.py
  - tests/spatial/test_eda_floor.py
  - results/tables/P1_eda.md
autonomous: true
requirements: [EDA-01, EDA-03]
gap_closure: true

must_haves:
  truths:
    - "The structure floor has a profile-level, drug-disjoint AUROC computed on the SAME aligned profile rows and SAME StratifiedGroupKFold drug folds the ceiling uses (D-06 true head-to-head)."
    - "P1_eda.md shows a profile-level drug-disjoint head-to-head row placing floor(profile-disjoint) next to ceiling(profile-disjoint), labeled the supporting/better-powered sensitivity view, distinct from the PRIMARY drug-level gate comparison (D-06)."
    - "P1_eda.md carries the D-07 guidance note: drug-disjoint (group-aware) CV is the recommended evaluation for every ceiling/floor/AUROC comparison across the milestone, enforcement per-phase guidance not a project hard rule."
    - "A regression test asserts the new floor profile-disjoint function groups by drug (a drug's profiles never straddle train/test), mirroring test_ceiling_no_drug_leakage."
    - "Halt Gate 2 firing, the PRIMARY drug-level gate, and HALT_REASON.md are unchanged; the +0.31 benchmark-leakage finding stays documented in P1_eda.md."
  artifacts:
    - path: "src/spatial/eda/floor.py"
      provides: "floor_profile_disjoint_auroc — profile-level drug-disjoint structure AUROC via StratifiedGroupKFold grouped by compound"
      contains: "def floor_profile_disjoint_auroc("
    - path: "scripts/run_p1_eda.py"
      provides: "profile-level drug-disjoint head-to-head wiring (profile->drug ECFP4 expansion on the aligned ceiling profile set) + D-07 guidance writer"
      contains: "floor_profile_disjoint_auroc"
    - path: "tests/spatial/test_eda_floor.py"
      provides: "regression test mirroring test_ceiling_no_drug_leakage for the profile-disjoint floor"
      contains: "profile_disjoint"
    - path: "results/tables/P1_eda.md"
      provides: "profile-level drug-disjoint head-to-head section + D-07 guidance note"
      contains: "profile-level drug-disjoint"
  key_links:
    - from: "scripts/run_p1_eda.py run_ceiling profile->drug alignment (profile_names + in_dilirank_mask + drug_key)"
      to: "src/spatial/eda/floor.py floor_profile_disjoint_auroc"
      via: "expand each aligned profile to its drug's ECFP4 fingerprint, group by the same lowercased compound_name key, StratifiedGroupKFold seed=42"
      pattern: "floor_profile_disjoint_auroc"
    - from: "src/spatial/eda/floor.py floor_profile_disjoint_auroc"
      to: "results/tables/P1_eda.md profile-level drug-disjoint head-to-head row"
      via: "driver writes floor(profile-disjoint) beside ceiling(profile-disjoint) from _leakage_decomposition"
      pattern: "profile-level drug-disjoint"
---

<objective>
Close the single reframe gap (D-06): add the structure floor's **profile-level,
drug-disjoint** AUROC so the profile-level sensitivity view is a true head-to-head.

Today the asymmetry is: the driver's `_leakage_decomposition()` gives the CEILING a
profile-level drug-disjoint number (0.605 disjoint vs 0.912 leaky), but `floor.py`
only produces a DRUG-level number. So the profile-level sensitivity view compares a
ceiling number against nothing on the floor side. This plan mirrors the existing
`compute_ceiling` StratifiedGroupKFold-grouped-by-compound pattern on the structure
side: expand each measured profile to ITS DRUG's ECFP4 fingerprint (using the exact
profile->drug alignment `run_ceiling` already builds), run StratifiedGroupKFold grouped
by the same lowercased `compound_name` key at profile granularity, and report the floor
AUROC next to the ceiling's profile-disjoint AUROC. Then add the D-07 guidance note.

Purpose: make the profile-level sensitivity view (D-06) a genuine apples-to-apples
floor-ceiling comparison on identical profile rows and identical drug-disjoint folds,
and record the milestone-wide leakage-discipline guidance (D-07).

Output: extended `src/spatial/eda/floor.py`, extended `scripts/run_p1_eda.py`, a new
regression test, and the finalized `results/tables/P1_eda.md` with the head-to-head row
and the D-07 note.

**HARD scope boundary (DEFERRED in 01-CONTEXT.md "Deferred Ideas" — do NOT touch):**
- Do NOT expand the negative-drug set or re-power the gate (38-negative question is deferred to the NEXT gate review).
- Do NOT re-run, alter, or re-decide the PRIMARY drug-level gate, the Halt Gate 2 firing, or HALT_REASON.md. The halt stands.
- Do NOT touch other organs, rodent floor/labels, or any model/training code (Phase 2+). Still "no model code".
- Do NOT re-resolve SMILES from scratch — reuse the sibling v0.5 resolved tables already wired in the driver.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-eda-the-bracket/01-CONTEXT.md

# Source-of-truth pattern to MIRROR (read before implementing):
@src/spatial/eda/ceiling.py
@scripts/run_p1_eda.py
@src/spatial/eda/floor.py
@tests/spatial/test_eda_ceiling.py

<interfaces>
<!-- The contracts the executor needs. Extracted from the codebase. Use directly; no exploration. -->

From src/spatial/eda/ceiling.py — the StratifiedGroupKFold pattern to mirror (compute_ceiling):
```python
# Per-fold StandardScaler + LogisticRegression pipeline, grouped by drug:
drug_key = profiles["compound_name"].str.lower().str.strip().values   # grouping key normalization
clf = make_pipeline(
    StandardScaler(),
    LogisticRegression(max_iter=2000, class_weight="balanced", random_state=seed),  # seed=42 default
)
cv = StratifiedGroupKFold(n_splits=eff_splits, shuffle=True, random_state=seed)
oof_proba = cross_val_predict(clf, de, y, cv=cv, groups=drug_key, method="predict_proba")
```

From scripts/run_p1_eda.py — the EXISTING ceiling profile-level decomposition to head-to-head against:
```python
def _leakage_decomposition(de_aligned, y_profiles, drug_key, seed=42) -> dict:
    # returns {"profile_auroc_leaky", "profile_auroc_disjoint", "leakage_inflation"}
    # profile_auroc_disjoint uses StratifiedGroupKFold(5, shuffle=True, random_state=seed) groups=drug_key
```

From scripts/run_p1_eda.py run_ceiling() — the profile->drug alignment ALREADY built (reuse it):
```python
labels_df = load_dilirank(str(DILIRANK_PATH))
dilirank_map = dict(zip(labels_df["name_lower"], labels_df["dili_binary"]))
profile_names = profiles["compound_name"].str.lower().str.strip()
in_dilirank_mask = profile_names.isin(dilirank_map)
profiles_aligned = profiles[in_dilirank_mask].copy()
de_aligned = de[in_dilirank_mask.values]
y_profiles = profile_names[in_dilirank_mask].map(dilirank_map).values.astype(int)
drug_key = profiles_aligned["compound_name"].str.lower().str.strip().values
```

From scripts/run_p1_eda.py run_floor() — how drug-name->fingerprint is built (reuse for expansion):
```python
# smiles_df has columns: smiles, dili_binary, name_lower
# fps_valid, y_valid, valid_drug_names are aligned ECFP4 rows (name_lower keys)
fps, valid_mask = smiles_to_ecfp4(smiles_covered)   # fps shape (n, 2048) uint8
valid_drug_names = smiles_df.loc[covered_mask, "name_lower"].values[valid_mask]
```

From src/spatial/eda/fingerprints.py:
```python
def smiles_to_ecfp4(smiles_list) -> tuple[np.ndarray, np.ndarray]:  # (fps (n,2048) uint8, valid_mask (n,) bool)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: floor_profile_disjoint_auroc — profile-level drug-disjoint floor in floor.py (RED to GREEN)</name>
  <read_first>
    - src/spatial/eda/floor.py (the file being modified; mirror its float32 cast + LR convention)
    - src/spatial/eda/ceiling.py (SOURCE-OF-TRUTH pattern: StratifiedGroupKFold + StandardScaler + LogisticRegression pipeline grouped by compound, the eff_splits=min(n_splits, n_unique_groups) degenerate guard)
    - tests/spatial/test_eda_ceiling.py (test_ceiling_no_drug_leakage — the regression test to MIRROR)
    - tests/spatial/test_eda_floor.py (the test file being modified)
  </read_first>
  <behavior>
    - Test (mirror test_ceiling_no_drug_leakage): build n_drugs=10, profiles_per_drug=8.
      Give each drug a UNIQUE per-drug constant identity feature dim (sig[d]=5.0,
      repeated across its profiles) plus tiny noise, label = 1 if d < 5 else 0,
      groups = [f"drug{d}"]*8. Assert floor_profile_disjoint_auroc(fps_expanded, y, groups)
      returns AUROC < 0.75 (a held-out drug's identity dim is never seen with a label,
      so drug-disjoint CV cannot memorize it). This locks the GroupKFold grouping.
    - Existing test_floor_auroc_above_chance must still pass (no regression to compute_floor).
  </behavior>
  <action>
    Add a new pure-library function to `src/spatial/eda/floor.py` (append; do NOT modify
    compute_floor / floor_probabilities). Add it to `__all__`.

    Signature:
    ```python
    def floor_profile_disjoint_auroc(
        fps: np.ndarray,        # (n_profiles, n_bits) — each PROFILE row carries ITS DRUG's ECFP4 fingerprint
        y: np.ndarray,          # (n_profiles,) profile-level binary DILI labels
        groups: np.ndarray,     # (n_profiles,) drug grouping key (lowercased compound_name)
        n_splits: int = 5,
        seed: int = 42,
    ) -> float:
    ```
    Implementation — MIRROR compute_ceiling's leakage-free path exactly so floor and
    ceiling sit on identical folds:
    - `_validate_inputs(fps, y)` reuse (raise on ndim/length mismatch); additionally raise
      ValueError if `len(groups) != len(y)`.
    - `fps = np.asarray(fps, dtype=np.float32)` (matches existing floor memory convention);
      `y = np.asarray(y, dtype=int)`.
    - Build the SAME per-fold pipeline as the ceiling:
      `clf = make_pipeline(StandardScaler(), LogisticRegression(max_iter=2000, class_weight="balanced", random_state=seed))`
      (import make_pipeline, StandardScaler, StratifiedGroupKFold at the top of floor.py
      alongside the existing sklearn imports).
    - `n_unique = len(set(groups))`; `eff_splits = min(n_splits, n_unique)`.
    - If `eff_splits >= 2`: `cv = StratifiedGroupKFold(n_splits=eff_splits, shuffle=True, random_state=seed)`
      then `oof = cross_val_predict(clf, fps, y, cv=cv, groups=groups, method="predict_proba")[:, 1]`.
      Else (degenerate <2 groups): fall back to a small StratifiedKFold like compute_ceiling does
      (`StratifiedKFold(n_splits=max(2, min(n_splits, len(y))), shuffle=True, random_state=seed)`).
    - Return `float(roc_auc_score(y, oof))` at PROFILE granularity (no drug aggregation — this is
      the profile-level sensitivity view, the head-to-head partner of the ceiling's
      _leakage_decomposition profile_auroc_disjoint).
    - `log.info(...)` a one-line summary (n_profiles, n_drugs, auroc, seed) matching the module's logging style.

    Then add the regression test to `tests/spatial/test_eda_floor.py`:
    `def test_floor_profile_disjoint_no_drug_leakage()` exactly mirroring
    test_ceiling_no_drug_leakage (per-drug identity dim, 8 profiles/drug), asserting
    `floor_profile_disjoint_auroc(de, y, np.asarray(names)) < 0.75`. Import the new function
    at the top of the test file. Run RED first (function absent then ImportError/fail), then GREEN.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import sys; s=open('src/spatial/eda/floor.py').read(); sys.exit(0 if 'def floor_profile_disjoint_auroc(' in s and 'StratifiedGroupKFold' in s else 1)" && conda run -n dili_v04_env python -m pytest tests/spatial/test_eda_floor.py -k "profile_disjoint or above_chance" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def floor_profile_disjoint_auroc(' src/spatial/eda/floor.py` returns 1.
    - `grep StratifiedGroupKFold src/spatial/eda/floor.py` matches (group-aware CV present).
    - `grep -c 'profile_disjoint' tests/spatial/test_eda_floor.py` is >= 1.
    - `pytest tests/spatial/test_eda_floor.py -k profile_disjoint` exits 0 (GREEN).
    - `pytest tests/spatial/test_eda_floor.py -k above_chance` still exits 0 (no regression).
  </acceptance_criteria>
  <done>floor.py exposes floor_profile_disjoint_auroc using StratifiedGroupKFold grouped by drug + per-fold StandardScaler/LR (seed=42), and the mirrored regression test is GREEN while the existing floor test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire profile->drug ECFP4 expansion + write head-to-head row and D-07 note in run_p1_eda.py</name>
  <read_first>
    - scripts/run_p1_eda.py (the file being modified: run_floor builds fps/valid_drug_names by name_lower; run_ceiling builds profiles_aligned, de_aligned, y_profiles, drug_key, and calls _leakage_decomposition which yields profile_auroc_disjoint for the ceiling)
    - src/spatial/eda/ceiling.py (SOURCE-OF-TRUTH grouping key normalization: compound_name.str.lower().str.strip())
    - src/spatial/eda/floor.py (the new floor_profile_disjoint_auroc signature from Task 1)
  </read_first>
  <action>
    In `scripts/run_p1_eda.py`:

    1. Import the new function: extend the existing floor import line to
       `from src.spatial.eda.floor import compute_floor, floor_probabilities, floor_profile_disjoint_auroc  # noqa: E402`.

    2. Make a per-drug fingerprint lookup available. In `run_floor`, after `floor_probs_by_drug`
       is built, also build and return a `floor_fp_by_drug: dict[str, np.ndarray]` mapping
       `name_lower -> fps_valid[i]` (the drug's ECFP4 row, float32). Add `"floor_fp_by_drug"`
       to the dict run_floor returns. (Reuse `valid_drug_names` and `fps_valid` already in scope —
       do NOT re-resolve SMILES or recompute fingerprints.)

    3. Expose the ceiling's aligned profile rows. In `run_ceiling`, add to its returned dict the
       three variables already computed there: `"de_aligned"`, `"y_profiles"`, `"drug_key"`.

    4. In `run_gap_and_gate`, AFTER the existing PRIMARY drug-level bootstrap/gate table is
       written (do NOT change any of it), compute the profile-level drug-disjoint floor on the
       SAME aligned profile set the ceiling used:
       - Pull `floor_fp_by_drug = floor_data["floor_fp_by_drug"]`,
         `drug_key = ceiling_data["drug_key"]`, `y_profiles = ceiling_data["y_profiles"]`.
       - Build the profile-expanded floor fingerprint matrix: for each profile index i, look up its
         drug's fingerprint via `floor_fp_by_drug.get(drug_key[i])`. Keep only profiles whose drug
         has a fingerprint (intersection with the floor's ECFP4-covered drugs), giving
         `fps_prof` (n_kept, 2048), `y_kept`, `groups_kept` — all aligned, drug-disjoint-ready.
       - Call `floor_auroc_profile_disjoint = floor_profile_disjoint_auroc(fps_prof, y_kept, groups_kept, n_splits=5, seed=42)`.
       - Read the ceiling's numbers from the existing decomposition:
         `ceiling_auroc_profile_disjoint = ceiling_data["ceiling_result"]["leakage"]["profile_auroc_disjoint"]`
         (and `["profile_auroc_leaky"]` if you wish to show it).
       - Append a section to `report_lines` with this EXACT heading text:
         `### Profile-level drug-disjoint head-to-head (supporting sensitivity view, D-06)`
         followed by a table (use the literal phrase "profile-level drug-disjoint" in the prose):
         ```
         | Profile-level drug-disjoint AUROC | Value |
         |--------|-------|
         | Structure floor (ECFP4, profile-disjoint) | {floor_auroc_profile_disjoint:.4f} |
         | Measured ceiling (LINCS DE, profile-disjoint) | {ceiling_auroc_profile_disjoint:.4f} |
         | Ceiling minus floor (profile-disjoint) | {ceiling_auroc_profile_disjoint - floor_auroc_profile_disjoint:+.4f} |
         ```
         Prose MUST state: "This profile-level drug-disjoint comparison is the **supporting,
         better-powered sensitivity view** (D-06). The PRIMARY gate operates on the drug-level
         drug-disjoint comparison above; Halt Gate 2's firing is unchanged by this row." Also note
         both numbers sit on the SAME aligned profile rows and the SAME StratifiedGroupKFold drug
         folds (seed=42) as the ceiling's leakage decomposition, so it is a true head-to-head.

    5. Append the D-07 guidance note as its own section (after the head-to-head), with this EXACT
       heading and content:
       ```
       ### Leakage-discipline guidance (D-07)
       Drug-disjoint (group-aware) cross-validation is the **recommended** evaluation for every
       ceiling/floor/AUROC comparison across this milestone, including Phase 2+ predicted-signature
       evals. Enforcement is per-phase planner guidance, not a project hard rule. The +0.31 AUROC
       benchmark drug-leakage finding (profile-level 0.912 leaky vs 0.605 drug-disjoint) stays
       documented in this report (see the EDA-02 Leakage decomposition section).
       ```
       (The +0.31 finding is already present in the EDA-02 section; this note references and
       preserves it — do not delete or alter the existing decomposition.)

    Use seed=42 throughout (matches `_leakage_decomposition`). Do NOT alter the PRIMARY gate table,
    the bootstrap, the Power section, the Interpretation caveat, `_write_halt_reason`, or the
    gate_fires/sys.exit logic.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import sys; s=open('scripts/run_p1_eda.py').read(); ok = ('floor_profile_disjoint_auroc' in s) and ('Profile-level drug-disjoint head-to-head' in s) and ('Leakage-discipline guidance (D-07)' in s) and ('floor_fp_by_drug' in s); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python -c "import ast,sys; ast.parse(open('scripts/run_p1_eda.py').read()); print('parses ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'floor_profile_disjoint_auroc' scripts/run_p1_eda.py` is >= 2 (import + call site).
    - `grep -c 'floor_fp_by_drug' scripts/run_p1_eda.py` is >= 2 (built in run_floor, used in gate).
    - `grep 'Profile-level drug-disjoint head-to-head' scripts/run_p1_eda.py` matches (head-to-head heading writer present).
    - `grep 'Leakage-discipline guidance (D-07)' scripts/run_p1_eda.py` matches (D-07 note writer present).
    - run_ceiling returns dict includes `de_aligned`, `y_profiles`, `drug_key` (`grep -c '"drug_key"' scripts/run_p1_eda.py` is >= 1).
    - `python -c "import ast; ast.parse(open('scripts/run_p1_eda.py').read())"` succeeds (file parses).
    - The PRIMARY gate table, `_write_halt_reason`, and `gate_fires`/`sys.exit(1)` lines are unchanged (HALT logic untouched).
  </acceptance_criteria>
  <done>The driver reuses the existing profile->drug alignment to expand the floor to profile granularity on the ceiling's exact aligned rows + drug folds (seed=42), writes the "Profile-level drug-disjoint head-to-head (supporting sensitivity view, D-06)" section with floor beside ceiling, and writes the D-07 guidance note — without touching the PRIMARY gate, the firing, or HALT_REASON.md.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate P1_eda.md via run_p1_eda.py all and confirm head-to-head + D-07 strings present</name>
  <read_first>
    - scripts/run_p1_eda.py (the driver to run; note `all` writes P1_eda.md then sys.exit(1) when Halt Gate 2 fires — exit 1 is EXPECTED and correct, the halt stands)
    - results/tables/P1_eda.md (the existing report; confirm the +0.31 leakage decomposition and PRIMARY gate sections are preserved after regeneration)
    - .planning/phases/01-eda-the-bracket/HALT_REASON.md (must remain unchanged in substance — the gate still fires)
  </read_first>
  <action>
    Run the driver end-to-end on real data to regenerate the report with the new sections:
    `conda run -n dili_v04_env python scripts/run_p1_eda.py all`.

    Expected: the run computes floor + ceiling + gap/gate + the new profile-level drug-disjoint
    head-to-head + D-07 note + diagnostics, writes `results/tables/P1_eda.md`, and (because
    Halt Gate 2 fires) writes HALT_REASON.md and exits with code 1. **Exit code 1 here is the
    correct, expected outcome — the halt stands; do NOT treat it as a failure and do NOT
    suppress or alter the gate.** Capture the run with `; echo "exit=$?"` so the nonzero exit is
    visible without aborting the step.

    After the run, confirm:
    - `results/tables/P1_eda.md` contains the literal string "profile-level drug-disjoint"
      (head-to-head section) and "Leakage-discipline guidance (D-07)".
    - The pre-existing EDA-02 "Leakage decomposition (Phase 1 headline)" section and its
      0.912 / 0.605 / +0.31 numbers are still present (D-07 preservation).
    - The PRIMARY "Gap + Halt Gate 2" section and the "Halt Gate 2 | **FIRES**" verdict are
      still present and unchanged.
    Do NOT hand-edit P1_eda.md — it must be the regenerated driver output so it stays
    reproducible from `run_p1_eda.py all` (seed=42).
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python scripts/run_p1_eda.py all; echo "driver_exit=$?"; conda run -n dili_v04_env python -c "import sys; s=open('results/tables/P1_eda.md').read(); ok = ('profile-level drug-disjoint' in s) and ('Leakage-discipline guidance (D-07)' in s) and ('Leakage decomposition (Phase 1 headline)' in s) and ('FIRES' in s); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'profile-level drug-disjoint' results/tables/P1_eda.md` is >= 1 (head-to-head section present).
    - `grep 'Leakage-discipline guidance (D-07)' results/tables/P1_eda.md` matches (D-07 note present).
    - `grep 'Leakage decomposition (Phase 1 headline)' results/tables/P1_eda.md` matches (+0.31 finding preserved, D-07).
    - `grep 'FIRES' results/tables/P1_eda.md` matches (Halt Gate 2 still fires — halt stands).
    - HALT_REASON.md still present and still records gate_fires True (no substantive change).
  </acceptance_criteria>
  <done>P1_eda.md is regenerated from `run_p1_eda.py all` (seed=42) carrying the profile-level drug-disjoint head-to-head row (floor beside ceiling) and the D-07 guidance note, while the EDA-02 +0.31 leakage decomposition, the PRIMARY drug-level gate, and the Halt Gate 2 FIRES verdict remain intact.</done>
</task>

</tasks>

<verification>
- floor.py exposes `floor_profile_disjoint_auroc` (StratifiedGroupKFold grouped by drug, per-fold StandardScaler + LR, seed=42); mirrored regression test GREEN; existing floor + ceiling tests still pass.
- run_p1_eda.py imports and calls the new function on the ceiling's exact aligned profile rows + drug folds, writes the head-to-head section and the D-07 note; PRIMARY gate and HALT logic untouched.
- P1_eda.md regenerated with both new sections; +0.31 leakage decomposition and Halt Gate 2 FIRES verdict preserved.
- No model/training code, no negative-set expansion, no SMILES re-resolution, no other organs (scope boundary held).
</verification>

<success_criteria>
- D-06 satisfied: the structure floor now has a profile-level, drug-disjoint AUROC computed on the SAME aligned profile rows + SAME drug-disjoint folds as the ceiling, written next to ceiling(profile-disjoint) in P1_eda.md as the supporting/better-powered sensitivity view, distinct from the PRIMARY drug-level gate comparison.
- D-07 satisfied: P1_eda.md carries the leakage-discipline guidance note (drug-disjoint CV recommended milestone-wide; per-phase guidance, not a hard rule) and preserves the +0.31 benchmark-leakage finding.
- Regression test locks the GroupKFold grouping for the new floor function.
- Halt Gate 2 firing, the PRIMARY drug-level gate, and HALT_REASON.md unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-eda-the-bracket/01-07-SUMMARY.md`.
</output>
