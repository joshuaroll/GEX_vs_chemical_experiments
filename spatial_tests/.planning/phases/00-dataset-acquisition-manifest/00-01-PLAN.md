---
phase: 00-dataset-acquisition-manifest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/spatial/datasets.py
  - tests/test_data_paths.py
  - tests/fixtures/biomart_chr21_sample.tsv
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
user_setup: []

must_haves:
  truths:
    - "The registry no longer contains the stale accessions GSE189994 (Yu) or GSE144239 (Maynard)."
    - "Yu liver resolves to figshare 22321447; Lake/KPMP primary is GSE183456 + GSE183279; Maynard is spatialLIBD; Siletti is relabeled snRNA-seq (not MERFISH)."
    - "Every SpatialDataset carries species, access_mechanism, expected_files, license, whole_transcriptome, and slug fields."
    - "The `slug` field is the single source of truth for each dataset's data/raw/spatial/<slug>/ directory — the download driver (Plan 02) and test_raw_datasets_present both read entry.slug, neither slugifies independently."
    - "tests/test_data_paths.py exists with the Wave-0 stub tests and runs offline (network tests skipped by default)."
  artifacts:
    - path: "src/spatial/datasets.py"
      provides: "Corrected, extended SpatialDataset registry (the Halt-Gate-1 whole_transcriptome flag + canonical slug live here)"
      contains: "whole_transcriptome"
    - path: "tests/test_data_paths.py"
      provides: "Sanity-only data-path test suite for DATA-01/02/03"
      contains: "def test_accessions_corrected"
    - path: "tests/fixtures/biomart_chr21_sample.tsv"
      provides: "Offline deterministic BioMart fixture for the ortholog test"
      min_lines: 5
  key_links:
    - from: "tests/test_data_paths.py"
      to: "src/spatial/datasets.py"
      via: "import SPATIAL_DATASETS; assert corrected accessions; resolve dirs via entry.slug"
      pattern: "from src.spatial.datasets import SPATIAL_DATASETS"
    - from: "tests/test_data_paths.py"
      to: "src/spatial/gene_alignment.coverage_fraction"
      via: "whole-transcriptome gate uses coverage_fraction(...) > 0.80"
      pattern: "coverage_fraction"
---

<objective>
Correct the dataset registry and lay the Wave-0 test scaffold. This is the foundation
plan: every download and every coverage check downstream reads the corrected
`SPATIAL_DATASETS` registry, and every requirement is checked by the new
`tests/test_data_paths.py`. DATA-01's acceptance criterion is explicit that the
accession corrections must be applied to the registry BEFORE any download script runs —
so this plan ships first, in its own wave.

Purpose: Single-source the verified-correct accessions so no download pulls wrong/empty
data, and create the sanity-only test suite (per the hard rule "tests never on model
outputs") that gates the rest of the phase.
Output: extended/corrected `src/spatial/datasets.py`, new `tests/test_data_paths.py`,
and a small offline BioMart fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md
@.planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md
@.planning/phases/00-dataset-acquisition-manifest/00-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor must extend/use. Do NOT explore the codebase to find these. -->

From src/spatial/datasets.py (EXISTING — extend in place, do NOT rebuild):
```python
class SpatialDataset(NamedTuple):
    name: str
    organ: str            # "liver" | "kidney" | "brain" | "heart"
    platform: str         # "Visium" | "MERFISH" | "CosMx" | "Xenium" | "ISS"
    accession: str
    region_annotation_source: str
    usable_as_input: bool

SPATIAL_DATASETS: Final[list[SpatialDataset]] = [ ... ]   # positional constructor calls today
```

From src/spatial/config.py (import, never redefine):
```python
N_LANDMARK: Final[int] = 978
N_PDG: Final[int] = 10716      # the 10,716-gene MultiDCP coverage denominator
```

From src/spatial/gene_alignment.py (reuse as-is for the whole-transcriptome gate):
```python
def coverage_fraction(source_genes: list[str], target_genes: list[str]) -> float: ...
```

From conftest.py (the `network` marker is registered but UNUSED — be its first user):
```python
# marker docstring names the env gate: TDC_NETWORK_TESTS=1
"network: mark a test that requires internet access (skipped by default; set TDC_NETWORK_TESTS=1 ...)"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct and extend the SpatialDataset registry</name>
  <read_first>
    - src/spatial/datasets.py (the file being modified — read in full)
    - src/spatial/config.py (import N_PDG; do not redefine)
    - .planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md (section "src/spatial/datasets.py (EXTEND)" — exact field list and per-line corrections)
    - .planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md (lines 276-302 "Verified accession resolution")
  </read_first>
  <behavior>
    - Test (test_accessions_corrected): no entry's `accession` contains "GSE189994" or "GSE144239".
    - Test (test_accessions_corrected): the Yu liver entry's `accession` contains "22321447".
    - Test (test_accessions_corrected): some entry's `accession` contains "GSE183456" AND some contains "GSE183279" (Lake/KPMP primary).
    - Test (test_registry_schema): every SpatialDataset has non-empty `species`, `access_mechanism`, `license`, and `slug` strings and a bool `whole_transcriptome`; every `slug` is unique, lowercase, and matches `^[a-z0-9_]+$`.
  </behavior>
  <action>
Extend the `SpatialDataset` NamedTuple (currently at datasets.py:48-53) by ADDING these six fields, and update the numpydoc `Attributes` block to document each:
```python
class SpatialDataset(NamedTuple):
    name: str
    organ: str
    species: str                       # NEW: "human" | "mouse" | "rat"
    platform: str
    accession: str                     # CORRECTED values
    access_mechanism: str              # NEW: "figshare_api"|"geo_supp"|"spatialLIBD"|"kpmp"|"url"
    expected_files: tuple[str, ...]    # NEW: filenames expected on disk after download
    license: str                       # NEW: SPDX-style tag or source license string
    whole_transcriptome: bool          # NEW: the Halt-Gate-1 flag (genome-scale Visium=True, fixed panel=False)
    slug: str                          # NEW: canonical lowercase data/raw/spatial/<slug> dir name — SINGLE SOURCE OF TRUTH (no independent slugification anywhere)
    usable_as_input: bool
    region_annotation_source: str
```
The `slug` field is load-bearing: both `tests/test_data_paths.py::test_raw_datasets_present` (Task 2) and the Plan 02 download driver `scripts/download_spatial.py` MUST read `entry.slug` directly to build `data/raw/spatial/<slug>/`. Neither component is permitted to slugify `name` independently — that is exactly the desync bug `slug` prevents. Set each `slug` to the lowercase stable name used below (e.g. `"yu2022_liver"`, `"lake_kpmp_kidney"`, `"maynard_dlpfc"`, `"gse280652_apap_liver"`, `"gse272564_apap_liver"`, `"andrews_liver"`, plus the rodent slugs); slugs must be unique and match `^[a-z0-9_]+$`.

Convert ALL existing entries in `SPATIAL_DATASETS` from positional to KEYWORD-arg constructor calls (positional shifts would silently misalign — keyword args make the diff safe). Set `species` for each (existing human Visium/MERFISH = "human") and set each entry's `slug`.

Apply these CORRECTIONS (these supersede both datasets.py AND the stale DOC-09 corrections):
  1. Yu liver (datasets.py:64-71): set `accession="figshare: 22321447 (DOI 10.6084/m9.figshare.22321447.v1)"`, `access_mechanism="figshare_api"`, `expected_files=("L5_upload.zip", "L18_upload.zip")`, `license="GPL-3.0+"`, `whole_transcriptome=True`, `slug="yu2022_liver"`. Remove BOTH the GSE189994 and figshare 17058105 strings.
  2. Lake/KPMP kidney (datasets.py:91-98): set `accession="GEO: GSE183456 + GSE183279 (superseries); KPMP: https://atlas.kpmp.org"`, `access_mechanism="geo_supp"`, `whole_transcriptome=True`, `slug="lake_kpmp_kidney"`. Add a SEPARATE substitute entry or note GSE211785 (Abedini 2024) as a documented fallback in `name`/`expected_files` notes — do NOT keep GSE211785 as the Lake/KPMP primary accession.
  3. Maynard brain (datasets.py:118-125): set `accession="spatialLIBD / LieberInstitute (http://spatial.libd.org/; Bioconductor spatialLIBD)"`, `access_mechanism="spatialLIBD"`, `whole_transcriptome=True`, `slug="maynard_dlpfc"`. Remove the GSE144239 string.
  4. Siletti brain (datasets.py:126-133): relabel — it is snRNA-seq, NOT human MERFISH. Set `platform="snRNA-seq"`, `whole_transcriptome=False`, `usable_as_input=False`, and update `name`/`region_annotation_source` note to "no public human-brain MERFISH exists (ABC MERFISH is mouse-only)".
For all remaining entries, fill the new fields with accurate values: Visium genome-scale → `whole_transcriptome=True`; MERFISH/Xenium fixed-panel → `whole_transcriptome=False`. Set `license` to the source's known license or "see source" where unverified, and `access_mechanism` per the dataset's repository ("geo_supp" for GEO, "url" otherwise).

ADD validation-anchor entries (mouse APAP liver Visium, validation-only, never basal — set `usable_as_input=False`, `whole_transcriptome=True`, `species="mouse"`): GSE280652 (`slug="gse280652_apap_liver"`) and GSE272564 (`slug="gse272564_apap_liver"`), `access_mechanism="geo_supp"`.

ADD rodent basal-context Visium candidate entries (whole_transcriptome=True, usable_as_input=True), recording the planner-selected primary healthy series per organ in `name`/`expected_files` notes (per 00-RESEARCH.md Open Question 1, RESOLVED), each with a unique `slug`:
  - mouse liver: a control/zonation series (record candidate, e.g. GSE272564 control arm) species="mouse".
  - mouse kidney: GSE252772 (lifespan/sex atlas, has healthy) species="mouse".
  - mouse brain: GSE233983 (aging brain atlas) species="mouse".
Mark each rodent entry's `region_annotation_source` note "healthy/control spots to be confirmed at download (Pitfall 5)". Selecting these is a planning task, NOT a gate blocker.

Update the module docstring "Hard rules enforced" block to note: `whole_transcriptome=True` ⇔ genome-scale Visium; fixed panels → False; APAP series are validation-only; `slug` is the single source of truth for the on-disk directory name.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -m pytest tests/test_data_paths.py::test_accessions_corrected tests/test_data_paths.py::test_registry_schema -x -q</automated>
  </verify>
  <acceptance_criteria>No registry entry contains GSE189994 or GSE144239; Yu accession contains 22321447; GSE183456 and GSE183279 both present; every entry has species/access_mechanism/expected_files/license/whole_transcriptome/slug fields populated; every `slug` is unique, lowercase, matches `^[a-z0-9_]+$`. The 124 existing tests still import datasets.py without error.</acceptance_criteria>
  <done>No registry entry contains GSE189994 or GSE144239; Yu accession contains 22321447; GSE183456 and GSE183279 both present; every entry has species/access_mechanism/expected_files/license/whole_transcriptome/slug fields populated; slugs unique+lowercase. The 124 existing tests still import datasets.py without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_data_paths.py Wave-0 stubs + offline BioMart fixture</name>
  <read_first>
    - tests/spatial/test_gene_alignment.py (style/fixture analog — read the docstring + first 90 lines for the one-behavior-per-test pattern)
    - conftest.py (the registered `network` marker, lines 10-16)
    - .planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md (section "tests/test_data_paths.py (NEW)")
    - .planning/phases/00-dataset-acquisition-manifest/00-VALIDATION.md (the Per-Task Verification Map — exact test function names)
    - src/spatial/gene_alignment.py (coverage_fraction signature)
    - src/spatial/datasets.py (the corrected registry from Task 1 — read the new `slug` field)
  </read_first>
  <action>
Create `tests/test_data_paths.py` (flat under tests/, NOT under tests/spatial/ — matches the DATA-01 acceptance wording and the sibling dili_downstream convention; leave the 124 tests/spatial/ tests untouched). Open with the enumerated `"""Unit tests for ... Behaviors covered: ..."""` docstring style from test_gene_alignment.py. Imports:
```python
from __future__ import annotations
import os, pathlib
import pytest
from src.spatial.datasets import SPATIAL_DATASETS
from src.spatial.config import N_PDG
from src.spatial.gene_alignment import coverage_fraction
```
Add the network gate (the `network` marker exists in conftest.py but is UNUSED — be its first user):
```python
needs_net = pytest.mark.skipif(
    not os.environ.get("TDC_NETWORK_TESTS"),
    reason="network test; set TDC_NETWORK_TESTS=1 to enable",
)
ROOT = pathlib.Path(__file__).resolve().parents[1]
RAW = ROOT / "data" / "raw" / "spatial"
```
Implement these test functions (one behavior each, `-> None`, one-line docstring). Some assert on artifacts that later waves produce — mark those with `@needs_net` or `pytest.skip(...)` when the artifact is absent so this plan's run is GREEN offline, but the assertion is real once files exist:

  - `test_accessions_corrected() -> None` — assert no entry.accession contains "GSE189994" or "GSE144239"; assert any entry.accession contains "22321447"; assert any contains "GSE183456" and any contains "GSE183279". (DATA-01, runs offline.)
  - `test_registry_schema() -> None` — for every d in SPATIAL_DATASETS assert d.species and d.access_mechanism and d.license and d.slug are non-empty str and isinstance(d.whole_transcriptome, bool); assert all slugs are unique, lowercase, and match `^[a-z0-9_]+$`. (runs offline.)
  - `test_raw_datasets_present() -> None` — for each d where d.usable_as_input or d is a validation anchor, build the dir as `RAW / d.slug` (use `entry.slug` directly — do NOT slugify d.name here; the registry `slug` is the single source of truth shared with the Plan 02 download driver) and assert `(RAW / d.slug).exists()` with its `d.expected_files` present; `pytest.skip("downloads not present; run with TDC_NETWORK_TESTS=1")` when RAW is empty. (DATA-01.)
  - `test_whole_transcriptome_gate() -> None` — for each downloaded Visium basal dataset, read its .var_names via scanpy/anndata and assert `coverage_fraction(var_names, multidcp_10716_symbols) > 0.80` with message `f"Halt Gate 1: {name} covers only {cov:.1%} of the 10,716 space"`; skip when downloads/gene-list absent. (DATA-01 / Gate 1.)
  - `test_manifest_complete() -> None` — parse MANIFEST.md; assert it has a SHA256 + license + Whole-transcriptome column for every raw dataset and rows for both frozen checkpoints; skip when MANIFEST.md absent. (DATA-02.)
  - `test_squidpy_available() -> None` — `pytest.importorskip("squidpy")`; assert import succeeds. (DATA-02.)
  - `test_ortholog_one2one() -> None` — load the offline fixture tests/fixtures/biomart_chr21_sample.tsv, call the pure orthology filter (import `from src.spatial.orthology import build_one2one_orthologs` inside the test, skip if module absent), assert the returned table is nonempty, dropped_fraction in [0,1], and rows ≤ unique human genes. (DATA-03, runs offline once orthology.py exists.)
  - `test_coverage_report_exists() -> None` — assert (ROOT/"results"/"tables"/"P0_coverage.md").exists() and "P0_orthologs.md" exists; skip if absent. (DATA-03.)

Create the offline fixture `tests/fixtures/biomart_chr21_sample.tsv`: a small (≥5 data rows) tab-separated file with the 8 BioMart columns from 00-RESEARCH.md Pattern 2 header (ensembl_gene_id, external_gene_name, mmusculus_homolog_ensembl_gene, mmusculus_homolog_associated_gene_name, mmusculus_homolog_orthology_type, rnorvegicus_homolog_ensembl_gene, rnorvegicus_homolog_associated_gene_name, rnorvegicus_homolog_orthology_type), with a MIX of "ortholog_one2one" and "ortholog_one2many" rows so the filter has something to drop. Use real chr21 gene symbols (e.g. APP, SOD1, RUNX1) — this is reference annotation, not fabricated experimental data.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -m pytest tests/test_data_paths.py -q && conda run -n dili_v04_env python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>tests/test_data_paths.py collects and runs green offline (artifact-dependent tests skip cleanly); `test_raw_datasets_present` resolves directories via `entry.slug` (no independent slugification); the 124 existing tests/spatial/ tests still pass; tests/fixtures/biomart_chr21_sample.tsv exists with mixed orthology_type rows.</acceptance_criteria>
  <done>tests/test_data_paths.py collects and runs green offline (artifact-dependent tests skip cleanly); the 124 existing tests/spatial/ tests still pass; tests/fixtures/biomart_chr21_sample.tsv exists with mixed orthology_type rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale registry → download driver | Wrong accessions flow into side-effecting downloads if not corrected first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering (data integrity) | src/spatial/datasets.py accessions | mitigate | `test_accessions_corrected` asserts the stale GSE189994/GSE144239 cannot reappear and the verified accessions are present — a regression guard against silent accession drift |
| T-00-02 | Information disclosure | tests/fixtures + test code | accept | Fixture contains only public reference gene symbols (chr21), no PII or secrets; low/info severity |
</threat_model>

<verification>
- `conda run -n dili_v04_env python -m pytest tests/test_data_paths.py -q` green (artifact tests skip).
- `conda run -n dili_v04_env python -m pytest tests/ -q` green — 124 existing tests preserved.
- `grep -c "GSE189994\|GSE144239" src/spatial/datasets.py` returns 0.
</verification>

<success_criteria>
Registry corrected and extended (no stale accessions; six new fields populated incl. the canonical `slug`; Siletti relabeled); `tests/test_data_paths.py` exists with all named test functions and runs green offline, resolving dataset dirs via `entry.slug`; offline BioMart fixture present; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/00-dataset-acquisition-manifest/00-01-SUMMARY.md`
</output>
