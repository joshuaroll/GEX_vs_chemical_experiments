---
phase: 00-dataset-acquisition-manifest
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - MANIFEST.md
  - results/tables/P0_coverage.md
  - results/tables/P0_orthologs.md
  - scripts/report_coverage.py
autonomous: true
requirements: [DATA-02, DATA-03]
user_setup:
  - service: dataset license review
    why: "Restricted-redistribution terms (TG-GATEs / DrugMatrix / KPMP) are a human/legal judgment, not a code check"
    dashboard_config:
      - task: "Read each source's license and record an SPDX-style tag + URL in the MANIFEST.md license column before any redistribution"
        location: "each dataset's source page"

must_haves:
  truths:
    - "MANIFEST.md records path + SHA256 + license + whole-transcriptome flag for every raw dataset, plus paths+SHAs for both frozen MultiDCP/CheMoE checkpoints."
    - "squidpy is installed in dili_v04_env and the installed version + a conda env snapshot are recorded in MANIFEST."
    - "Per-Visium-dataset coverage against the 10,716-gene MultiDCP space is reported in results/tables/P0_coverage.md, each with a pass/fail vs the 0.80 whole-transcriptome gate."
    - "results/tables/P0_orthologs.md reports n_input / n_one2one / dropped_fraction."
  artifacts:
    - path: "MANIFEST.md"
      provides: "Single source of truth: data SHAs+licenses, frozen-checkpoint SHAs, env snapshot"
      contains: "Whole-transcriptome"
    - path: "results/tables/P0_coverage.md"
      provides: "Per-Visium coverage vs 10,716 space with Halt-Gate-1 pass/fail"
      contains: "coverage"
    - path: "results/tables/P0_orthologs.md"
      provides: "Ortholog dropped-fraction report"
      contains: "dropped"
  key_links:
    - from: "results/tables/P0_coverage.md"
      to: "src/spatial/gene_alignment.coverage_fraction"
      via: "coverage_fraction(visium_var_names, multidcp_10716_symbols)"
      pattern: "coverage_fraction"
    - from: "MANIFEST.md"
      to: "/raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt"
      via: "frozen-checkpoint SHA row"
      pattern: "best_model.pt"
---

<objective>
Close out Phase 0: install the one required env addition (squidpy), write the single
source-of-truth MANIFEST.md (data SHAs + licenses + whole-transcriptome flags + frozen
checkpoint SHAs + env snapshot), and produce the two results tables — per-Visium coverage
against the 10,716-gene space (the Halt-Gate-1 evidence) and the ortholog dropped-fraction
report. Depends on Plan 02 (downloaded files for SHA + coverage) and Plan 03 (the one2one
ortholog TSV + report).

Purpose: DATA-02 (MANIFEST + env) and the DATA-03 coverage half. This is the phase's
provenance and gate-evidence artifact.
Output: MANIFEST.md, results/tables/P0_coverage.md, results/tables/P0_orthologs.md,
scripts/report_coverage.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md
@.planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md

<interfaces>
<!-- Consume Plan 02 (downloads + sha256_file) and Plan 03 (ortholog TSV + report). -->

From scripts/compute_sha256.py (Plan 02): `sha256_file(path: str) -> str` streaming digest.
From src/spatial/orthology.py (Plan 03): `ortholog_report(table) -> str` markdown string;
  data/processed/spatial/orthologs_h_m_r_one2one.tsv on disk.
From src/spatial/gene_alignment.py: `coverage_fraction(source_genes, target_genes) -> float`.
From src/spatial/config.py: `N_PDG = 10716` (the coverage denominator).

Frozen checkpoints to record (verified on filesystem, 00-RESEARCH.md:309-321):
- MultiDCP-CheMoE: /raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt
- MultiDCP-PDG: under /raid/home/joshua/projects/MultiDCP/trained_models/
- MultiDCP repo SHA: 871b8de0332045a3ad5ab3a39e689014317dadfe

MANIFEST format (mirror sibling dili_downstream/MANIFEST.md; extend with License + Whole-transcriptome columns):
- "Source code (SHA-pinned)" table: | File | Source repo | Source SHA | Purpose |
- "Data files (SHA256-pinned)" table: | File | Path | SHA256 | Source | License | Whole-transcriptome | Notes |
- "Environment" row: conda env export snapshot @date + installed squidpy version.

10,716-gene MultiDCP symbol list (00-RESEARCH.md Open Question 3): locate + pin the canonical
file (geneinfo_beta.txt / pdg_de_genes_*.pkl); cross-check count == 10,716.

HGNC alias normalization (Pitfall 4, 00-RESEARCH.md:257-261): normalize alias->approved symbols
before intersecting Visium .var_names with the 10,716 list, or coverage is understated (~95.5% vs ~100%).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install squidpy + write coverage report (scripts/report_coverage.py + P0_coverage.md)</name>
  <read_first>
    - src/spatial/gene_alignment.py (coverage_fraction — the coverage primitive)
    - src/spatial/config.py (N_PDG = 10716)
    - src/spatial/datasets.py (corrected registry — which entries are Visium/whole_transcriptome basal)
    - .planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md (Pattern 3 lines 194-204 the gate; Pitfall 4 lines 257-261 alias normalization; Open Question 3 the 10,716 file)
  </read_first>
  <action>
Install the required env addition (DATA-02): `conda run -n dili_v04_env pip install squidpy`. Record the EXACT installed version (`conda run -n dili_v04_env python -c "import squidpy; print(squidpy.__version__)"`) — pin the actually-installed version, not a guess. Do NOT install tangram-sc (out of scope for P0).

Locate and pin the canonical 10,716-gene MultiDCP symbol list (00-RESEARCH.md Open Question 3): check geneinfo_beta.txt / pdg_de_genes_*.pkl in the MultiDCP/PDGrapher repo; assert `len(symbols) == 10716` (N_PDG). Record the chosen file path in MANIFEST.

Create `scripts/report_coverage.py`: for each Visium basal dataset on disk under data/raw/spatial/, read its `.var_names` via `scanpy.read_visium` / `scanpy.read_10x_h5` / `anndata.read_h5ad` (pick by file type). Apply HGNC alias normalization (alias->approved) before intersection so the ~44 obsolete L1000 aliases don't understate coverage (Pitfall 4). Compute `coverage_fraction(normalized_var_names, multidcp_10716_symbols)`. Write `results/tables/P0_coverage.md` as a markdown table: one row per dataset with `| dataset | platform | n_genes | coverage | gate (PASS if >0.80) |`. If ANY whole_transcriptome basal dataset has coverage <= 0.80, write `.planning/phases/00-dataset-acquisition-manifest/HALT_REASON.md` ("Halt Gate 1: <name> not whole-transcriptome, coverage X%") — the panels/annotation-only entries (whole_transcriptome=False) are expected to fail and are NOT gate violations; only basal-input Visium datasets gate.

Run it: `conda run -n dili_v04_env python scripts/report_coverage.py`.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -c "import squidpy; print(squidpy.__version__)" && conda run -n dili_v04_env python -m pytest tests/test_data_paths.py::test_squidpy_available tests/test_data_paths.py::test_coverage_report_exists -x -q && grep -qi "coverage" results/tables/P0_coverage.md</automated>
  </verify>
  <done>squidpy importable in dili_v04_env (version captured); results/tables/P0_coverage.md exists with per-Visium coverage rows and a PASS/FAIL gate column; basal Visium datasets show coverage > 0.80 (or HALT_REASON.md written for any that don't).</done>
</task>

<task type="auto">
  <name>Task 2: Write MANIFEST.md + P0_orthologs.md and run the phase gate</name>
  <read_first>
    - /raid/home/joshua/projects/GEX_vs_chemical_experiments/dili_downstream/MANIFEST.md (sibling format to mirror)
    - scripts/compute_sha256.py (Plan 02 — sha256_file helper)
    - src/spatial/orthology.py (Plan 03 — ortholog_report)
    - .planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md (section "MANIFEST.md (NEW)" — exact tables + env row + XC-10)
    - .planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md (lines 304-321 MANIFEST skeleton + frozen-checkpoint note)
  </read_first>
  <action>
Create `MANIFEST.md` at the spatial_tests root, mirroring the sibling dili_downstream/MANIFEST.md:
  - Header + update-discipline line ("single source of truth ... update at the end of every phase; tests/test_data_paths.py reads from this implicitly").
  - "Source code (SHA-pinned)" table with both frozen checkpoints: MultiDCP-CheMoE at /raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt (compute its SHA via sha256_file), MultiDCP-PDG under /raid/home/joshua/projects/MultiDCP/trained_models/, MultiDCP repo SHA 871b8de0332045a3ad5ab3a39e689014317dadfe.
  - "Data files (SHA256-pinned)" table: | File | Path | SHA256 | Source | License | Whole-transcriptome | Notes |. One row per downloaded raw dataset (run scripts/compute_sha256.py over data/raw/spatial/ and data/raw/labels/ to fill SHA256). License column mandatory — Yu = "GPL-3.0+"; fill each from its source (mark "see source / review pending" where a human license review is still required, per the user_setup gate).
  - Row for data/processed/spatial/orthologs_h_m_r_one2one.tsv with its SHA + Ensembl release + query date (XC-10 time-leakage discipline).
  - "Environment" section: `conda run -n dili_v04_env conda env export > MANIFEST_env_snapshot.yml` and record the snapshot path + the installed squidpy version + date.

Create `results/tables/P0_orthologs.md` by writing the `ortholog_report(table)` output (n_input / n_one2one / dropped_fraction as a percentage) — load data/processed/spatial/orthologs_h_m_r_one2one.tsv and the cached raw TSV to recompute counts, or capture the printed report from Plan 03's build_orthologs.py run.

Phase gate: run the FULL suite `conda run -n dili_v04_env python -m pytest tests/ -q` — all of test_manifest_complete, test_whole_transcriptome_gate, test_raw_datasets_present, test_ortholog_one2one, test_coverage_report_exists must now pass (no longer skip), AND the 124 existing tests stay green.
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -m pytest tests/ -q && grep -q "Whole-transcriptome" MANIFEST.md && grep -q "best_model.pt" MANIFEST.md && grep -qi "dropped" results/tables/P0_orthologs.md</automated>
  </verify>
  <done>MANIFEST.md has SHA256+license+whole-transcriptome rows for every raw dataset and both frozen-checkpoint rows + env snapshot with squidpy version; results/tables/P0_orthologs.md reports the dropped fraction; full test suite (incl. 124 existing) is green with the previously-skipped data-path tests now passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded files -> MANIFEST provenance | SHA-256 pins integrity; license column gates redistribution |
| frozen checkpoint files -> MANIFEST | path+SHA recorded but NOT loaded in P0 (region_signature.py seam stays NotImplementedError per XC-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-10 | Tampering (data integrity) | every raw dataset + checkpoint | mitigate | SHA-256 pinned per file in MANIFEST so later phases can detect any post-download modification or substitution |
| T-00-11 | Information disclosure / license compliance | TG-GATEs / DrugMatrix / KPMP restricted terms | mitigate | mandatory License column in MANIFEST + the human license-review user_setup gate before any redistribution |
| T-00-12 | Elevation of privilege | conda env export / installed packages | accept | squidpy installed from PyPI into an existing env; version pinned in MANIFEST; standard supply-chain trust, low/info severity for a research env |
</threat_model>

<verification>
- `conda run -n dili_v04_env python -m pytest tests/ -q` fully green (data-path tests no longer skip; 124 existing preserved).
- `grep -q "Whole-transcriptome" MANIFEST.md` and `grep -q "best_model.pt" MANIFEST.md` succeed.
- `results/tables/P0_coverage.md` and `results/tables/P0_orthologs.md` both populated.
</verification>

<success_criteria>
squidpy installed + version recorded; MANIFEST.md complete (data SHAs/licenses/whole-transcriptome flags, both frozen-checkpoint SHAs, env snapshot); P0_coverage.md shows basal Visium > 0.80 gate pass; P0_orthologs.md reports dropped fraction; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/00-dataset-acquisition-manifest/00-04-SUMMARY.md`
</output>
