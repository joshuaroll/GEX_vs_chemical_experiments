---
phase: 00-dataset-acquisition-manifest
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/download_spatial.py
  - scripts/download_labels.py
  - scripts/compute_sha256.py
  - data/raw/spatial/  # dataset subdirs only; biomart/ is owned by Plan 03 (disjoint)
autonomous: true
requirements: [DATA-01]
user_setup:
  - service: KPMP atlas (kidney)
    why: "atlas.kpmp.org may require a click-through Terms-of-Service acceptance that has no API"
    dashboard_config:
      - task: "If GEO supplementary (GSE183456/GSE183279, no auth) fails, accept KPMP ToS at https://atlas.kpmp.org and download the Visium objects manually into data/raw/spatial/lake_kpmp_kidney/"
        location: "https://atlas.kpmp.org"

must_haves:
  truths:
    - "Every planned input dataset is on disk under data/raw/spatial/<entry.slug>/ with its expected_files present."
    - "The download driver reads the corrected registry and dispatches per access_mechanism (no hardcoded stale accessions), resolving each directory from entry.slug (the single source of truth shared with the data-path test — no independent slugification)."
    - "After downloads run, the driver asserts each dataset's expected_files are on disk; a partial/failed download is caught in-task, not deferred to Plan 04."
    - "If any planned dataset is unavailable (404 / empty), the driver writes HALT_REASON.md and stops (Halt Gate 1) — an ACCEPTED terminal outcome for Phase 0."
    - "A SHA-256 helper produces per-file digests for the MANIFEST."
  artifacts:
    - path: "scripts/download_spatial.py"
      provides: "Registry-driven download driver dispatching on access_mechanism, resolving dirs via entry.slug"
      exports: ["main"]
    - path: "scripts/compute_sha256.py"
      provides: "Streaming SHA-256 helper for MANIFEST data rows"
      contains: "hashlib.sha256"
    - path: "scripts/download_labels.py"
      provides: "Per-organ toxicity label-set fetcher (DILIst/DILIrank liver; DIRIL kidney; SIDER brain SOC only)"
      exports: ["main"]
  key_links:
    - from: "scripts/download_spatial.py"
      to: "src/spatial/datasets.py"
      via: "imports SPATIAL_DATASETS; dispatches on access_mechanism; dirs from entry.slug"
      pattern: "from src.spatial.datasets import SPATIAL_DATASETS"
    - from: "scripts/download_spatial.py"
      to: ".planning/phases/00-dataset-acquisition-manifest/HALT_REASON.md"
      via: "on unavailable/empty dataset, write HALT_REASON.md and exit nonzero"
      pattern: "HALT_REASON"
---

<objective>
Acquire every planned input dataset onto disk under data/raw/spatial/, driven by the
corrected registry from Plan 01. All side-effecting I/O lives in scripts/ per the
project's pure-library mandate — src/spatial/ is never touched here. The driver
dispatches on each dataset's `access_mechanism`, resolves its directory from
`entry.slug` (the single source of truth shared with the Plan 01 data-path test), and,
on any unavailable or empty dataset, writes HALT_REASON.md and stops (Halt Gate 1).

Purpose: Get the right bytes on disk with provenance hooks (SHA helper) so Plan 04 can
build MANIFEST.md and the coverage report.
Output: downloaded datasets under data/raw/spatial/, three scripts, and (on failure
only) a HALT_REASON.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md
@.planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md

<interfaces>
<!-- Plan 01 produced the corrected registry. Consume it; do not re-correct. -->

From src/spatial/datasets.py (after Plan 01 — corrected + extended):
```python
class SpatialDataset(NamedTuple):
    name: str; organ: str; species: str; platform: str; accession: str
    access_mechanism: str          # "figshare_api"|"geo_supp"|"spatialLIBD"|"kpmp"|"url"
    expected_files: tuple[str, ...]
    license: str; whole_transcriptome: bool
    slug: str                      # canonical data/raw/spatial/<slug> dir — USE THIS, never re-slugify name
    usable_as_input: bool
    region_annotation_source: str
SPATIAL_DATASETS: Final[list[SpatialDataset]]
```

SHA-256 streaming idiom (lift from region_signature.py:218 hashlib usage):
```python
import hashlib
def sha256_file(path: str) -> str:
    h = hashlib.sha256()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(1 << 20), b""):
            h.update(chunk)
    return h.hexdigest()
```

Verified accessions / download specifics (from 00-RESEARCH.md lines 276-302):
- Yu liver: figshare article 22321447 via Figshare API (api.figshare.com/v2/articles/22321447 -> file download URLs). Files L5_upload.zip (md5 3abf91538674c3b0cbc3f55b9c5b6074), L18_upload.zip (md5 31bbf855afd7e3ab26f65f8786575a0d).
- Lake/KPMP kidney: GSE183456 + GSE183279 GEO supplementary HTTPS (no auth); KPMP portal fallback.
- Maynard brain: spatialLIBD / Bioconductor (spatial.libd.org) or LieberInstitute GitHub.
- APAP validation (mouse liver): GSE280652, GSE272564 GEO supplementary.
- Andrews/Teichmann liver backup: GSE185477.
- Label sets: DILIst/DILIrank (FDA Excel, direct URLs in sibling dili_downstream/MANIFEST.md); DIRIL (Connor 2024 supplement); SIDER (sideeffects.embl.de, HTTP 200).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHA helper + registry-driven spatial download driver</name>
  <read_first>
    - src/spatial/datasets.py (the corrected registry from Plan 01 — read expected_files/access_mechanism/slug per entry)
    - src/spatial/region_signature.py (lines 211-218 — the hashlib idiom to lift)
    - .planning/phases/00-dataset-acquisition-manifest/00-PATTERNS.md (section "scripts/download_*.py ... (NEW)")
    - .planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md (lines 146-148 Pattern 1; lines 276-302 verified accessions)
  </read_first>
  <action>
Create `scripts/compute_sha256.py` with the streaming `sha256_file(path) -> str` helper (verbatim from the interfaces block) and a `main()` that, given a directory or file list, prints `<sha256>  <relpath>` lines (mirrors the MANIFEST data-row format). No torch import.

Create `scripts/download_spatial.py`: a driver that imports `SPATIAL_DATASETS` from `src.spatial.datasets`, iterates entries that are `usable_as_input` or validation anchors, and dispatches on `access_mechanism`. For EVERY entry, resolve the target directory as `data/raw/spatial/{entry.slug}` — read `entry.slug` from the registry; do NOT slugify `entry.name` or compute the directory name any other way. `entry.slug` is the single source of truth, shared with `tests/test_data_paths.py::test_raw_datasets_present` (Plan 01), so a rename can never desync the driver and the test.
  - `"figshare_api"`: GET `https://api.figshare.com/v2/articles/22321447`, parse the `files` list for `download_url`, stream each `expected_files` item to `data/raw/spatial/{entry.slug}/` (= `yu2022_liver`). Compare the figshare-provided md5 against the downloaded file (warn-and-record on mismatch; never silently pass).
  - `"geo_supp"`: build the GEO supplementary HTTPS URL for the accession (e.g. `https://ftp.ncbi.nlm.nih.gov/geo/series/GSE183nnn/GSE183456/suppl/`) and stream supplementary files into `data/raw/spatial/{entry.slug}/`.
  - `"spatialLIBD"`: fetch the Maynard DLPFC objects from spatialLIBD/Bioconductor or LieberInstitute GitHub into `data/raw/spatial/{entry.slug}/` (= `maynard_dlpfc`).
  - `"kpmp"`: attempt GEO supplementary first (no auth); if that 404s, do NOT crash — print that the KPMP portal ToS click-through (user_setup) is required and continue.
  - `"url"`: stream the plain URL.
Use `requests` with `stream=True` and a 1 MB chunk write loop; `pathlib.Path(...).mkdir(parents=True, exist_ok=True)`. The slugs come from the registry (`yu2022_liver`, `lake_kpmp_kidney`, `maynard_dlpfc`, `gse280652_apap_liver`, `gse272564_apap_liver`, `andrews_liver`, plus rodent slugs) — never hardcode a slug that disagrees with `entry.slug`.

HALT GATE 1: if a fetch returns HTTP 404 / a zero-byte file / an HTML error page (Content-Type text/html where a binary is expected), write `.planning/phases/00-dataset-acquisition-manifest/HALT_REASON.md` stating which dataset failed, the URL tried, and the rationale ("Halt Gate 1: planned input dataset unavailable"), then `sys.exit(1)`. Never fabricate or silently substitute data (XC-01).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -c "import ast; ast.parse(open('scripts/download_spatial.py').read()); ast.parse(open('scripts/compute_sha256.py').read()); print('ok')" && grep -q "from src.spatial.datasets import SPATIAL_DATASETS" scripts/download_spatial.py && grep -q "\.slug" scripts/download_spatial.py && grep -q "HALT_REASON" scripts/download_spatial.py && grep -q "hashlib.sha256" scripts/compute_sha256.py</automated>
  </verify>
  <acceptance_criteria>scripts/compute_sha256.py and scripts/download_spatial.py parse cleanly; the driver imports SPATIAL_DATASETS, dispatches on access_mechanism, resolves every target directory from `entry.slug` (grep finds `.slug` usage; no independent slugification of `name`), and contains the HALT_REASON.md path + sys.exit(1) gate.</acceptance_criteria>
  <done>scripts/compute_sha256.py and scripts/download_spatial.py parse cleanly; the driver imports SPATIAL_DATASETS, dispatches on access_mechanism, resolves dirs via entry.slug, and contains the HALT_REASON.md path + sys.exit(1) gate.</done>
</task>

<task type="auto">
  <name>Task 2: Per-organ toxicity label-set fetcher + run the spatial downloads (with expected_files assertion)</name>
  <read_first>
    - scripts/download_spatial.py (from Task 1 — reuse its streaming helpers + entry.slug dir resolution)
    - /raid/home/joshua/projects/GEX_vs_chemical_experiments/dili_downstream/MANIFEST.md (sibling — DILIst/DILIrank direct URLs + SHAs)
    - .planning/phases/00-dataset-acquisition-manifest/00-RESEARCH.md (Open Question 4 RESOLVED — label-set scope decision; lines 364-368)
  </read_first>
  <action>
**DECISION (record this in the SUMMARY and honor it here):** P0 brain toxicity labels = SIDER nervous-system SOC (serious terms) ONLY. Lane-Ekins seizure and DNT-IVB are DEFERRED to Phase 1 because brain is the last organ sequenced (liver → kidney → brain). DIRIL (kidney) supplement URL is resolved at download time; if unresolved, write a TODO line into the HALT_REASON candidates note — do NOT fabricate labels. This converts the previously-silent brain-label scope reduction into a recorded decision; DATA-01 coverage stays intact (SIDER brain labels still acquired).

Create `scripts/download_labels.py` that fetches the per-organ toxicity label sets into `data/raw/labels/<set>/`:
  - liver: DILIst + DILIrank FDA Excel (reuse the direct URLs + expected SHAs from the sibling dili_downstream/MANIFEST.md).
  - kidney: DIRIL (Connor 2024 Drug Discov Today supplement) — resolve the supplement URL; if unresolved, write a TODO line into HALT_REASON.md candidates note (do not fabricate labels).
  - brain: SIDER (sideeffects.embl.de) nervous-system SOC (serious terms) ONLY — per the DECISION above; seizure/DNT-IVB are explicitly NOT fetched in P0.
  - heart (DICTrank) is DEFERRED per ROADMAP — skip with a logged note, not a download.
Reuse the same streaming + HALT-GATE-1-on-404 discipline from Task 1. If any required (non-deferred) label set is unavailable, write HALT_REASON.md and exit nonzero (XC-01 stop-and-ask).

Then RUN the spatial + label downloads against the live network: `TDC_NETWORK_TESTS=1 conda run -n dili_v04_env python scripts/download_spatial.py` and `... python scripts/download_labels.py`.

POST-DOWNLOAD EXPECTED-FILES ASSERTION (deterministic in-task success check): after the downloads complete, iterate `SPATIAL_DATASETS` for every entry that was fetched (usable_as_input or validation anchor) and assert that EACH filename in `entry.expected_files` exists and is non-zero-byte under `data/raw/spatial/{entry.slug}/`. If any expected file is missing or zero-byte, this is a partial/failed download: write HALT_REASON.md (naming the dataset, slug, and the missing files) and `sys.exit(1)` — do not let it slip through to Plan 04. This makes "download succeeded" a per-entry, on-disk fact rather than a no-exception assumption.

Finally run `conda run -n dili_v04_env python -m pytest tests/test_data_paths.py::test_raw_datasets_present -x -q` — it must now find the files (no longer skip). If any dataset 404s and triggers HALT_REASON.md, STOP and surface the halt (do not proceed to Plan 04).
  </action>
  <verify>
    <automated>conda run -n dili_v04_env python -c "import ast; ast.parse(open('scripts/download_labels.py').read()); print('ok')" && grep -q "HALT_REASON" scripts/download_labels.py && grep -q "expected_files" scripts/download_spatial.py && test -d data/raw/spatial</automated>
  </verify>
  <acceptance_criteria>scripts/download_labels.py parses and carries the HALT_REASON gate; the brain-label-scope DECISION (SIDER SOC only; seizure/DNT-IVB deferred) is honored (no seizure/DNT-IVB fetch in P0); the post-download expected_files assertion is present and enforced (grep finds `expected_files` in the driver); the spatial + label downloads have been executed; data/raw/spatial/<entry.slug>/ subdirs contain every expected_files entry (non-zero-byte) OR a HALT_REASON.md exists documenting the unavailable/partial dataset and execution stopped.</acceptance_criteria>
  <done>scripts/download_labels.py parses and carries the HALT_REASON gate; brain labels = SIDER SOC only (seizure/DNT-IVB deferred to P1, recorded); the post-download expected_files assertion catches partial downloads in-task; the spatial + label downloads have been executed; data/raw/spatial/<entry.slug>/ subdirs contain expected_files OR a HALT_REASON.md exists documenting the unavailable dataset and execution stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote registry (figshare/GEO/KPMP/SIDER) -> local disk | Untrusted bytes cross here; downloaded files are data, parsed never executed |
| corrected registry -> download driver | Accession integrity must carry through to the actual fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | Tampering (data integrity) | downloaded dataset bytes | mitigate | figshare-provided md5 compared on download; SHA-256 computed for every file (recorded in MANIFEST by Plan 04) so corruption/substitution is detectable |
| T-00-04 | Elevation of privilege (untrusted content execution) | downloaded .zip/.h5/.xlsx files | mitigate | files are only read by scanpy/h5py/pandas as data; the driver never executes, eval()s, or unpickles downloaded content; .zip extraction (if any) targets a fixed data/raw subdir, no path traversal |
| T-00-05 | Denial of service / wrong-data | unavailable, 404, or partial dataset | mitigate | HALT_REASON.md + sys.exit(1) on 404/empty/HTML AND the post-download expected_files assertion (partial download = missing/zero-byte expected file) rather than silently writing a corrupt/empty file (Halt Gate 1) |
| T-00-06 | Information disclosure / license | TG-GATEs / DrugMatrix / KPMP restricted terms | accept | public sources; license recorded in MANIFEST (Plan 04); restricted-redistribution review is the manual gate in 00-VALIDATION.md |
</threat_model>

<verification>
- Both scripts parse (`python -c "import ast; ..."`).
- `grep -q "from src.spatial.datasets import SPATIAL_DATASETS" scripts/download_spatial.py` succeeds.
- `grep -q "\.slug" scripts/download_spatial.py` and `grep -q "expected_files" scripts/download_spatial.py` succeed (entry.slug dir resolution + post-download assertion present).
- After running downloads: `conda run -n dili_v04_env python -m pytest tests/test_data_paths.py::test_raw_datasets_present -x -q` passes (files found), OR a HALT_REASON.md exists and execution stopped.
</verification>

<success_criteria>
All planned datasets on disk under data/raw/spatial/<entry.slug>/ (and labels under data/raw/labels/) with every expected_files entry present and non-zero-byte, driven by the corrected registry and resolved via `entry.slug`; SHA helper present; Halt-Gate-1 path wired and honored (404/empty/HTML and partial-download cases); brain-label scope DECISION recorded (SIDER SOC only); no fabricated or substituted data.

Writing HALT_REASON.md and stopping (Halt Gate 1 fired) is an ACCEPTED terminal outcome for Phase 0 — the downstream verifier must treat a legitimately-halted phase as gate-fired, not as plan failure.
</success_criteria>

<output>
After completion, create `.planning/phases/00-dataset-acquisition-manifest/00-02-SUMMARY.md`
</output>
