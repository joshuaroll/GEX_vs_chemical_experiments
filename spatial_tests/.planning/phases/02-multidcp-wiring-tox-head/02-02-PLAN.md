---
phase: 02-multidcp-wiring-tox-head
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/spatial/region_signature.py
  - configs/liver_p2.yaml
  - scripts/cache_region_de.py
autonomous: true
requirements: [WIRE-01]

must_haves:
  truths:
    - "The region_signature.py NotImplementedError seams (load_model, _call_model) are replaced with a real strict-load of MultiDCP_CheMoE_AE from the D-04 row-17 checkpoint /raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt (0 missing/0 unexpected keys), and a real forward returning absolute predicted treated over 10,716 genes."
    - "Spatial DE is rule B (D-02): de = predicted_treated(drug) - predicted_control(region_basal), where control = an inert/empty-drug control forward pass in the same region basal context (D-02 amendment); the model emits absolute treated (research-confirmed), so there is no double-subtract."
    - "The cache stores three vectors per (pert_id, region) — de_array, treated_array, control_array (D-03) — and the manifest de_convention reads 'predicted_treated(drug) - predicted_control(region_basal)'."
    - "The 10,716-gene PDG space (D-01) is the model I/O and cache space; N_PDG=10716 is added without breaking the 978 N_LANDMARK callers; the region basal is 0-1 min-max normalized to the manifold range BEFORE the forward pass (Pitfall 1)."
    - "Per-region predicted DE is cached for liver in BOTH human and mouse (D-01 scope: human for the P4 headline, mouse for the WIRE-03 APAP gate)."
    - "S-B is DESCOPED this phase (D-04 amendment): the row-18 chemoe_kpgt checkpoint is collapsed/incompatible and is NOT wired; it is recorded as a checkpoint-provenance blocker for a later phase. WIRE-01 is satisfied by S-C alone."
  artifacts:
    - path: "src/spatial/region_signature.py"
      provides: "filled load_model + _call_model (MultiDCP_CheMoE_AE, row-17 ckpt), rule-B compute_de, 3-vector RegionSignatureCache (D-03), N_PDG=10716 (D-01), updated de_convention"
      contains: "predicted_control"
    - path: "configs/liver_p2.yaml"
      provides: "checkpoint path + SHA, gene-order file + SHA, fixed 2-dim dose one-hot, basal-normalization params, control-input definition (D-02), cache output dir"
      contains: "best_model.pt"
    - path: "scripts/cache_region_de.py"
      provides: "CUDA-hygiene driver (--gpu before import torch) that loads the frozen backbone and writes the 3-vector liver cache for human + mouse"
      contains: "CUDA_VISIBLE_DEVICES"
  key_links:
    - from: "scripts/cache_region_de.py"
      to: "src.spatial.region_signature.RegionSignatureCacher.load_model + .run"
      via: "load row-17 best_model.pt, run over liver drugs x zones for human then mouse, write npy + manifest JSON"
      pattern: "best_model.pt"
    - from: "src.spatial.region_signature._call_model"
      to: "/raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/models/multidcp_chemoe_pdg.py MultiDCP_CheMoE_AE.forward"
      via: "sys.path inject + featurize_drug + 0-1 normalized 10716 basal + fixed 2-dim dose, no_grad"
      pattern: "input_cell_gex"
    - from: "src.spatial.region_signature.compute_de"
      to: "RegionSignatureCache.de_array (rule B)"
      via: "de = predicted_treated - predicted_control (D-02), cached alongside treated_array/control_array (D-03)"
      pattern: "predicted_control"
---

<objective>
WIRE-01 — replace the two `NotImplementedError` seams in
`src/spatial/region_signature.py` with the real frozen MultiDCP-CheMoE forward
path (D-04 amended: `MultiDCP_CheMoE_AE` + row-17 `best_model.pt`), switch the
spatial DE rule from rule A to rule B (D-02: `predicted_treated(drug) -
predicted_control(region_basal)` with an inert-drug control pass), extend the
cache to the three-vector schema (D-03), generalize the gene space to 10,716
(D-01), and cache per-region predicted DE for liver in BOTH human and mouse.

Purpose: produce the cached, auditable per-region predicted DE signature that the
tox head (02-03) and the APAP gate (02-04) consume — the core of the end-to-end
forward path. The control-pass vectors are cached so the rule-B choice is
re-derivable (D-02/D-03).

Output: filled `region_signature.py` (load_model, _call_model, rule-B compute_de,
3-vector cache, N_PDG); new `configs/liver_p2.yaml`; new
`scripts/cache_region_de.py` with CUDA hygiene; the liver 3-vector cache on disk
for human + mouse.

**Scope boundary:** Wire ONLY S-C (MultiDCP-CheMoE, row-17). Do NOT wire the
row-18 `chemoe_kpgt_…` checkpoint (D-04 amendment: collapsed/incompatible — record
the S-B descope as a provenance blocker in the SUMMARY, do not implement it).
Do NOT modify any test file (Wave 0 owns them) — this plan flips them GREEN by
editing source only. No model retraining (frozen baseline). No splits/training/APAP
(later plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-multidcp-wiring-tox-head/02-CONTEXT.md
@.planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md
@.planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md

# The file being MODIFIED + the driver analog:
@src/spatial/region_signature.py
@scripts/run_p1_eda.py
@src/spatial/gene_alignment.py

<interfaces>
<!-- VERIFIED contracts (02-RESEARCH.md Code Examples; strict-load tested). Use directly. -->

# Cross-project model home (read-only static import; sys.path inject inside impure methods only):
MDCP_SRC = "/raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src"
#   src/models/multidcp_chemoe_pdg.py : class MultiDCP_CheMoE_AE (alias MultiDCP_CheMoEBase)
#   src/utils/multidcp_ae_pdg_utils.py : initialize_model_registry()  -> cell_id_input_dim=10716, num_gene=10716
#   src/utils/  : Molecules / convert_smile_to_feature (SMILES -> atom62/bond6 drug graph dict)

# load_model body (VERIFIED strict 0/0):
#   reg = initialize_model_registry(); reg.update({"num_gene":10716,"pert_idose_input_dim":2,"dropout":0.3,"linear_encoder_flag":False})
#   model = MultiDCP_CheMoE_AE(device=self.device, model_param_registry=reg).double()
#   state = torch.load(ckpt, map_location=self.device, weights_only=False); model.load_state_dict(state, strict=True)
#   model.eval(); for p in model.parameters(): p.requires_grad_(False)

# _call_model forward (VERIFIED signature; returns [batch, 10716] ABSOLUTE treated):
#   pred, _cell_hidden = self._model(input_cell_gex=basal[1,10716 float64, 0-1 normalized],
#                                    input_drug=drug, input_gene=self._gene_tensor, mask=mask,
#                                    input_pert_idose=dose[[1.0,0.0] float64], job_id="perturbed", epoch=0)
#   return pred.squeeze(0).float().cpu().numpy()   # [10716]

# Checkpoint (D-04 row-17, SHA fbee15f...): /raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt
# Gene order (P0): data/processed/spatial/multidcp_10716_symbols.txt  (10,716 human symbols, the cache column order)
# Manifold basal range (normalization target): ~[0.018, 1.000], mean 0.62 (Pitfall 1)

# Existing PURE functions to KEEP import-clean (no torch): make_cache_key, build_manifest, compute_de, assemble_cache
# gene_alignment.align_to_gene_space(profile, source_ids, target_ids, missing=...) -> aligned (len target) array
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule-B DE + 3-vector cache + N_PDG in region_signature.py (GREEN the Wave-0 pure tests)</name>
  <read_first>
    - src/spatial/region_signature.py (the file being modified: compute_de lines 297-356 rule A; RegionSignatureManifest de_convention line 285; RegionSignatureCache lines 155-169 de_array-only; assemble_cache lines 364-449; N_LANDMARK line 67)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (region_signature MODIFY, changes 1-4: N_PDG, rule A->B, de_convention string, 3-vector cache — exact line refs)
    - tests/spatial/test_region_signature.py (the RED tests from 02-01 this task must turn GREEN: rule_b, cache_three_vector, n_pdg)
  </read_first>
  <behavior>
    - test_rule_b_de_subtracts_control: compute_de(treated, control) == treated - control (float32).
    - manifest.de_convention == "predicted_treated(drug) - predicted_control(region_basal)".
    - test_cache_three_vector_schema: RegionSignatureCache has treated_array, control_array, de_array,
      each (n_pert_ids, n_regions, n_genes) float32; de_array == treated_array - control_array.
    - test_n_pdg_constant: N_PDG == 10716; N_LANDMARK still == 978 (no 978-caller regression).
  </behavior>
  <action>
    Edit `src/spatial/region_signature.py` (pure portions only; keep torch-free):

    1. Add `N_PDG: Final[int] = 10716` alongside `N_LANDMARK` (line ~67) with a one-line docstring
       (D-01: PDG/CheMoE native output + cache space). Relax the constructor docstring (line ~487)
       so a 10,716-length `gene_ids` tuple is accepted (it already is — no hard 978 assertion exists;
       just update the prose to name both spaces).

    2. Rule A -> rule B in `compute_de` (D-02). Rename the 2nd parameter `region_basal` -> `predicted_control`,
       keep the IDENTICAL shape-check structure (copy the ndim/length guards verbatim, rename `rb`->`pc`
       and the error strings), and return `pt - pc`. Update the docstring DE convention to rule B and the
       module header docstring (lines 8-9, 18-27) to state rule B + the D-02 inert-control definition.

    3. Update the manifest `de_convention` (line 285) to
       `"predicted_treated(drug) - predicted_control(region_basal)"`.

    4. Extend `RegionSignatureCache` (lines 155-169) with `treated_array` and `control_array`
       (same (n_pert_ids, n_regions, n_genes) float32 shape). Update `assemble_cache` (lines 364-449)
       to take a `predicted_control_map: dict[(pert_id, region) | region, np.ndarray]` in place of
       `region_basal_map` (control is per-region; key by region), allocate all three arrays, and fill
       `treated_array[i,j]=pred`, `control_array[i,j]=pred_control`, `de_array[i,j]=compute_de(pred, pred_control)`.
       Keep the exact double-loop + KeyError/ValueError guards. Update `RegionDE` docstring to rule B.

    Do NOT touch load_model/_call_model in this task (Task 2). Keep `make_cache_key`/`build_manifest`
    torch-free. Run the Wave-0 pure tests to confirm RED -> GREEN.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import sys; s=open('src/spatial/region_signature.py').read(); ok=('N_PDG' in s) and ('predicted_control' in s) and ('predicted_treated(drug) - predicted_control(region_basal)' in s) and ('treated_array' in s) and ('control_array' in s); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python -m pytest tests/spatial/test_region_signature.py -q -m "not gpu"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'N_PDG' src/spatial/region_signature.py` >= 1 and `grep -c 'N_LANDMARK' src/spatial/region_signature.py` >= 1 (both spaces; 978 not removed).
    - `grep -c 'predicted_control' src/spatial/region_signature.py` >= 3 (param + cache + manifest convention).
    - `grep -c 'treated_array' src/spatial/region_signature.py` >= 2 and `grep -c 'control_array' src/spatial/region_signature.py` >= 2.
    - `grep -c 'predicted_treated(drug) - predicted_control(region_basal)' src/spatial/region_signature.py` >= 1.
    - `pytest tests/spatial/test_region_signature.py -m "not gpu"` exits 0 (all pure tests GREEN, including the formerly-RED rule_b/cache_three_vector/n_pdg).
  </acceptance_criteria>
  <done>region_signature.py implements rule-B compute_de (treated - predicted_control), the 3-vector cache (D-03), N_PDG=10716 (D-01) without breaking 978 callers, and the updated de_convention; the Wave-0 pure tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Fill load_model + _call_model (MultiDCP_CheMoE_AE, row-17 ckpt) + featurize_drug + control pass + config</name>
  <read_first>
    - src/spatial/region_signature.py (the seams: load_model lines 512-557, _call_model lines 563-608; the run() loop lines 614-680 that must now also build the per-region control map)
    - .planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md (Code Examples: load_model VERIFIED strict-load body; _call_model VERIFIED forward; Pitfall 1 0-1 normalization; Pitfall 2 float64; Pitfall 4 2-dim dose; Cross-project import landmine — sys.path inside impure methods only)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (region_signature changes 5-6: load_model + _call_model verbatim bodies; configs/liver_p2.yaml content from run_p1_eda.py path block)
    - .planning/phases/02-multidcp-wiring-tox-head/02-CONTEXT.md (D-02 amendment: control = inert/empty-drug control pass in the same region basal context; D-05 fixed 2-dim dose)
  </read_first>
  <action>
    Implement the impure model path in `src/spatial/region_signature.py` (these methods only;
    keep all pure functions torch-free per the Cross-project import landmine):

    1. `load_model(self, checkpoint_path)` — replace the NotImplementedError (lines 550-557) with the
       VERIFIED body (02-RESEARCH Code Examples / 02-PATTERNS change 5). Inside the method:
       `import os, sys, torch`; `MDCP_SRC = "/raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src"`;
       `sys.path.insert(0, os.path.join(MDCP_SRC,"models"))` and `.../utils"))`;
       `from multidcp_ae_pdg_utils import initialize_model_registry`; `import multidcp_chemoe_pdg as mc`;
       build the registry with `{"num_gene":10716,"pert_idose_input_dim":2,"dropout":0.3,"linear_encoder_flag":False}`;
       `model = mc.MultiDCP_CheMoE_AE(device=self.device, model_param_registry=reg).double()`;
       `state = torch.load(checkpoint_path, map_location=self.device, weights_only=False)`;
       `model.load_state_dict(state, strict=True)` (expect 0/0; if it raises, FAIL loudly — do not fall back);
       `model.eval(); for p in model.parameters(): p.requires_grad_(False); self._model = model`.
       Add `self.device` to `__init__` (default auto-detect deferred to the driver; accept a `device="cpu"`
       kwarg). SHA-assert: before load, assert the checkpoint path exists and (optionally) matches the
       MANIFEST SHA `fbee15f…` (Security: the kpgt-vs-CheMoE mixup risk). Replace the stale TODO docstring
       (lines 515-534) pointing at dili_downstream/trained_models — use the row-17 path.

    2. `_featurize_drug(self, smiles)` — NEW private method: `from molecules import Molecules`/
       `convert_smile_to_feature` (repo utils; do NOT hand-roll atom/bond featurizer — Pitfall/Don't-Hand-Roll),
       build the `input_drug` dict + `mask` tensors (float64, on self.device). Cache `self._gene_tensor`
       (the 10,716 gene-index tensor) once at load.

    3. `_call_model(self, smiles, region_basal)` — replace the NotImplementedError (lines 604-608) with the
       VERIFIED forward (02-PATTERNS change 6): cast basal to float64, `unsqueeze(0)` -> [1,10716]
       (caller passes an ALREADY 0-1 normalized basal), fixed dose `[[1.0,0.0]]` float64 (Pitfall 4),
       `with torch.no_grad(): pred,_ = self._model(input_cell_gex=basal, input_drug=drug, input_gene=self._gene_tensor,
       mask=mask, input_pert_idose=dose, job_id="perturbed", epoch=0)`; return `pred.squeeze(0).float().cpu().numpy()`
       (absolute treated [10716]). Keep the docstring contract "return raw predicted treated; do NOT subtract here".

    4. Control pass for rule B (D-02 amendment): add a module constant
       `INERT_CONTROL_SMILES` (a fixed inert reference, e.g. an empty/methane-like reference) AND document
       in the config that control = `_call_model(INERT_CONTROL_SMILES, region_basal)` in the SAME region
       context. Update `run()` (lines 614-680) so for each region it computes
       `predicted_control_map[region] = self._call_model(INERT_CONTROL_SMILES, basal)` ONCE per region
       (not per drug), then passes `predicted_control_map` (not region_basal_map) to `assemble_cache`.
       (If the NeuralFingerprint rejects an empty graph, fall back to a fixed DMSO-like SMILES and note it
       in the SUMMARY — D-02 amendment allows either; cache makes it auditable.)

    5. Create `configs/liver_p2.yaml` (analog: run_p1_eda.py path block lines 50-77). Keys:
       `checkpoint_path: /raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt`,
       `checkpoint_sha256: fbee15f…` (full SHA from MANIFEST row 17),
       `gene_order_file: data/processed/spatial/multidcp_10716_symbols.txt` (+ its SHA),
       `dose_one_hot: [1.0, 0.0]`, `basal_normalization: {method: minmax_0_1, manifold_range: [0.018, 1.000]}`,
       `control_input: inert_empty_drug` (D-02), `cache_dir: data/processed/spatial/region_de_cache`,
       `organ: liver`, `species: [human, mouse]`.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import sys; s=open('src/spatial/region_signature.py').read(); ok=('best_model.pt' in s) and ('MultiDCP_CheMoE_AE' in s) and ('input_cell_gex' in s) and ('INERT_CONTROL_SMILES' in s) and ('NotImplementedError' not in s.split('def _call_model')[1][:400]); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python -c "import ast; ast.parse(open('src/spatial/region_signature.py').read()); print('parse ok')" && test -f configs/liver_p2.yaml && conda run -n dili_v04_env python -c "import yaml,sys; c=yaml.safe_load(open('configs/liver_p2.yaml')); sys.exit(0 if c.get('checkpoint_path','').endswith('best_model.pt') and c.get('dose_one_hot')==[1.0,0.0] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'best_model.pt' src/spatial/region_signature.py` >= 1 (row-17 path; stale dili_downstream path removed).
    - `grep -c 'chemoe_kpgt' src/spatial/region_signature.py` == 0 (row-18 NOT wired — S-B descoped).
    - `grep -c 'MultiDCP_CheMoE_AE' src/spatial/region_signature.py` >= 1 and `grep -c 'load_state_dict' src/spatial/region_signature.py` >= 1 (strict load).
    - `grep -c 'INERT_CONTROL_SMILES' src/spatial/region_signature.py` >= 1 (rule-B control pass, D-02).
    - The pure functions stay torch-free: `grep -n 'import torch' src/spatial/region_signature.py` only appears inside methods, never at module top.
    - `configs/liver_p2.yaml` loads and carries checkpoint_path (best_model.pt) + dose_one_hot [1.0,0.0] + basal_normalization minmax + control_input.
    - File parses; the pure suite still GREEN (`pytest tests/spatial/test_region_signature.py -m "not gpu"` exits 0).
  </acceptance_criteria>
  <done>load_model strict-loads MultiDCP_CheMoE_AE from the row-17 checkpoint, _call_model runs the VERIFIED forward returning absolute treated [10716], the rule-B inert control pass is wired per region (D-02), and configs/liver_p2.yaml pins the checkpoint/gene-order SHAs + dose + normalization. S-B/row-18 is not wired.</done>
</task>

<task type="auto">
  <name>Task 3: CUDA-hygiene cache driver — write the liver 3-vector cache for human + mouse; real-inference smoke GREEN</name>
  <read_first>
    - scripts/run_p1_eda.py (driver template: sys.path inject lines 43-44, ROOT/paths block 50-77, _assert_manifest_rows 82-118 with sys.exit(2), logging 125-130, imports-after-sys.path with noqa:E402 136-157, _write_report 164-168)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (cache_region_de.py NEW section: drives RegionSignatureCacher; CUDA-hygiene block has NO in-repo analog — build per Hard Rule 5 / Pitfall 7)
    - .planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md (Pitfall 1 normalize basal 0-1; Pitfall 7 CUDA hygiene; D-01 both species)
    - tests/spatial/test_model_load.py (the gpu RED smoke from 02-01 this task turns GREEN)
    - src/spatial/gene_alignment.py (align_to_gene_space — Visium genes -> 10,716 target order before normalization)
  </read_first>
  <action>
    Create `scripts/cache_region_de.py` (analog scripts/run_p1_eda.py) driving the now-real cacher:

    1. **CUDA hygiene FIRST (Hard Rule 5 / Pitfall 7), before `import torch`:** parse `--gpu` with argparse
       at the very top; auto-detect a free device by parsing `nvidia-smi --query-gpu=memory.used --format=csv,noheader,nounits`
       and picking the least-used GPU that is NOT the last free one (always leave one GPU free); set
       `os.environ["CUDA_VISIBLE_DEVICES"]` to the chosen id; ONLY THEN `import torch`. If `--gpu cpu`,
       force CPU. Put this block above all heavy imports.

    2. sys.path inject (run_p1_eda.py lines 43-44); logging.basicConfig (lines 125-130);
       `_assert_manifest_rows` (lines 82-118 analog): assert the checkpoint (best_model.pt) + the
       gene-order file are MANIFEST rows and the checkpoint SHA matches `fbee15f…`; `sys.exit(2)` if absent.

    3. Load `configs/liver_p2.yaml`. Build the gene-order tuple from
       `data/processed/spatial/multidcp_10716_symbols.txt` (the 10,716 cache column order, D-01).

    4. Build the liver region basal map per species: for HUMAN use a human liver Visium basal
       (yu2022_liver / andrews_liver) pseudobulked per zone; for MOUSE use the GSE272564 CONTROL arm
       (gse272564_mouse_liver_ctrl) pseudobulked per zone. Align each region basal to the 10,716 order via
       `align_to_gene_space`, then **0-1 min-max normalize to the manifold range** (Pitfall 1) BEFORE caching.
       (Zone labels here: periportal/pericentral; reuse the published-annotation-else-canonical-markers
       logic shared with 02-04 if available, else a documented zone partition.) Use the real liver drug
       set with resolved SMILES (reuse the P1 sibling-resolved SMILES tables; do NOT re-resolve from scratch).

    5. `cacher = RegionSignatureCacher(model_variant="multidcp_chemoe", gene_ids=<10716 tuple>, device=<chosen>)`;
       `cacher.load_model(checkpoint_path)`; `cache = cacher.run(pert_ids, smiles_map, region_basal_map)`.
       Write the 3-vector cache (de_array, treated_array, control_array as .npy) + manifest JSON under
       `data/processed/spatial/region_de_cache/{species}/`. Do this for BOTH human and mouse (D-01 scope).

    6. Real-inference sanity (Pitfall 1): log the prediction value range for the first (drug, region);
       assert finite and broadly within/near [0,1]; if std≈0 or far out of range, log a loud warning
       (the collapse signature). This is what the gpu smoke test_model_load.py asserts.

    Run the gpu smoke once to flip it GREEN. Pick a GPU that leaves one free.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast,sys; s=open('scripts/cache_region_de.py').read(); ast.parse(s); idx_env=s.find('CUDA_VISIBLE_DEVICES'); idx_torch=s.find('import torch'); sys.exit(0 if (idx_env!=-1 and idx_torch!=-1 and idx_env<idx_torch) else 1)" && conda run -n dili_v04_env python -m pytest tests/spatial/test_model_load.py -m gpu -x -q; echo "gpu_smoke_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CUDA_VISIBLE_DEVICES' scripts/cache_region_de.py` >= 1 and it is SET before `import torch` (the verify asserts ordering).
    - `grep -c 'nvidia-smi' scripts/cache_region_de.py` >= 1 (auto-detect free GPU, Hard Rule 5).
    - `grep -c 'treated_array\|control_array\|de_array' scripts/cache_region_de.py` >= 1 (writes the 3-vector cache).
    - `grep -c 'minmax\|min-max\|normaliz' scripts/cache_region_de.py` >= 1 (Pitfall 1 normalization applied).
    - Both species cached: `ls data/processed/spatial/region_de_cache/human/ data/processed/spatial/region_de_cache/mouse/` show .npy + manifest after the run.
    - `pytest tests/spatial/test_model_load.py -m gpu` exits 0 (strict-load 0/0 + forward sanity GREEN, not mocked).
  </acceptance_criteria>
  <done>cache_region_de.py sets CUDA_VISIBLE_DEVICES before importing torch (leaves one GPU free), normalizes the 10,716 basal to the manifold 0-1 range, and writes the 3-vector liver cache for human + mouse; the gpu real-inference smoke is GREEN (strict 0/0 load + in-range forward).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spatial code -> /raid frozen checkpoint | First-party SHA-pinned .pt loaded with weights_only=False; the kpgt-vs-CheMoE mixup is the integrity risk. |
| Visium basal -> model input | Untrusted distribution (lognorm) crossing into a [0,1]-trained encoder; wrong normalization silently corrupts every feature. |
| pure functions <-> impure model methods | torch/sys.path must stay inside impure methods so the pure DE/cache stays fixture-testable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering / data-integrity | Wrong checkpoint silently loaded (row-18 collapsed kpgt instead of row-17) | mitigate | SHA-pin best_model.pt to fbee15f in config + assert SHA at load; strict_load=True must be 0/0 or FAIL; `grep chemoe_kpgt == 0`. |
| T-02-04 | Tampering | Silent OOD feature corruption from un-normalized basal (Pitfall 1) | mitigate | 0-1 min-max normalize to manifold range before _call_model; range-sanity assertion in the driver + gpu smoke. |
| T-02-05 | Spoofing / data-integrity | Fabricated/mocked model output bypassing Hard Rule 1 | mitigate | Real inference only; NotImplementedError discipline removed only by real strict-load; gpu smoke is not mocked. |
| T-02-06 | Elevation (resource) | Taking the last free GPU on the shared box | accept->mitigate | nvidia-smi auto-detect + always leave one GPU free (Hard Rule 5). |
</threat_model>

<verification>
- region_signature.py: rule-B compute_de, 3-vector cache (D-03), N_PDG (D-01), updated de_convention; load_model strict-loads MultiDCP_CheMoE_AE from row-17; _call_model returns absolute treated [10716]; rule-B inert control pass per region (D-02); pure functions torch-free.
- configs/liver_p2.yaml pins checkpoint + gene-order SHAs, dose one-hot [1.0,0.0], 0-1 normalization, control-input definition.
- cache_region_de.py sets CUDA_VISIBLE_DEVICES before import torch, leaves one GPU free, normalizes basal, writes the 3-vector liver cache for human + mouse.
- gpu real-inference smoke GREEN (strict 0/0 + in-range forward); pure suite GREEN.
- S-B/row-18 NOT wired; descope recorded.
</verification>

<success_criteria>
- WIRE-01 satisfied: the NotImplementedError seam is replaced with the real frozen forward path; per-region predicted DE cached for liver in both species (D-01); rule B with cached control (D-02/D-03).
- The model I/O + cache live in the 10,716 PDG space (D-01) with manifold-normalized basal (Pitfall 1).
- D-04 amendment honored: only S-C (row-17) wired; S-B (row-18) descoped as a provenance blocker, recorded in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multidcp-wiring-tox-head/02-02-SUMMARY.md`,
explicitly recording the S-B (row-18) checkpoint-provenance descope/blocker (D-04 amendment)
and the chosen rule-B control input (D-02 amendment: empty-drug vs DMSO-like fallback).
</output>
