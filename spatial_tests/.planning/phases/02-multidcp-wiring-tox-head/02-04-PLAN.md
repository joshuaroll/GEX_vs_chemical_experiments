---
phase: 02-multidcp-wiring-tox-head
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/spatial/apap_validation.py
  - scripts/run_apap_validation.py
  - results/tables/P2_apap_validation.md
autonomous: true
requirements: [WIRE-03]

must_haves:
  truths:
    - "Per-zone Pearson of predicted-vs-measured DE is computed on the mouse APAP anchor: predicted DE comes from the WIRE-01 rule-B mouse cache (D-02); measured DE = pseudobulk(APAP_zone) - pseudobulk(control_zone) on GSE272564's matched arms (D-06 primary), with GSE280652 as a weaker partial replication (no matched control, D-06 amendment)."
    - "Zones are assigned by published annotation if present, else canonical zonation markers (pericentral Glul/Cyp2e1; periportal Sds/Cyp2f2) per D-07."
    - "The Pearson is over the 10,716 ∩ ortholog ∩ Visium intersection (mouse->human via the P0 one2one ortholog map), absent genes FLAGGED via a present_mask not silently zero-filled, and n_genes_compared reported per zone (D-08)."
    - "Halt Gate 3 is keyed to the PERICENTRAL zone (where APAP injury classically acts): pericentral Pearson < 0.3 -> write HALT_REASON.md into the phase dir and stop-and-REFRAME the spatial claim (D-08/D-09), mirroring the P1 halt pattern; all zones are reported regardless."
    - "The rodent-anchor asymmetry is stated honestly in the report: the gate is rodent while the headline organ is human (D-09 accepted)."
  artifacts:
    - path: "src/spatial/apap_validation.py"
      provides: "zone assignment (D-07), measured_zone_de (APAP-ctrl, ortholog-aligned, present-mask), zone_pearson (D-08), halt_gate_3_fires (pericentral<0.3)"
      contains: "halt_gate_3_fires"
    - path: "scripts/run_apap_validation.py"
      provides: "driver: load GSE272564 (split arms, Pitfall 6) + mouse cache, compute per-zone Pearson, write P2_apap_validation.md, fire Halt Gate 3 on pericentral<0.3"
      contains: "GSE272564"
    - path: "results/tables/P2_apap_validation.md"
      provides: "per-zone predicted-vs-measured Pearson table + intersection sizes + Halt-Gate-3 verdict + rodent-anchor caveat"
      contains: "pericentral"
  key_links:
    - from: "data/processed/spatial/region_de_cache/mouse (WIRE-01 rule-B cache)"
      to: "scripts/run_apap_validation.py predicted-DE source"
      via: "load cached mouse APAP-drug per-zone DE (rule B, D-02)"
      pattern: "region_de_cache"
    - from: "src.spatial.apap_validation.measured_zone_de"
      to: "src.spatial.apap_validation.zone_pearson"
      via: "measured DE = APAP_zone - ctrl_zone (GSE272564), mouse->human ortholog align, present_mask -> per-zone Pearson over the intersection (D-08)"
      pattern: "present_mask"
    - from: "src.spatial.apap_validation.halt_gate_3_fires"
      to: ".planning/phases/02-multidcp-wiring-tox-head/HALT_REASON.md"
      via: "pericentral Pearson < 0.3 -> write HALT_REASON.md + stop-and-reframe (D-08/D-09)"
      pattern: "HALT_REASON"
---

<objective>
WIRE-03 — implement `src/spatial/apap_validation.py` and its driver to report the
predicted-vs-measured per-zone Pearson on the mouse APAP anchor, and enforce Halt
Gate 3 (pericentral Pearson < 0.3 -> write HALT_REASON.md, stop-and-reframe). This
is the phase's only direct-validity evidence: predicted DE (from the WIRE-01 rule-B
mouse cache, D-02) vs measured DE (APAP_zone - control_zone on GSE272564's matched
arms, D-06), per zone, over the 10,716 ∩ ortholog ∩ Visium intersection (D-08).

Purpose: test whether the predicted, region-resolved signature recovers the
classical pericentral APAP injury pattern — the live bet after P1's reframe. A fired
gate is a reportable stop-and-reframe (D-09), not a bug.

Output: `src/spatial/apap_validation.py` (zone assignment, measured DE, per-zone
Pearson, gate); `scripts/run_apap_validation.py`; `results/tables/P2_apap_validation.md`;
and, ONLY if pericentral < 0.3, `HALT_REASON.md` in the phase dir.

**Scope boundary:** Validation path ONLY — it reads Visium + the cache, NEVER feeds
the model and NEVER uses the APAP data as basal input (Pitfall 6: GSE272564's control
arm is the basal-context candidate, its APAP arm is the anchor; split by GEO sample
metadata). Do NOT modify any test file (Wave 0 owns them) — turn test_apap_validation.py
GREEN by editing source only. GSE280652 is a weaker partial replication (no matched
control, D-06 amendment), not an independent matched-DE anchor. No splits/training.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-multidcp-wiring-tox-head/02-CONTEXT.md
@.planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md
@.planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md

# Analogs to MIRROR + the driver template:
@src/spatial/eda/region_diagnostics.py
@src/spatial/pseudobulk.py
@src/spatial/gene_alignment.py
@scripts/run_p1_eda.py
@.planning/phases/01-eda-the-bracket/HALT_REASON.md

<interfaces>
<!-- Contracts. test_apap_validation.py (02-01) asserts these; region_diagnostics is the template. -->

# region_diagnostics.py (analog: ortholog-mapped, intersection-flagged Pearson):
#   human_mouse_liver_correlation(...) lines ~266-340 : h_to_m / h_idx / m_idx lookup, scipy.stats.pearsonr,
#   reports n_genes_compared, raises ValueError if < 2 matched genes  -> zone_pearson mirrors this per zone.

# pseudobulk.py:
#   from_anndata(adata, obs_col="zone", agg="mean") -> per-zone pseudobulk (PseudobulkResult)

# gene_alignment.py:
#   align_to_gene_space(profile, source_ids, target_ids, missing="nan")  # nan-flag absent genes (D-08)

# orthology.py (P0 output map):
#   build_one2one_orthologs(<data/processed/spatial/orthologs_h_m_r_one2one.tsv>) -> OrthologTable.pairs
#   columns: human_symbol, mouse_symbol  (15,956 one2one pairs)

# apap_validation.py (THIS plan writes; test_apap_validation.py asserts):
#   assign_zones(adata, annotation=None, markers={pericentral:[Glul,Cyp2e1], periportal:[Sds,Cyp2f2]}) -> per-spot zone labels (D-07)
#   measured_zone_de(apap_adata, ctrl_adata, target_human_ids, ortholog_table) -> {zone: (de_vec_10716, present_mask)}  # D-08
#   zone_pearson(pred_de, measured_de, present_mask) -> {"pearson_r","p_value","n_genes_compared"}  # raises if <2
#   halt_gate_3_fires(per_zone_r: dict, threshold=0.3, key_zone="pericentral") -> bool   # D-08/D-09

# Predicted DE source: data/processed/spatial/region_de_cache/mouse/  (WIRE-01 rule-B cache, D-02)
# APAP anchor: GSE272564 RAW.tar (matched ctrl + APAP arms; split by GEO sample metadata, Pitfall 6) — D-06 primary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: apap_validation.py — zones, measured DE (ortholog-flagged), per-zone Pearson, Halt Gate 3 (GREEN test_apap_validation.py)</name>
  <read_first>
    - src/spatial/eda/region_diagnostics.py (human_mouse_liver_correlation lines ~266-340: the EXACT ortholog-mapped, intersection-flagged Pearson template — h_to_m/h_idx/m_idx, pearsonr, n_genes_compared, <2 ValueError; guarded scanpy import line ~116)
    - src/spatial/pseudobulk.py (from_anndata obs_col grouping, NaN-aware agg)
    - src/spatial/gene_alignment.py (align_to_gene_space missing="nan" -> present-mask source)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (apap_validation NEW section: zone markers, measured DE = APAP-ctrl, ortholog map, present-mask not zero-fill, scanpy import discipline)
    - tests/spatial/test_apap_validation.py (the RED tests this task turns GREEN: zone, measured_de, pearson_gate)
  </read_first>
  <behavior>
    - assign_zones: spots high in Glul/Cyp2e1 -> pericentral; high in Sds/Cyp2f2 -> periportal (D-07 markers).
    - measured_zone_de: returns per-zone (de_vec over 10,716 human order, present_mask), de = APAP_zone - ctrl_zone,
      mouse->human ortholog-aligned, absent genes flagged via present_mask (NOT zeroed), n_genes_compared derivable.
    - zone_pearson: dict {pearson_r, p_value, n_genes_compared}; raises ValueError if present_mask.sum() < 2.
    - halt_gate_3_fires({"pericentral":0.21,"periportal":0.6}) is True; ({"pericentral":0.45,...}) is False (D-08).
  </behavior>
  <action>
    Create `src/spatial/apap_validation.py` (primary analog region_diagnostics.py). Standard module
    docstring + "Hard rules honored" block (pure-where-possible; scanpy/squidpy imported INSIDE the load
    function never at module top — Pitfall 9; validation-only, never feeds the model; DE-rule discipline).

    1. `assign_zones(adata, annotation=None, markers=None)` (D-07): if `annotation` (published per-spot
       labels) present, use it; else default markers `{"pericentral":["Glul","Cyp2e1"], "periportal":["Sds","Cyp2f2"]}`
       and label each spot by which marker set scores higher (mean of available marker genes; spots below a
       documented threshold -> "unassigned" and excluded). Return a per-spot label array. Leiden is last resort
       (not implemented here unless markers absent).

    2. `measured_zone_de(apap_adata, ctrl_adata, target_human_ids, ortholog_table, agg="mean")` (WIRE-03, D-08):
       pseudobulk each arm per zone via `pseudobulk.from_anndata(obs_col="zone")`; for each zone compute
       `de_mouse = apap_zone - ctrl_zone` in MOUSE symbols; map mouse->human via `ortholog_table` (h_to_m/h_idx/m_idx
       lookup mirrored from region_diagnostics lines ~307-340); `align_to_gene_space(de, human_syms, target_human_ids,
       missing="nan")` so absent genes are NaN; build `present_mask = ~np.isnan(aligned)`. Return
       `{zone: (de_vec_10716, present_mask)}`. Report intersection size = present_mask.sum() per zone.

    3. `zone_pearson(pred_de, measured_de, present_mask)` (D-08): `idx = np.where(present_mask)[0]`; if
       `idx.size < 2` raise ValueError (mirror region_diagnostics <2 guard); `r,p = pearsonr(pred_de[idx], measured_de[idx])`;
       return `{"pearson_r": float(r), "p_value": float(p), "n_genes_compared": int(idx.size)}`. Absent genes are
       NEVER included (flag-not-zero, D-08).

    4. `halt_gate_3_fires(per_zone_r, threshold=0.3, key_zone="pericentral")` (D-08/D-09): return
       `per_zone_r[key_zone] < threshold` (gate keyed to pericentral — APAP's classical site). All zones are
       reported by the caller regardless; only the pericentral value gates.

    Add `__all__`. Run test_apap_validation.py RED -> GREEN.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast,sys; ast.parse(open('src/spatial/apap_validation.py').read()); s=open('src/spatial/apap_validation.py').read(); ok=('def assign_zones' in s and 'def measured_zone_de' in s and 'def zone_pearson' in s and 'def halt_gate_3_fires' in s and 'present_mask' in s and 'pericentral' in s); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python -m pytest tests/spatial/test_apap_validation.py -q -m "not gpu"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def assign_zones\|def measured_zone_de\|def zone_pearson\|def halt_gate_3_fires' src/spatial/apap_validation.py` == 4.
    - `grep -c 'Glul\|Cyp2e1\|Sds\|Cyp2f2' src/spatial/apap_validation.py` >= 2 (D-07 canonical markers).
    - `grep -c 'present_mask' src/spatial/apap_validation.py` >= 2 (flag-not-zero, D-08).
    - `grep -c 'pericentral' src/spatial/apap_validation.py` >= 1 (gate keyed to pericentral, D-08).
    - scanpy import is function-local: `grep -n 'import scanpy' src/spatial/apap_validation.py` not at module top (Pitfall 9).
    - `pytest tests/spatial/test_apap_validation.py -m "not gpu"` exits 0 (zone/measured_de/pearson_gate GREEN).
  </acceptance_criteria>
  <done>apap_validation.py implements zone assignment (D-07 markers), measured DE = APAP_zone - ctrl_zone with mouse->human ortholog alignment and a present_mask (D-08, flag-not-zero), per-zone Pearson (raises <2), and the pericentral-keyed Halt-Gate-3 predicate (D-08/D-09); test_apap_validation.py is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: APAP driver — GSE272564 arms + mouse cache -> per-zone Pearson report + Halt Gate 3 firing</name>
  <read_first>
    - scripts/run_p1_eda.py (driver template: sys.path 43-44, _assert_manifest_rows 82-118 sys.exit(2), logging 125-130, imports noqa:E402, _write_report 164-168, and the _write_halt_reason / gate_fires / sys.exit(1) pattern to MIRROR for Halt Gate 3)
    - .planning/phases/01-eda-the-bracket/HALT_REASON.md (the P1 halt-record format to mirror: trigger, value, decision = stop-and-reframe, dated)
    - .planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md (Pitfall 5 mouse symbols + ortholog map; Pitfall 6 GSE272564 split arms by GEO metadata; D-06 GSE272564 primary, GSE280652 partial replication)
    - src/spatial/apap_validation.py (the functions from Task 1)
    - src/spatial/datasets.py (gse272564_apap_liver + gse272564_mouse_liver_ctrl share the same RAW.tar — split arms; gse280652_apap_liver APAP-only)
  </read_first>
  <action>
    Create `scripts/run_apap_validation.py` (analog run_p1_eda.py):

    1. sys.path inject; logging.basicConfig; `_assert_manifest_rows`: assert GSE272564_RAW.tar (+ GSE280652
       for the replication) and the ortholog map + gene-order file are MANIFEST rows; `sys.exit(2)` if absent.
       (No torch needed here — this path is scanpy/numpy only, no CUDA hygiene block required.)

    2. Load GSE272564 with scanpy (function-local import). Split spots/samples into the CONTROL arm and the
       APAP arm by GEO sample metadata (Pitfall 6 — the two registry entries share one RAW.tar; do NOT mix
       treated into control). `assign_zones` on each arm (published annotation if present else markers, D-07).

    3. Load the ortholog table (`build_one2one_orthologs(data/processed/spatial/orthologs_h_m_r_one2one.tsv)`)
       and the 10,716 human gene order. `measured_zone_de(apap_arm, ctrl_arm, target_human_ids, ortholog_table)`
       -> per-zone (measured_de_10716, present_mask). (Pitfall 5: mouse->human via the ortholog map; D-08
       absent genes flagged.)

    4. Load the predicted DE for the APAP drug (acetaminophen SMILES) from the WIRE-01 MOUSE cache
       (`data/processed/spatial/region_de_cache/mouse/`) per zone (rule B, D-02). If the APAP drug is not in
       the cached drug set, run it through the cacher first (reuse RegionSignatureCacher; this is the one place
       the driver may need the frozen model — if so, add the CUDA-hygiene block before import torch).

    5. For each zone: `zone_pearson(pred_de, measured_de, present_mask)` -> {pearson_r, p_value, n_genes_compared}.
       Build the report table over ALL zones. Compute `gate_fires = halt_gate_3_fires({zone: r for ...})`
       (pericentral-keyed, <0.3, D-08).

    6. Write `results/tables/P2_apap_validation.md`: a per-zone table (zone | predicted-vs-measured Pearson |
       p | n_genes_compared), the GSE272564 primary result, the GSE280652 partial-replication note (no matched
       control, D-06 amendment), the explicit rodent-anchor-for-a-human-headline caveat (D-09), and the
       Halt-Gate-3 verdict line.

    7. Halt Gate 3 (mirror run_p1_eda.py `_write_halt_reason` + gate_fires + sys.exit pattern, and P1's
       HALT_REASON.md format): if `gate_fires`, write `.planning/phases/02-multidcp-wiring-tox-head/HALT_REASON.md`
       with trigger ("pericentral predicted-vs-measured Pearson {value} < 0.3"), the per-zone table, decision =
       **stop-and-REFRAME the spatial claim** (D-09, NOT abandon), dated, and `sys.exit(1)`. If the gate does NOT
       fire, write the report and exit 0. Capture the run with `; echo "exit=$?"` so a firing exit-1 is visible
       without aborting downstream report checks.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast,sys; s=open('scripts/run_apap_validation.py').read(); ast.parse(s); ok=('GSE272564' in s) and ('halt_gate_3_fires' in s) and ('HALT_REASON.md' in s) and ('region_de_cache' in s) and ('stop-and-reframe' in s.lower() or 'reframe' in s.lower()); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python scripts/run_apap_validation.py; echo "apap_driver_exit=$?"; conda run -n dili_v04_env python -c "import sys; s=open('results/tables/P2_apap_validation.md').read(); sys.exit(0 if ('pericentral' in s and 'Pearson' in s and 'n_genes_compared' in s.lower().replace(' ','_') or 'genes' in s) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'GSE272564' scripts/run_apap_validation.py` >= 1 (primary anchor, arms split — Pitfall 6).
    - `grep -c 'region_de_cache' scripts/run_apap_validation.py` >= 1 (predicted DE from the WIRE-01 mouse cache).
    - `grep -c 'halt_gate_3_fires' scripts/run_apap_validation.py` >= 1 and `grep -c 'HALT_REASON.md' scripts/run_apap_validation.py` >= 1 (gate + halt writer).
    - `grep -ci 'reframe' scripts/run_apap_validation.py` >= 1 (stop-and-reframe, D-09).
    - `results/tables/P2_apap_validation.md` exists with a per-zone Pearson table including pericentral, intersection sizes, the GSE280652 partial-replication note, and the rodent-anchor caveat (D-09).
    - If pericentral < 0.3: `.planning/phases/02-multidcp-wiring-tox-head/HALT_REASON.md` exists and records the trigger + stop-and-reframe + date, and the driver exited 1 (firing is correct, not a failure).
  </acceptance_criteria>
  <done>run_apap_validation.py splits GSE272564's control/APAP arms (Pitfall 6), assigns zones (D-07), computes measured DE vs the WIRE-01 mouse rule-B predicted DE per zone over the ortholog-flagged intersection (D-08), writes P2_apap_validation.md with the rodent-anchor caveat (D-09), and fires Halt Gate 3 (pericentral<0.3 -> HALT_REASON.md + stop-and-reframe + sys.exit(1)) mirroring the P1 halt pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| APAP Visium -> analysis | GSE272564's two arms share one RAW.tar; mixing treated into control is the integrity risk (Pitfall 6). |
| mouse symbols -> human 10,716 | Cross-species mapping; un-flagged absent genes silently inflate/deflate the Pearson (D-08). |
| gate value -> halt record | A mis-keyed gate (wrong zone) would mis-fire/mis-clear Halt Gate 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering / data-integrity | APAP treated spots leaking into the control reference (Pitfall 6) | mitigate | Split GSE272564 arms by GEO sample metadata before pseudobulking; control arm is the basal candidate, APAP arm is the anchor. |
| T-02-11 | Tampering | Absent ortholog genes silently zero-filled into the Pearson (D-08) | mitigate | align_to_gene_space(missing="nan") + present_mask; zone_pearson uses only present indices; n_genes_compared reported per zone. |
| T-02-12 | Repudiation | Halt Gate 3 firing not durably recorded | mitigate | Write HALT_REASON.md into the phase dir (mirrors P1) + sys.exit(1); report states the verdict and the rodent-anchor caveat. |
</threat_model>

<verification>
- apap_validation.py: zone assignment (D-07), measured DE = APAP-ctrl ortholog-aligned with present_mask (D-08), per-zone Pearson (raises <2), pericentral-keyed Halt-Gate-3 predicate (D-08/D-09); test_apap_validation.py GREEN.
- run_apap_validation.py: GSE272564 arms split (Pitfall 6), predicted DE from the WIRE-01 mouse cache (rule B, D-02), per-zone Pearson over the ortholog-flagged intersection, P2_apap_validation.md written with the GSE280652 partial-replication note + rodent-anchor caveat (D-09).
- Halt Gate 3: pericentral<0.3 -> HALT_REASON.md + stop-and-reframe + sys.exit(1) mirroring P1; all zones reported regardless.
</verification>

<success_criteria>
- WIRE-03 satisfied: predicted-vs-measured per-zone Pearson reported on the APAP anchor (GSE272564 primary, GSE280652 partial replication); intersection sizes reported; no silent zero-fill (D-08).
- Halt Gate 3 enforced and keyed to pericentral (D-08); a fired gate writes HALT_REASON.md and is a stop-and-reframe (D-09).
- The rodent-anchor-for-a-human-headline asymmetry is stated honestly in the report (D-09).
</success_criteria>

<output>
After completion, create `.planning/phases/02-multidcp-wiring-tox-head/02-04-SUMMARY.md`,
recording the pericentral Pearson value, the Halt-Gate-3 verdict, and (if fired) the
stop-and-reframe note.
</output>
