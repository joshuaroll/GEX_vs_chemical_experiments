---
phase: 02-multidcp-wiring-tox-head
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/spatial/test_region_signature.py
  - tests/spatial/test_model_load.py
  - tests/spatial/test_tox_head.py
  - tests/spatial/test_apap_validation.py
  - tests/spatial/conftest.py
  - pytest.ini
autonomous: true
requirements: [WIRE-01, WIRE-02, WIRE-03]

must_haves:
  truths:
    - "Every Phase-2 behavior in 02-VALIDATION.md has a pytest test that imports the not-yet-written symbol and FAILS (RED) — rule-B DE math, 3-vector cache schema (D-03), tox-head zero-channel masking, zone assignment, measured-DE subtraction, per-zone Pearson + Halt-Gate-3 threshold (D-08)."
    - "The real-inference checkpoint-load + forward-sanity tests exist and are marked @pytest.mark.gpu so they are excluded from the pure suite (Hard Rule 1: real inference is NOT mocked; D-04 row-17 checkpoint)."
    - "The `gpu` pytest marker is registered so `-m 'not gpu'` selects the pure suite cleanly with no unknown-marker warning."
    - "Shared fixtures (tiny zone-marker AnnData, synthetic treated/control vectors, gene-order slice) live in conftest.py so all four Phase-2 test files reuse them."
  artifacts:
    - path: "tests/spatial/conftest.py"
      provides: "shared Phase-2 fixtures: zone-marker AnnData, synthetic treated/control 10716-slice vectors, gene-order slice"
      contains: "def "
    - path: "tests/spatial/test_model_load.py"
      provides: "gpu-marked real-ckpt strict-load + forward-sanity smoke (D-04 row-17, not mocked)"
      contains: "pytest.mark.gpu"
    - path: "tests/spatial/test_tox_head.py"
      provides: "WIRE-02 tests: zero-GEX-channel masking, combiner feed [B,n_regions,10716]->[B,10716]->logit"
      contains: "ToxHead"
    - path: "tests/spatial/test_apap_validation.py"
      provides: "WIRE-03 tests: zone assignment, measured DE = APAP_zone - ctrl_zone, per-zone Pearson + Halt-Gate-3 (<0.3 pericentral, D-08)"
      contains: "pericentral"
    - path: "pytest.ini"
      provides: "gpu marker registration"
      contains: "markers"
  key_links:
    - from: "tests/spatial/test_region_signature.py"
      to: "src.spatial.region_signature.compute_de (rule B), RegionSignatureCache (3-vector, D-03)"
      via: "import + RED assertion on treated/control subtraction and treated_array/control_array fields"
      pattern: "rule_b|cache_three_vector"
    - from: "tests/spatial/test_apap_validation.py"
      to: "src.spatial.apap_validation.zone_pearson + Halt-Gate-3 threshold"
      via: "import (ImportError until 02-04 writes module) + pericentral<0.3 assertion"
      pattern: "pearson_gate"
---

<objective>
Wave 0 — write the Nyquist test scaffolds for all of Phase 2 in the RED state,
add the shared `conftest.py`, and register the `gpu` marker. Per 02-VALIDATION.md
every Phase-2 behavior must have an `<automated>` test before the implementation
plans (02-02/03/04) run; this plan creates exactly those tests so the impl plans
go RED -> GREEN by editing source only.

Purpose: lock the contracts (rule-B DE, D-03 3-vector cache, tox-head masking,
zone/Pearson/Halt-Gate-3) as executable tests so the impl plans cannot drift, and
keep real-inference smokes (Hard Rule 1, not mocked) gpu-marked and out of the
pure suite.

Output: extended `tests/spatial/test_region_signature.py`; new
`tests/spatial/test_model_load.py` (gpu), `test_tox_head.py`,
`test_apap_validation.py`, `conftest.py`; `pytest.ini` with the `gpu` marker.

**Scope boundary:** WRITE TESTS ONLY. Do NOT implement any source under `src/spatial/`.
The pure tests must FAIL (RED) on missing symbols/fields; the gpu tests must be
collectible but skipped by the pure run. Do NOT touch `src/spatial/region_signature.py`
or any production module here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-multidcp-wiring-tox-head/02-CONTEXT.md
@.planning/phases/02-multidcp-wiring-tox-head/02-VALIDATION.md
@.planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md

# Analog test files to MIRROR (read before writing):
@tests/spatial/test_region_signature.py
@tests/spatial/test_region_combiner.py
@tests/spatial/test_eda_region.py
@tests/spatial/test_pseudobulk.py

<interfaces>
<!-- Contracts the future source must satisfy. Tests assert these; source comes in 02-02/03/04. -->

# region_signature.py (02-02 fills these — rule B + D-03):
#   compute_de(predicted_treated, predicted_control) -> np.ndarray   # rule B: pt - pc (NOT - basal)
#   RegionSignatureCache(de_array, treated_array, control_array, manifest)  # 3 vectors (D-03)
#   manifest.de_convention == "predicted_treated(drug) - predicted_control(region_basal)"
#   N_PDG == 10716   # new constant alongside N_LANDMARK=978 (D-01)

# region_combiner.py (EXISTS — feeds the tox head):
#   AttentionPoolCombiner(d=10716).forward(x)-> RegionCombinerOutput(pooled (*,d), attn_weights (*,n_regions))
#   input x: (*, n_regions, d)

# tox_head.py (02-03 writes this — WIRE-02):
#   ToxHead(d_gex=10716, d_chem=2048, d_dr=0, d_proj=256, hidden=(256,64), dropout=0.3)
#   forward(gex_pooled, chem_emb, dr_emb=None)-> ToxHeadOutput(logit, attn_weights)
#   zero GEX channel (torch.zeros(B,10716)) still yields a finite logit (condition A)

# apap_validation.py (02-04 writes this — WIRE-03 / D-08):
#   zone_pearson(pred_de, measured_de, present_mask)-> {"pearson_r","p_value","n_genes_compared"}
#   absent genes flagged via present_mask, NOT zero-filled (D-08)
#   Halt Gate 3: pericentral pearson_r < 0.3 -> stop-and-reframe (D-09)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conftest.py + gpu marker + RED tests for region_signature rule-B and 3-vector cache (WIRE-01)</name>
  <read_first>
    - tests/spatial/test_region_signature.py (the file being EXTENDED; reuse its fixture style lines 53-89 — move `three_genes`, `two_regions`, `two_pert_ids`, `region_basal_map` into conftest.py)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (region_signature MODIFY section: rule B = pt - predicted_control; D-03 adds treated_array/control_array; de_convention string)
    - src/spatial/region_signature.py (the CURRENT contract: compute_de(predicted_treated, region_basal) rule A; RegionSignatureCache(de_array, manifest); de_convention="predicted_treated - region_basal" — these are what the tests must assert AGAINST the new rule-B form so they go RED now)
  </read_first>
  <action>
    Create `tests/spatial/conftest.py` first. Move the shared fixtures verbatim from
    `test_region_signature.py` (`three_genes`, `two_regions`, `two_pert_ids`,
    `small_manifest`, `region_basal_map`) into it, and ADD two Phase-2 fixtures:
    - `synthetic_treated_control(three_genes)`: returns `(treated, control)` two float32
      vectors of len(three_genes) with known values so rule-B subtraction is checkable
      (e.g. treated = [3,5,7], control = [1,2,3] -> de = [2,3,4]).
    - `gene_order_slice()`: a 16-element tuple of human gene symbols read from the FIRST
      16 lines of `data/processed/spatial/multidcp_10716_symbols.txt` IS FORBIDDEN here
      (purity gate: no /raid, no real-data reads in tests) — instead hardcode a 16-symbol
      synthetic tuple (e.g. ("FLNC","MAP2K4",...)) for shape contracts only.
    Then DELETE those moved fixtures from `test_region_signature.py` (they resolve via conftest).

    Create `pytest.ini` at repo root registering the marker (mirror standard pytest form):
    ```ini
    [pytest]
    markers =
        gpu: real-inference smoke needing a GPU + frozen checkpoint (Hard Rule 1, not mocked); excluded by -m "not gpu"
    ```

    Extend `tests/spatial/test_region_signature.py` with RED tests (these will FAIL until
    02-02 flips rule A->B and adds the 3-vector cache):
    - `test_rule_b_de_subtracts_control(synthetic_treated_control)`: call
      `compute_de(treated, control)` and assert result == treated - control elementwise
      (float32). Under the CURRENT rule-A signature the 2nd arg is named `region_basal`
      but the math is identical; to force RED, ALSO assert the manifest convention:
      build a manifest and assert `manifest.de_convention == "predicted_treated(drug) - predicted_control(region_basal)"`.
      (Current code returns the rule-A string -> RED now, GREEN after 02-02.)
    - `test_cache_three_vector_schema(...)`: construct an `assemble_cache(...)` result and
      assert the returned `RegionSignatureCache` has attributes `treated_array`,
      `control_array`, AND `de_array`, each shape `(n_pert_ids, n_regions, n_genes)` float32,
      with `de_array == treated_array - control_array`. (Current cache has only `de_array`
      and `assemble_cache` takes a `region_basal_map` -> AttributeError/TypeError RED now.)
      Mark these two with a clear `# RED until 02-02` comment.
    - Add `from src.spatial.region_signature import N_PDG` and `test_n_pdg_constant()` asserting
      `N_PDG == 10716` (ImportError RED now; D-01).
    Keep the existing pure tests untouched (they must still collect).
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast; ast.parse(open('tests/spatial/conftest.py').read()); ast.parse(open('tests/spatial/test_region_signature.py').read()); print('parse ok')" && conda run -n dili_v04_env python -m pytest tests/spatial/test_region_signature.py -k "rule_b or cache_three_vector or n_pdg" -q; echo "expect_red_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/spatial/conftest.py` and `grep -c 'def ' tests/spatial/conftest.py` >= 5.
    - `grep -c 'gpu:' pytest.ini` >= 1 (marker registered).
    - `grep -c 'def test_rule_b_de_subtracts_control' tests/spatial/test_region_signature.py` == 1.
    - `grep -c 'treated_array' tests/spatial/test_region_signature.py` >= 1 and `grep -c 'control_array' tests/spatial/test_region_signature.py` >= 1.
    - `grep -c 'N_PDG' tests/spatial/test_region_signature.py` >= 1.
    - The targeted pytest run reports the new rule_b/cache_three_vector/n_pdg tests as FAILED or ERROR (RED), not passed — the contracts are not yet implemented.
  </acceptance_criteria>
  <done>conftest.py holds the shared + new Phase-2 fixtures, pytest.ini registers the gpu marker, and the region_signature rule-B / 3-vector-cache / N_PDG tests exist and are RED (fail on the rule-A convention string + missing treated_array/control_array/N_PDG).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for tox_head (WIRE-02) and the gpu-marked real-inference smoke (WIRE-01)</name>
  <read_first>
    - tests/spatial/test_region_combiner.py (analog: nn.Module forward shape tests, RegionCombinerOutput NamedTuple assertions)
    - src/spatial/region_combiner.py (AttentionPoolCombiner(d).forward(x:(*,n_regions,d))-> RegionCombinerOutput(pooled,(attn_weights)); the head consumes `.pooled`)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (tox_head NEW section: ToxHeadOutput NamedTuple; __init__ proj_gex/proj_chem/proj_dr; zero-channel masking; combiner feed)
    - .planning/phases/02-multidcp-wiring-tox-head/02-RESEARCH.md (Code Examples: load_model strict 0/0 into MultiDCP_CheMoE_AE; _call_model returns [10716] absolute treated; Pitfall 1 normalization sanity [0,1])
  </read_first>
  <action>
    Create `tests/spatial/test_tox_head.py` (analog test_region_combiner.py) with RED tests
    importing `from src.spatial.tox_head import ToxHead, ToxHeadOutput` (ImportError -> RED):
    - `test_zero_gex_channel_finite_logit()`: build `ToxHead(d_gex=10716, d_chem=2048, d_dr=0)`,
      feed `gex_pooled=torch.zeros(4,10716)`, `chem_emb=torch.randn(4,2048)`, assert
      `out.logit` is shape (4,) or (4,1) and `torch.isfinite(out.logit).all()` (condition A masking).
    - `test_combiner_feed()`: build `AttentionPoolCombiner(d=10716)`, pass
      `x=torch.randn(4,3,10716)` -> `.pooled` (4,10716) -> `ToxHead(...)`-> finite logit;
      assert the [B,n_regions,10716]->[B,10716]->logit chain (use `-k combiner_feed`).
    - `test_dr_channel_optional()`: `ToxHead(d_dr=0)` forward without dr_emb works (dose-response
      zero this phase).
    Keep batch sizes tiny; CPU only (no @pytest.mark.gpu — the head itself needs no GPU/ckpt).

    Create `tests/spatial/test_model_load.py` — the ONLY gpu-marked file. Decorate the module/tests
    with `@pytest.mark.gpu`. CANNOT be mocked (Hard Rule 1). Tests import the cacher and load the
    REAL row-17 checkpoint:
    - `test_strict_load_chemoe_ae()`: instantiate `RegionSignatureCacher(model_variant="multidcp_chemoe",
      gene_ids=<10716 tuple from data/processed/spatial/multidcp_10716_symbols.txt>)`,
      call `.load_model("/raid/home/joshua/projects/MultiDCP_CheMoE_pdg/src/best_model.pt")`,
      assert it does NOT raise and `cacher._model is not None`. (RED now: load_model raises
      NotImplementedError until 02-02.) NOTE: this file MAY read /raid paths and the gene-order
      file because it is an integration smoke, explicitly exempt from the pure-test purity gate;
      add a module docstring stating that exemption.
    - `test_forward_sane()` (`-k forward_sane`): one `_call_model(smiles="CCO", region_basal=<normalized 10716>)`
      returns finite `[10716]`; assert values broadly in/near [0,1] (manifold range sanity, Pitfall 1).
    Use `pytestmark = pytest.mark.gpu` so a pure run (`-m "not gpu"`) skips this file entirely.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast; ast.parse(open('tests/spatial/test_tox_head.py').read()); ast.parse(open('tests/spatial/test_model_load.py').read()); print('parse ok')" && conda run -n dili_v04_env python -m pytest tests/spatial/test_tox_head.py -q -m "not gpu"; echo "toxhead_red_exit=$?"; conda run -n dili_v04_env python -m pytest tests/spatial/test_model_load.py --collect-only -q -m gpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from src.spatial.tox_head import' tests/spatial/test_tox_head.py` >= 1.
    - `grep -c 'def test_combiner_feed' tests/spatial/test_tox_head.py` == 1 and `grep -c 'zeros' tests/spatial/test_tox_head.py` >= 1 (zero-channel masking).
    - `grep -c 'pytest.mark.gpu' tests/spatial/test_model_load.py` >= 1.
    - `grep -c 'best_model.pt' tests/spatial/test_model_load.py` >= 1 (row-17 checkpoint path).
    - test_tox_head.py runs RED (collection or import error / failing tests) under `-m "not gpu"`; test_model_load.py is collectible under `-m gpu`.
  </acceptance_criteria>
  <done>test_tox_head.py asserts the zero-GEX-channel finite logit + combiner feed and is RED on the missing ToxHead; test_model_load.py is gpu-marked, references the row-17 checkpoint, and is excluded from the pure suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests for apap_validation (WIRE-03 / Halt Gate 3, D-08)</name>
  <read_first>
    - tests/spatial/test_eda_region.py (analog: AnnData fixtures + ortholog-mapped Pearson tests; mirror its fixture/marker style)
    - tests/spatial/test_pseudobulk.py (analog: AnnData -> pseudobulk-per-label tests)
    - src/spatial/eda/region_diagnostics.py (human_mouse_liver_correlation lines ~266-340 — the ortholog-mapped, intersection-flagged Pearson template apap_validation.zone_pearson mirrors; the `<2 genes` ValueError)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (apap_validation NEW section: zone_pearson, mouse->human ortholog map, measured DE = APAP_zone - ctrl_zone, zone markers Glul/Cyp2e1 pericentral, Sds/Cyp2f2 periportal)
  </read_first>
  <action>
    Create `tests/spatial/test_apap_validation.py` (analog test_eda_region.py + test_pseudobulk.py)
    with RED tests importing `from src.spatial.apap_validation import (assign_zones, measured_zone_de,
    zone_pearson, halt_gate_3_fires)` (ImportError -> RED). Use the conftest zone-marker AnnData fixture.
    - `-k zone` `test_assign_zones_from_markers(zone_marker_adata)`: spots high in Glul/Cyp2e1 ->
      "pericentral", spots high in Sds/Cyp2f2 -> "periportal" (D-07 canonical markers). Assert
      per-spot labels match the fixture's planted zones.
    - `-k measured_de` `test_measured_de_apap_minus_control(...)`: given pseudobulked APAP-arm and
      control-arm zone vectors, assert `measured_zone_de` returns APAP_zone - ctrl_zone per zone,
      and that mouse->human ortholog alignment reports `n_genes_compared` (intersection size), with
      absent genes flagged via a present_mask, NOT zero-filled (D-08).
    - `-k pearson_gate` `test_zone_pearson_and_halt_gate3()`:
        * `zone_pearson(pred_de, measured_de, present_mask)` over a tiny vector returns a dict with
          keys `pearson_r`, `p_value`, `n_genes_compared`; with `present_mask.sum() < 2` it raises
          ValueError (mirror region_diagnostics `<2` guard).
        * `halt_gate_3_fires({"pericentral": 0.21, "periportal": 0.6})` returns True (pericentral
          0.21 < 0.3, D-08 gate keyed to pericentral); `halt_gate_3_fires({"pericentral": 0.45,...})`
          returns False. (D-09: fired = stop-and-reframe.)
    Keep all arrays small synthetic; pure (no /raid, no real Visium). Add a `# RED until 02-04` comment.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast; ast.parse(open('tests/spatial/test_apap_validation.py').read()); print('parse ok')" && conda run -n dili_v04_env python -m pytest tests/spatial/test_apap_validation.py -q -m "not gpu"; echo "apap_red_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from src.spatial.apap_validation import' tests/spatial/test_apap_validation.py` >= 1.
    - `grep -c 'pericentral' tests/spatial/test_apap_validation.py` >= 2 (zone + gate-keyed-to-pericentral).
    - `grep -c 'halt_gate_3_fires' tests/spatial/test_apap_validation.py` >= 2 (fires True + False cases).
    - `grep -c 'n_genes_compared' tests/spatial/test_apap_validation.py` >= 1 (intersection size reported, D-08).
    - The run is RED (ImportError on the missing module) under `-m "not gpu"`.
    - Purity: `grep -c '/raid' tests/spatial/test_apap_validation.py` == 0.
  </acceptance_criteria>
  <done>test_apap_validation.py asserts zone assignment (canonical markers), measured DE = APAP_zone - ctrl_zone with ortholog-flagged intersection (D-08), per-zone Pearson, and the pericentral-keyed Halt-Gate-3 (<0.3) fire/no-fire (D-08/D-09); it is RED on the missing module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> source modules | Tests import not-yet-written symbols; RED state is the intended signal, not a defect. |
| gpu test -> /raid checkpoint | The only place a real first-party checkpoint is touched in this plan (read-only, in 02-02 it loads). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | A pure test silently passing against a stubbed/mocked model output | mitigate | Real inference lives only in the gpu-marked test_model_load.py; pure tests assert pure math only (Hard Rule 1). |
| T-02-02 | Information disclosure | Pure tests reading /raid real-data paths (purity-gate violation) | mitigate | Acceptance greps assert `grep -c '/raid' == 0` in the pure test files; only test_model_load.py (explicitly exempt) reads /raid. |
</threat_model>

<verification>
- conftest.py + pytest.ini present; gpu marker registered; pure suite selectable via `-m "not gpu"`.
- All Phase-2 behaviors from 02-VALIDATION.md have a test; pure tests are RED on missing symbols/fields; gpu smoke is collectible and excluded from the pure run.
- No production source under src/spatial/ modified.
</verification>

<success_criteria>
- Wave 0 complete: every WIRE-01/02/03 behavior in the 02-VALIDATION map has an `<automated>` test.
- Real-inference smokes are gpu-marked, reference the row-17 checkpoint (D-04), and are not mocked (Hard Rule 1).
- The pure suite runs RED for the Phase-2 contracts (rule B, D-03 cache, ToxHead, apap_validation) and stays GREEN for all pre-existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multidcp-wiring-tox-head/02-01-SUMMARY.md`.
</output>
