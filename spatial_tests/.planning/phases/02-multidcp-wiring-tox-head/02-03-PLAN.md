---
phase: 02-multidcp-wiring-tox-head
plan: 03
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - src/spatial/tox_head.py
  - scripts/smoke_train_condA.py
autonomous: true
requirements: [WIRE-02]

must_haves:
  truths:
    - "src/spatial/tox_head.py is a concat-MLP nn.Module (proj_gex/proj_chem/proj_dr -> 3-layer MLP -> single logit) fed by AttentionPoolCombiner(d=10716).pooled, with per-condition zero-tensor masking so condition A (zero GEX channel) still yields a finite logit and no NaNs."
    - "The combiner feed is wired: [B, n_regions, 10716] -> AttentionPoolCombiner -> [B, 10716] -> proj_gex -> concat -> logit, and attn_weights pass through ToxHeadOutput for P6 interpretability."
    - "Condition A smoke-trains (structure-only, GEX channel = torch.zeros(B,10716), no frozen-model inference, no cache needed) for >=1 epoch with BCEWithLogits, loss moves, and wandb logging is confirmed."
    - "The chem channel uses ECFP4 (2048-d, P1 smiles_to_ecfp4) for the smoke-train (Claude's Discretion / RESEARCH A1); the ChemBERTa-vs-ECFP4 headline choice is deferred to P4."
  artifacts:
    - path: "src/spatial/tox_head.py"
      provides: "ToxHead concat-MLP + ToxHeadOutput NamedTuple; zero-channel masking; combiner-fed forward"
      contains: "class ToxHead"
    - path: "scripts/smoke_train_condA.py"
      provides: "CUDA-hygiene condition-A smoke-train driver (zero GEX tensor, BCEWithLogits, wandb)"
      contains: "wandb"
  key_links:
    - from: "src.spatial.region_combiner.AttentionPoolCombiner(d=10716)"
      to: "src.spatial.tox_head.ToxHead.forward (proj_gex)"
      via: ".pooled (B,10716) feeds proj_gex; attn_weights passed through to ToxHeadOutput"
      pattern: "pooled"
    - from: "scripts/smoke_train_condA.py"
      to: "src.spatial.tox_head.ToxHead"
      via: "condition A: zero GEX channel + ECFP4 chem; BCEWithLogits; >=1 epoch; wandb.log"
      pattern: "BCEWithLogits"
---

<objective>
WIRE-02 — implement `src/spatial/tox_head.py`, the trainable concat-MLP organ-tox
head (the ONLY module trained this phase, frozen backbone otherwise), fed by the
existing `AttentionPoolCombiner(d=10716)` with per-condition zero-tensor masking,
then smoke-train condition A (structure-only) to validate the head + training loop
+ wandb logging independent of the WIRE-01 cache.

Purpose: de-risk the head and the loop separately from the checkpoint/control-pass
questions (RESEARCH Pattern 3 — condition A feeds a zero GEX tensor and needs no
frozen-model inference), and prove the combiner -> head -> logit chain end to end.

Output: `src/spatial/tox_head.py` (ToxHead + ToxHeadOutput); new
`scripts/smoke_train_condA.py` with CUDA hygiene and wandb logging.

**Scope boundary:** Smoke-train scope = condition A ONLY (zero GEX channel). Do NOT
load the frozen checkpoint, do NOT read the cache, do NOT implement B/C/S-B/S-C
training (Phase 4). Do NOT modify any test file (Wave 0 owns them) — turn the
test_tox_head.py RED tests GREEN by editing source only. Dose-response channel is
zero this phase (d_dr=0); G/H deferred. No splits/APAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-multidcp-wiring-tox-head/02-CONTEXT.md
@.planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md

# Analog (the nn.Module + NamedTuple-output style to MIRROR) + the combiner it consumes:
@src/spatial/region_combiner.py
@scripts/run_p1_eda.py

<interfaces>
<!-- Contracts. AttentionPoolCombiner already exists; the head consumes .pooled. -->

# region_combiner.py (EXISTS):
#   class RegionCombinerOutput(NamedTuple): pooled: Tensor (*,d); attn_weights: Tensor|None (*,n_regions)
#   AttentionPoolCombiner(d:int, temperature=1.0).forward(x:(*,n_regions,d)) -> RegionCombinerOutput

# tox_head.py (THIS plan writes; test_tox_head.py from 02-01 asserts these):
#   class ToxHeadOutput(NamedTuple): logit: Tensor (B,) or (B,1); attn_weights: Tensor|None
#   ToxHead(d_gex=10716, d_chem=2048, d_dr=0, d_proj=256, hidden=(256,64), dropout=0.3)
#   forward(gex_pooled: (B,d_gex), chem_emb: (B,d_chem), dr_emb: (B,d_dr)|None=None) -> ToxHeadOutput
#   condition A: gex_pooled = torch.zeros(B, d_gex) -> still finite logit (zero-channel masking)

# P1 chem encoder (reuse for smoke-train chem channel):
#   src/spatial/eda/fingerprints.py: smiles_to_ecfp4(smiles_list) -> (fps (n,2048) uint8, valid_mask (n,) bool)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ToxHead concat-MLP + zero-channel masking + combiner feed (GREEN test_tox_head.py)</name>
  <read_first>
    - src/spatial/region_combiner.py (analog: nn.Module + RegionCombinerOutput NamedTuple; __init__ d-validation lines 119-131; forward shape-guard lines 148-152; the .pooled the head consumes)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (tox_head NEW section: ToxHeadOutput, __init__ proj_gex/proj_chem/proj_dr, 3-layer MLP GELU+dropout+BN, zero-tensor masking, combiner feed, ECFP4 chem A1)
    - tests/spatial/test_tox_head.py (the RED tests this task turns GREEN: zero_gex_channel_finite_logit, combiner_feed, dr_channel_optional)
  </read_first>
  <behavior>
    - test_zero_gex_channel_finite_logit: ToxHead(d_gex=10716,d_chem=2048,d_dr=0); gex=zeros(4,10716),
      chem=randn(4,2048) -> logit shape (4,) or (4,1), all finite.
    - test_combiner_feed: AttentionPoolCombiner(10716)(randn(4,3,10716)).pooled (4,10716) -> ToxHead -> finite logit.
    - test_dr_channel_optional: ToxHead(d_dr=0).forward(gex, chem) (no dr_emb) works.
  </behavior>
  <action>
    Create `src/spatial/tox_head.py` (analog region_combiner.py). Open with the standard module
    docstring + "Hard rules honored" block (pure library wrt no-/raid; nn.Module; CON-tox-head 3-layer
    concat-MLP; dose-response is a first-class channel but zeroed this phase).

    `ToxHeadOutput(NamedTuple)` with `logit: torch.Tensor`, `attn_weights: torch.Tensor | None`.

    `class ToxHead(nn.Module)`:
    - `__init__(self, d_gex=10716, d_chem=2048, d_dr=0, d_proj=256, hidden=(256,64), dropout=0.3)`:
      validate `d_gex>0`, `d_chem>0`, `d_dr>=0`, `d_proj>0` (mirror region_combiner __init__ validation).
      `self.proj_gex = nn.Linear(d_gex, d_proj)`; `self.proj_chem = nn.Linear(d_chem, d_proj)`;
      `self.proj_dr = nn.Linear(d_dr, d_proj) if d_dr>0 else None`.
      Concat dim = `d_proj * (2 if proj_dr is None else 3)`. Build a 3-layer MLP:
      `Linear(concat, hidden[0]) -> BatchNorm1d(hidden[0]) -> GELU -> Dropout(dropout) ->
       Linear(hidden[0], hidden[1]) -> GELU -> Dropout -> Linear(hidden[1], 1)` (single logit; CON-tox-head).
    - `forward(self, gex_pooled, chem_emb, dr_emb=None, attn_weights=None) -> ToxHeadOutput`:
      shape-guard (mirror region_combiner lines 148-152: require 2-D inputs). Project each channel;
      if `proj_dr is None` ignore dr; if a channel is the zero tensor it still projects (no special-casing —
      the zero tensor is a valid masked channel, condition A). Concat -> MLP -> `logit` (squeeze last dim to (B,)).
      Pass `attn_weights` through to `ToxHeadOutput.attn_weights`. Add `__all__`.

    Run the Wave-0 tox-head tests RED -> GREEN.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast,sys; ast.parse(open('src/spatial/tox_head.py').read()); s=open('src/spatial/tox_head.py').read(); sys.exit(0 if ('class ToxHead' in s and 'ToxHeadOutput' in s and 'proj_gex' in s) else 1)" && conda run -n dili_v04_env python -m pytest tests/spatial/test_tox_head.py -q -m "not gpu"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class ToxHead' src/spatial/tox_head.py` == 1 and `grep -c 'class ToxHeadOutput' src/spatial/tox_head.py` == 1.
    - `grep -c 'proj_gex\|proj_chem\|proj_dr' src/spatial/tox_head.py` >= 3 (three channels).
    - `grep -c 'BatchNorm1d\|GELU\|Dropout' src/spatial/tox_head.py` >= 2 (3-layer MLP per CON-tox-head).
    - `pytest tests/spatial/test_tox_head.py -m "not gpu"` exits 0 (zero-channel finite logit + combiner feed + dr-optional all GREEN).
  </acceptance_criteria>
  <done>tox_head.py exposes ToxHead (concat-MLP, 3 projected channels, single logit) + ToxHeadOutput; condition A's zero GEX channel yields a finite logit; the AttentionPoolCombiner .pooled feeds proj_gex and attn_weights pass through; test_tox_head.py is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Condition-A smoke-train driver (zero GEX channel, BCEWithLogits, wandb)</name>
  <read_first>
    - scripts/run_p1_eda.py (driver template: sys.path lines 43-44, logging 125-130, argparse sub-command + _write_report 164-168; the SMILES sibling-resolved tables already wired here — reuse, do not re-resolve)
    - .planning/phases/02-multidcp-wiring-tox-head/02-PATTERNS.md (smoke_train_condA.py NEW section: zero GEX tensor, head only, BCEWithLogits, wandb >=1 epoch loss decreases; CUDA hygiene block)
    - src/spatial/tox_head.py (the ToxHead built in Task 1)
    - src/spatial/eda/fingerprints.py (smiles_to_ecfp4 -> (n,2048) chem channel for condition A)
  </read_first>
  <action>
    Create `scripts/smoke_train_condA.py` (analog run_p1_eda.py driver + the CUDA-hygiene block):

    1. CUDA hygiene FIRST (Hard Rule 5 / Pitfall 7), before `import torch`: argparse `--gpu`;
       auto-detect a free GPU via `nvidia-smi` (least-used, never the last free one — always leave one free);
       set `os.environ["CUDA_VISIBLE_DEVICES"]`; `--gpu cpu` forces CPU; THEN `import torch`.

    2. sys.path inject; logging.basicConfig; `--epochs` (default small, e.g. 20) and `--wandb-mode`
       (default "online", allow "offline"/"disabled") args.

    3. Build a condition-A batch on REAL liver drug labels + SMILES (reuse the P1 sibling-resolved SMILES
       + DILIrank/DILIst labels already wired in run_p1_eda.py — do NOT re-resolve SMILES, do NOT fabricate
       labels, Hard Rule 1). chem channel = `smiles_to_ecfp4(smiles_list)` -> float32 (2048-d, A1).
       GEX channel = `torch.zeros(B, 10716)` (condition A — no cache, no frozen inference, RESEARCH Pattern 3).
       dr channel = none (d_dr=0).

    4. `head = ToxHead(d_gex=10716, d_chem=2048, d_dr=0)`; `opt = torch.optim.Adam(head.parameters(), lr=1e-3)`;
       `loss_fn = torch.nn.BCEWithLogitsLoss()`. Train >=1 epoch (default --epochs); each step:
       forward -> `loss_fn(out.logit, y_float)` -> backward -> step. Log `loss` to wandb each epoch
       (project: the project's standard wandb project, e.g. `joshroll/DILI_spatial_xspecies`; group
       `spatial_p2_smoke`; run name includes condition A). Assert the final epoch loss < the first epoch
       loss (loss moves) and print/log a one-line summary.

    5. `_write_report` a tiny smoke summary (epochs, first/last loss, wandb run url/id) so the run is auditable.

    Run the driver once (wandb offline if online not configured) to confirm the loop + logging.
  </action>
  <verify>
    <automated>cd /raid/home/joshua/projects/GEX_vs_chemical_experiments/spatial_tests && conda run -n dili_v04_env python -c "import ast,sys; s=open('scripts/smoke_train_condA.py').read(); ast.parse(s); ie=s.find('CUDA_VISIBLE_DEVICES'); it=s.find('import torch'); ok=(ie!=-1 and it!=-1 and ie<it) and ('BCEWithLogitsLoss' in s) and ('wandb' in s) and ('zeros' in s); sys.exit(0 if ok else 1)" && conda run -n dili_v04_env python scripts/smoke_train_condA.py --epochs 5 --wandb-mode offline; echo "smoke_exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CUDA_VISIBLE_DEVICES' scripts/smoke_train_condA.py` >= 1 and it is set BEFORE `import torch` (verify asserts ordering).
    - `grep -c 'nvidia-smi' scripts/smoke_train_condA.py` >= 1 (auto-detect free GPU, Hard Rule 5).
    - `grep -c 'BCEWithLogitsLoss' scripts/smoke_train_condA.py` >= 1 and `grep -c 'torch.zeros' scripts/smoke_train_condA.py` >= 1 (condition A zero GEX channel).
    - `grep -c 'wandb' scripts/smoke_train_condA.py` >= 2 (init + log).
    - The driver run prints a non-error summary and `smoke_exit=0` (loss moved over >=1 epoch; wandb logged offline).
  </acceptance_criteria>
  <done>smoke_train_condA.py sets CUDA_VISIBLE_DEVICES before importing torch (leaves one GPU free), feeds a zero GEX channel + ECFP4 chem on real labels, trains ToxHead >=1 epoch with BCEWithLogits, and logs loss to wandb — validating the head + loop + logging without the cache or frozen inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke driver -> GPU | Shared box; the driver must leave one GPU free (Hard Rule 5). |
| smoke driver -> labels/SMILES | Real DILI labels + sibling-resolved SMILES; no fabricated labels (Hard Rule 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing / data-integrity | Smoke-train on fabricated/synthetic labels | mitigate | Reuse real DILI labels + sibling-resolved SMILES wired in run_p1_eda.py; no synthetic labels (Hard Rule 1). |
| T-02-08 | Elevation (resource) | Smoke-train grabbing the last free GPU | mitigate | nvidia-smi auto-detect, leave one GPU free, CUDA_VISIBLE_DEVICES before import torch. |
| T-02-09 | Tampering | NaN logit from an unmasked zero channel slipping through | mitigate | test_tox_head.py asserts finite logit on the zero GEX channel (condition A). |
</threat_model>

<verification>
- tox_head.py: ToxHead concat-MLP (3 projected channels, single logit) + ToxHeadOutput; zero GEX channel -> finite logit; AttentionPoolCombiner .pooled feeds proj_gex; attn_weights pass through; test_tox_head.py GREEN.
- smoke_train_condA.py: CUDA hygiene before torch, zero GEX channel + ECFP4 chem on real labels, BCEWithLogits, >=1 epoch loss moves, wandb logged.
- No frozen checkpoint load, no cache read, no B/C/S training.
</verification>

<success_criteria>
- WIRE-02 satisfied: the concat-MLP head runs a working forward fed by the attention combiner with per-condition zero-tensor masking and no NaNs; condition A smoke-trains with wandb logging confirmed.
- Chem channel = ECFP4 for the smoke-train (A1); ChemBERTa-vs-ECFP4 deferred to P4.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multidcp-wiring-tox-head/02-03-SUMMARY.md`.
</output>
